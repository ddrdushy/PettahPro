# PettahPro Build Roadmap

Live tracker of what's shipped, what's next, and what's backlog — cross-checked against the 23 spec files in `/docs/` (see [`_summary.md`](./_summary.md) for a per-file digest).

**Three-doc triangle:** this file says what's shipped; [`_status.md`](./_status.md) says what's broken, fragile, or at-risk right now; [`_gaps.md`](./_gaps.md) is the parking lot for real gaps that aren't on the roadmap yet (security, cost-centers, bank feeds, e-filing, observability, platform workstreams). Promote items from `_gaps.md` into this file when it's their turn.

Last updated: 2026-04-23 after PR #82 "#35 kit/bundle items" (follows #81 document attachments, #80 purchase requisitions, #79 final-settlement routing, #78 payroll+bonus, #77 POs, #76 bills, #75 expense claims, #74 engine core + JE). **All must-haves, all original should-haves, #28 POS, #29 commission engine, #45 digest windows, #31 customer portal (+ hardening), #36 inventory categories, the full approval engine (#43 + #43a–#43e), #30 purchase requisitions, #32 document attachments, and now #35 kit/bundle items all shipped.** Bundles are the second nice-to-have off the M/L pile. Lightweight, no-BOM assembly: a bundle is a virtual SKU with a static component list; selling one explodes into per-component stock issues at invoice post time, rolling weighted-average COGS across components into a single JE.

---

## ✅ Shipped (PRs #1 – #73)

### Platform foundation
- Multi-tenant Postgres with RLS (`current_tenant_id()` + `SET LOCAL app.tenant_id`), enforced at a non-superuser `pettahpro_app` role so dev and prod both actually exercise the policies (PR #48)
- Cross-tenant auth reads go through SECURITY DEFINER helpers (`auth_find_user_by_email`, `auth_find_user_by_id`, `auth_email_in_use`, `auth_touch_last_login`) — same pattern as the recurring-invoice worker (PR #48)
- Auth: signup / login / logout / me / session cookies
- Branches (multi-location), warehouses (schema)
- `tenant_settings` JSONB store with `GET/PATCH /settings` (knobs: `salaryDaysPerMonth`, `stockRelieveOn`)
- Settings page replaces the old "Soon" placeholder
- BullMQ worker + Redis, hourly cron scaffold
- Notifications (header bell + red-dot badge, in-app only, routed per `refType`)
- Audit columns (created_at/updated_at, deleted_at, created_by_user_id)
- **Build status tracker** (`docs/_status.md` + PR template) — known bugs, typecheck debt baseline (47 → 32 errors after PR #47), fragile areas, regression log, module health; PR template forces every PR to declare modules touched + regression surface + test plan
- **Shared helpers for repeated patterns** — `pdfResponse(buffer, filename)` (apps/web) funnels every PDF route through one BodyInit-safe Response builder; `nextDocumentNumber(tx, kind)` (packages/db) wraps `SELECT next_document_number(...)` with typed kinds and undefined-guard so call sites can't silently destructure `undefined` (PR #47)

### Sell
- Customers (CRUD, statements, aging)
- Quotations (CRUD, send/accept/reject/expired/convert, **PDF**)
- Sales orders (CRUD, confirm, cancel, convert-to-invoice)
- Delivery notes (CRUD, deliver, cancel, **PDF** with signature block, optional stock relief at deliver time)
- Invoices (CRUD, post, void, duplicate, **PDF**, stock relief at post by default; `/app/invoices` defaults to `?channel=web` so POS receipts don't flood AR's daily list — PR #69 added `?channel=pos` and `?channel=all` filter pills for reconciliation)
- Recurring invoices (monthly templates, hourly BullMQ cron → draft invoices, pause/resume/generate-now)
- Credit notes (CRUD, post, **PDF** with draft banner — PR #46)
- Customer payments (cash/cheque/bank-transfer, cheque lifecycle linkage)
- **Proforma invoices** — pre-sale preview document for advance payments / customs purposes. CRUD + send → accepted → converted workflow; converts to a live invoice one-to-one (one proforma → one real invoice, idempotent). Separate `PRO-YYYY-NNNN` number series. **PDF** with clear non-fiscal banner and "proforma invoice — not a tax invoice" footer so it can't be mistaken for a live invoice (PR #51)
- **Customer statement email delivery** — on-demand send from the customer statement page plus a monthly cron that fires on day-1 for every customer with outstanding balance. `send_statements` per-customer opt-out, `customer_statement_emails` log table with success/failure per send, SMTP retries on transient failure. Bulk send summary shown in the customer list so AR can see at a glance who's been notified this cycle (PR #55)
- **POS (shift-based retail terminal)** — one-screen cashier terminal on top of the existing invoice + customer_payments plumbing. Cashier opens a shift with an opening float (`pos.operate` permission), rings up sales via item search + cart + tender tiles (cash, card, LankaQR, bank transfer), and closes the shift with a denomination-breakdown count (`pos.close` permission). Variance between expected and counted cash posts to **5190 Cash Over/Short**. Walk-in customer seeded per tenant so cash sales don't need a customer picker. Z-report view aggregates tender totals + sales per shift. Invoice post logic extracted into a shared `postDraftInvoice()` helper so POS and REST share one code path. POS-channel invoices carry `channel='pos'` so the default invoice list (`?channel=web`) hides them — AR sees them under `?channel=pos` for reconciliation. v1: single-cashier, in-store only, no offline mode (PR #67, permission keys added in PR #69)
- **Batch / consolidated invoicing** — roll multiple delivered delivery notes into one invoice per customer in one run. `POST /invoices/batch-from-delivery-notes` groups eligible DNs by customer, generates one draft invoice per group with all lines merged, and returns a per-customer success/skip summary. `/app/invoices/batch` web page lets AR pick the date window + customer filter, preview groupings, and fire the batch. Keeps one-invoice-per-customer atomicity so partial failures never strand a DN in a half-consumed state (PR #62)
- **Customer portal hardening** — follow-up to the v1 portal closing three security gaps: (1) `/portal/auth/verify` now rate-limits at 10 attempts per email per hour via Redis INCR (same shape as the OTP request budget), so an attacker can't brute-force the 6-digit code space inside its 10-min expiry; (2) audit events `portal.login`, `portal.logout`, and `portal.access_toggled` now flow into the existing `audit_events` stream so tenants see portal activity alongside admin actions in `/app/audit-log`; (3) new `customers.portal_enabled` toggle (default true) lets a tenant admin revoke portal access without archiving the customer — enforced both pre-auth (`portal_find_customers_by_email` filters it) and on every authenticated read (`portal_resolve_session` folds it into `customer_active`, so an existing session self-destructs on the next `/portal/auth/me` read). New admin route `PATCH /customers/:id/portal-access` records the toggle in the audit log. `portal.verify_failed` still logs to the fastify log (not audit_events) because we don't know the tenant on a bad-code verify (PR #72)
- **Customer portal (email+OTP self-serve)** — customers log in with email only; the API issues a 6-digit OTP (SHA-256 hashed, 10-min expiry, 5-per-hour rate limit per email), emails it, and on verify establishes a portal-scoped signed cookie `pp_portal_session` (path=/portal, distinct realm from admin `pp_session` — separate Redis prefix, different onRequest hook, belt-and-suspenders `customer_id = session.customerId` predicate on every read on top of `withTenant()` RLS). Cross-tenant safe: the same email at multiple tenants mints one OTP per (tenant, customer) pair with the same plaintext code, so the customer types one code and picks their tenant at verify if more than one matches (409 `AMBIGUOUS_TENANT` response carries candidates under `error.issues.candidates`). Enumeration-safe at request-otp — always returns 200 "sent". Portal pages (`/portal/*`) served by Next.js server components proxying to `/portal/*` API routes: invoice list + detail with **PDF download** (reuses the `InvoicePDF` renderer via a portal-scoped route), statement view (reuses `computeCustomerStatement`, default 6-month window), payments history with allocation drilldown, and recurring-template list. Drafts never leak — portal invoice detail returns 404 for `status='draft'`. Archived/inactive customers get 401 at `/portal/auth/me` so deactivating a customer instantly revokes portal access. SECURITY DEFINER helpers (`portal_find_customers_by_email`, `portal_mint_otp`, `portal_find_otp`, `portal_consume_otp`, `portal_resolve_session`) keep the pre-auth cross-tenant lookups off RLS (same pattern as `auth_find_user_*`). v1 scope: read-only self-serve; pay-online (#31 §3) explicitly deferred (PR #71)
- **Multi-currency on sales & purchases** — invoices, credit notes, bills, debit notes, and both payment sides carry optional `currency` + `fx_rate` + `foreign_total_cents` alongside the ledger-in-LKR `amount_cents`. Rate captured at document date from the `fx_rates` table (`/app/settings/fx-rates` CRUD for USD/EUR/GBP daily rates). v1 semantic: ledger stays in LKR (no dual-book revaluation yet), foreign fields drive document display only — PDFs show "USD 1,200.00 @ 305.50 = LKR 366,600.00". Unrealized FX revaluation at period close is a follow-up (see backlog). Realized FX gain/loss COA (4900 / 5900) seeded. Works transparently for single-currency tenants: if `currency` is null, everything behaves exactly as before (PR #61)

### Buy
- Suppliers (CRUD, statements)
- Purchase orders (CRUD, send/acknowledge/cancel/convert-to-bill, **PDF**)
- Goods received notes / GRNs (CRUD, receive, cancel, links to PO and bill)
- Bills (CRUD, post, void, **PDF** — draft banner on unposted bills so AP approvers can preview before posting)
- Debit notes (CRUD, post, **PDF** with draft banner — PR #46)
- Supplier payments (cash/cheque/bank-transfer, cheque lifecycle)
- **Recurring bill templates** — bills equivalent of recurring invoices. Template captures supplier + items + amount + frequency (weekly / monthly / quarterly / annual) + start/end dates + auto-post-vs-review flag. Hourly BullMQ cron generates drafts; review-queue variant lands in the bills list as draft, auto-post variant posts directly. Pause / resume / edit / end-date supported; variable-amount templates keep the structure fixed and prompt for the per-cycle amount (PR #51)
- **Supplier statement reconciliation** — upload (or paste CSV of) the statement your supplier sends you and reconcile it against what's on our bills/payments ledger. `POST /suppliers/:id/reconcile` returns a four-way classification per line: `matched` (exact ref + amount), `amount_mismatch` (ref match, different amount — likely their data entry error or a missed credit note), `only_in_ours` (we've booked it, they haven't — typical when we've posted a recent bill they haven't received), `only_in_theirs` (they claim it, we don't — investigate: lost bill, cleared cheque not yet in our system, or a dispute). `/app/suppliers/[id]/reconcile` web page for the workflow. AP finally has a tool for the monthly "what does the supplier statement say vs us" exercise without hand-ticking spreadsheets (PR #62)
- **Landed cost allocation** — freight / insurance / customs / clearing / loading / other charges captured as extra lines on a supplier bill. At post time those amounts allocate pro-rata across the bill's inventory lines (by `value` default, or by `quantity`) and fold into each item's unit cost before the stock receipt posts, so WAVG reflects the true landed cost. Largest-remainder rounding keeps the cents balanced; journal stays balanced (DR Inventory grows by base + allocated, CR AP matches). Charges on a bill with no inventory lines expense to a fallback `5130 Freight & handling` account (rare clearing-agent-only path). Client-side preview mirrors the server allocation algorithm. V2 follow-ups explicitly deferred (see #9b once scoped): post-GRN retrospective landed-cost bills, pro-rata by weight, per-line manual override, FX-tied landed cost (PR #59)

### Inventory
- Items (CRUD, WAVG valuation, reorder_point)
- Stock on-hand + stock ledger per item (inbound in-transit qty surfaced on the on-hand page)
- Low-stock report + crossing notifications
- **Stock counts (physical / cycle counts)** — blind-count workflow (counter can't see system qty until after entry), scope = full warehouse / category / specific items. Draft → in-progress → review → posted state machine; variance review shows System qty vs Counted qty vs Variance (qty + LKR) with per-line reason code (damage / theft / expiry / shrinkage / miscount / system-error / other). Tiered approval per inventory-module-spec §17.3: absolute variance ≤ 1% of counted value posts direct; > 1% routes to Owner per the SOD rule that the count executor ≠ variance approver. Post books the adjustment journal (Inventory adjustment ↔ Inventory) and writes `stock_count_adjustment` ledger entries; the count record becomes immutable once posted (PR #50)
- **Stock transfer between warehouses** — two-step draft → dispatched → received state machine; dispatch deducts source + writes `transfer_out` ledger + allocates transfer number; receive adds to destination at dispatch-time unit cost (preserving WAVG across warehouses) + writes `transfer_in` ledger; short receipts flag `has_discrepancy` on the header for later reconciliation; draft cancel supported (post-dispatch cancellation = reverse transfer); printable driver's note **PDF** with source/destination route and dispatched/received signatures

### Accounting
- Chart of accounts, tax codes (VAT, SSCL, WHT)
- Journal entries (manual + auto-posted from every module)
- Fixed assets (CRUD, monthly depreciation cron — PR #69 converted the old manual POST to `runMonthlyDepreciationForAllTenants` driven by a 24h BullMQ scheduled job that no-ops every day except the 1st; idempotent via the `(tenant, year, month)` unique index on `fixed_asset_depreciation_entries`)
- Bank reconciliation (CSV import, auto-match to customer/supplier payments + cleared cheques, reconcile)
- Cheques (9-state lifecycle: issued → handed-over → deposited → presented → cleared | bounced | stopped | cancelled | stale, with legal-action flag)
- Stale cheque auto-flag (PR #66) — daily cron transitions past-6-month cheques to `stale`, emits per-cheque notifications, offers reissue for issued-direction (new cheque linked via `replaced_by_cheque_id`, original JE untouched)
- **Period lock** (soft_closed / closed) enforced at `postJournal` choke point; month-end and year-end close with P&L→retained-earnings transfer; reopen audit trail
- **WHT** withheld at supplier-payment time; `/app/accounting/wht` dashboard with per-month balance, by-supplier totals, remittance history, and remit-to-IRD action
- **Opening balance** import for onboarding tenants from BUSY/Tally (TB grid with paste-CSV + one-shot posting guardrails)
- **Customer credit enforcement** — credit_hold hard block + credit_limit soft block at invoice post; auto-flag on 2+ bounced cheques
- **Bad debt write-off with VAT relief** — give up on collection cleanly, claim SL VAT Act §26 relief on 12+ month-old invoices, reverse if they pay later; `/app/reports/bad-debts` tallies it
- **Journal entry approval workflow** — tenant-set threshold above which manual JEs go to a drafts queue for second-pair-of-eyes approval; SOD-enforced (approver ≠ creator); `/app/journals/approvals` pending + recent queue
- **Recurring journal templates** — balanced-entry templates with variable prompts (amount placeholders resolved per cycle); auto-post variant books directly, review-queue variant lands in `/app/recurring-journals` for HR/accountant to adjust before posting. Per-template frequency (monthly / quarterly / annual) + next-run date; hourly cron fires due templates; every generated entry carries template provenance for audit (PR #52)
- **Number series config** — customise per-document prefix templates with token substitution (`INV-{YYYY}-{####}`, `FS-{YYYY}-{####}`, etc.) and reset period (yearly / monthly / continuous). Live preview before save; tenant-level override of the default per-kind template via `document_sequences.template`; default-template trigger on insert keeps existing kinds backfilled. Every callsite allocates via `nextDocumentNumber(tx, kind)` so changes here flow through uniformly (PR #53)
- **Audit log viewer** — append-only `audit_events` stream covering identity (login/logout), posting (journal.post/void, approve/reject), period (close/reopen/close_year), void (invoice/bill/payment), AR hygiene (bad_debt writeoff/reverse, credit hold/release), HR (exit, confirm probation, payroll post/void, final-settlement lifecycle), and settings (update, number_series.update) events. Viewer at `/app/audit-log` with filter-by-kind, date range, actor, and deep-link back to the affected entity (journal_entry / invoice / bill / employee / period / customer). `recordAuditEvent` never throws — a failed audit write doesn't break the primary action (PR #54)
- **FX rates catalogue** — `fx_rates` table keyed by `(tenant_id, currency, rate_date)` with `rate_cents_per_unit` storing LKR × 10⁴ per foreign unit for four decimals of precision. Admin page `/app/settings/fx-rates` to enter daily rates manually for USD/EUR/GBP (auto-fetch from CBSL / exchangerate.host is a v2 follow-up). Document capture uses the latest `rate_date ≤ document_date`, so back-dated documents pick up the contemporaneously-valid rate. Underpins the multi-currency fields on invoices/bills/payments shipped in PR #61
- **Commission engine** — tiered rules per sales rep (flat % of invoice, tiered by volume, on-collection vs on-invoice recognition). Accrual at invoice post books DR 5170 Commissions expense / CR 2260 Commissions payable; credit-note clawback reverses pro-rata, with a cumulative-cap guard so repeat CNs against the same invoice can't claw back more than 100% of the original earning (PR #69 hardening). Payroll integration bundles earned + unpaid commissions into the next run as a taxable earning, atomically claimed at draft via `applied_in_run_id` (same pattern as staff-loan EMIs and expense-claim reimbursements); payroll void releases the claim so commissions flow back to the next run. `/app/commissions/ledger` shows per-rep balance with a `netNegative` flag surfacing stuck balances (sorted ASC so negatives float to the top). Memo on a clawback annotates when the original earning was already paid out (PR #68, hardened in PR #69)
- **FX revaluation at period close** — standard LKAS 21 / IFRS re-measurement of open foreign AR (invoices) and AP (bills) to the closing rate on a user-picked `as_of_date`. `/app/accounting/fx-revaluation` drives a draft → posted → voided lifecycle: compute a draft to preview per-document deltas, post to book the Unrealized FX gain (4510) / loss (5510) journal against the AR (1100) / AP (2000) control accounts, or void to post a reversing JE. Incremental-delta semantics — each line records `cumulative_delta_cents` (vs issue-date rate) and `previous_cumulative_delta_cents` (the last posted run's cumulative for the same document), so the JE only books the incremental change. New runs naturally supersede prior ones without a month-start reversal. Unique `(tenant, as_of_date)` index where `status <> 'voided'` blocks duplicate active runs. Open foreign-outstanding computed proportionally from `amount_paid_cents / total_cents` — a pragmatic v1 approximation until the realized-FX-on-settlement path lands. v1 scope: invoices + bills (credit / debit notes float outside the "open balance" concept until explicitly allocated). Gated by `accounting.manage`; JE posts dated as-of so period-lock is enforced by `postJournal` (PR #65)

### HR / Payroll
- Employees (CRUD + salary structure assignments)
- Salary components (CRUD, earnings/deductions, EPF/ETF/PAYE flags)
- Payroll runs (create, post, pay, **void** — PR #69 added `POST /payroll-runs/:id/void` which reverses the JE via line-flip + `isReversed=true` when the run was posted, releases all bundled claims: commission earnings nulled back to `payout_run_id IS NULL`, salary revision arrears re-opened, loan EMI schedule rows unlocked, and expense-claim reimbursements released so they flow to the next run; EPF 8% employee / 12% employer, ETF 3%, PAYE)
- NP leave auto-deduction via `NOPAY-LV` component (pro-rated by tenant `salaryDaysPerMonth`)
- Paid + unpaid leave days shown on payslip + **PDF**
- Leave types (AL/CL/SL/ML/PL/NP seeded), requests (submit/approve/reject/cancel), allocations per year
- Statutory filings summary + remit (EPF, ETF, PAYE)
- **Salary revisions with auto-arrears** — HR records a back-dated rate change, the live basic updates immediately, the next payroll run auto-computes (new − previous) × intervening months as an ARREARS earning line (counts for EPF/ETF/PAYE); period-lock enforced on revision effective dates so you can't quietly rewrite closed months
- **Staff loan module** — apply → approve (SOD-enforced) → disburse (DR 1150 Employee loans receivable / CR bank); flat-rate amortization with installment schedule; EMIs auto-deduct as LOAN-REC on the next payroll run, atomically claimed at draft, principal and interest split on post (CR loans receivable / CR interest income); write-off moves outstanding to bad debt and waives remaining installments. Five SL-typical types (festival, salary advance, emergency, housing, vehicle) seeded per tenant with caps + defaults
- **Mid-period payroll events (pro-rata)** — mid-period joiners and leavers now earn for days actually worked. Exit workflow (`POST /employees/:id/exit`) stamps `exit_date` + `last_working_day` + notice period and flips status to resigned/terminated/retired/deceased. Probation confirmation (`POST /employees/:id/confirm-probation`) promotes status to active. Payroll compute expands eligibility to include in-period leavers, computes `daysWorked / daysInPeriod` and scales every earning by the ratio (basic, from_basic, percent_of_basic, fixed earnings); deductions stay unscaled (fixed obligations). Payslip shows an "N/M days" chip so HR can explain the smaller basic at a glance
- **Bonus schemes library** — tenant-configured bonus programs (Avurudu, Christmas, 13th-month, performance) with four formula types: `flat_amount` (cents), `percent_of_basic` (bps), `days_of_basic` (e.g. 15 days = half-month), or `manual` (HR enters per employee). Per-scheme eligibility (minimum tenure, employment types, statuses) + tax flags (counts for EPF / ETF / PAYE). Bulk bonus run workflow draft → posted → void: creating a draft filters eligible employees and seeds per-person amounts from the formula; HR can adjust any line before post; post books the journal (DR Salaries & wages / DR EPF+ETF employer / CR EPF+ETF payable / CR PAYE / CR Salaries payable) reusing the sl-tax compute engine for consistency with regular payroll; void reverses the entry. Four SL-typical schemes (AVURUDU, CHRISTMAS, 13TH_MONTH, PERFORMANCE) seeded at tenant signup
- **Employee expense claims** — five-state lifecycle (draft → submitted → approved | rejected → paid | void) for reimbursable out-of-pocket employee spend (travel, meals, fuel, communication, misc seeded as defaults; tenants can add more). Each claim links to a category that maps to a GL expense account (CoA 5210 / 5220 / 5230 / 5240 / 5290 seeded, `account_subtype='reimbursement'`). Two disbursement paths chosen at claim time: **direct** posts DR category expense / CR bank at approve-and-pay time; **payroll** leaves the row `approved` for the next payroll run to pick up. Payroll-compute integration (PR #69) loads approved payroll-method claims with `applied_in_run_id IS NULL` and `claim_date <= periodEnd`, emits per-employee `REIMBURSE_TAX` (counts for EPF/ETF/PAYE) and `REIMBURSE` (exempt, no statutory impact) earning lines bucketed by `is_taxable`, then atomically stamps `applied_in_run_id` + `applied_in_run_line_id` + `applied_at` on each consumed claim. Post time adds per-category DR lines (grouped by `expense_account_id`, falling back to the wages account when null) and subtracts the reimburse total from the main Salaries expense line so the journal balances. Payroll void (PR #69) releases claims by nulling the applied columns so they flow to the next run. SOD enforced: submitter ≠ approver, submitter ≠ rejecter. Category snapshot on submit so re-mapping the category later doesn't rewrite history. Rejected claims can be edited (resets to draft) and re-submitted. Receipt-file attachments and per-category approval caps explicitly deferred to v2. Separate `EXP-YYYY-NNNN` number series via `nextDocumentNumber`, allocated on first submit (PR #60 + PR #69 payroll wire-up)
- **Employee notification preferences** — per-user opt-out for notification kinds (invoice_posted, payment_received, je_approval_pending, low_stock, cheque_stale, etc.). `notification_preferences (tenant_id, user_id, kind, enabled, cadence)` with "absence of row = enabled + immediate" semantics so existing users keep receiving everything until they explicitly change. `/app/settings/notifications` lists all emitted kinds with a per-kind cadence dropdown (Off / Immediate / Daily digest / Weekly digest). `emitNotification()` checks prefs before inserting; broadcasts still bypass (e.g. "posting lock active" doesn't get muted by a stray preference row). Digest delivery (roadmap #45, PR #70): when cadence is daily/weekly, events divert to `notification_digest_queue` instead of the in-app bell; an hourly scheduled worker fires at the tenant-local digest hour (default 08:00 in the tenant's timezone, Monday-only for weekly), coalesces pending rows per user into one email, logs each attempt to `notification_digest_emails` for dedupe + audit. Dedupe window (20h daily / 6d weekly) guards against double-sends when two cron ticks straddle the same tenant-local hour. Failed sends stay queued for the next tick. Switching a kind from digest → immediate / off clears pending queue rows for that kind so the user sees them as fresh events going forward (PR #63 + PR #70)
- **Approval workflow designer (storage)** — tenant admin UI at `/app/settings/approvals` to author linear approval chains per document type (`journal_entry`, `expense_claim`, `bill`, `purchase_order`, `bonus_run`, `final_settlement`). Each policy carries a JSON `trigger_rule` (`{minAmountCents}` or `{submitters: [userIds]}`, composable via `{and: […]}`) and an ordered `steps[]` array where each step is `{approvers: [{kind: "role"|"user", id}], anyOf: bool}`. v1 is designer + storage only — actual routing into domain transitions is a follow-up (see backlog); existing per-domain approval columns (journal_entries.status = pending_approval, expense_claims approved_by_user_id, etc.) keep working unchanged, so nothing regresses (PR #63)
- **Custom roles + user role assignment** — `roles (tenant_id, name, permissions jsonb, is_system)` + `user_roles (user_id, role_id)` many-to-many. Permissions is a flat string→bool map (`{"invoices.create": true, "bills.void": false, ...}`). Five system templates seeded per tenant at bootstrap: Owner, Admin, Accountant, Sales, Read-only — cloneable + editable but `is_system=true` marks them. `/app/settings/roles` lists roles, lets admins create/clone/edit, and assign one or more roles per team member. `users.is_owner` stays the super-admin bypass — owners can't have permissions stripped beneath them (PR #63)
- **Role enforcement middleware** — `requirePermission(req, reply, "invoices.post")` in `apps/api/src/lib/permissions.ts` gates the high-leverage write routes. Three pass rules: (1) `users.is_owner=true` bypass, (2) tenant has zero `user_roles` rows → enforcement dormant (deploy-safe — existing tenants keep working until an admin assigns the first role), (3) caller holds a role whose permissions JSON grants the key (union across roles). Creates + reads stay at "authenticated in tenant" for v1 — real-world SoD is almost always about posting, not creating. `GET /auth/me` returns `permissions: {isOwner, enforcementActive, granted}` so the web can hide buttons it knows will 403; `hasPermission(perms, key)` client helper in `apps/web/lib/api.ts` mirrors the server decision (PR #64). **PR #69 expanded coverage**: seeded permission keys are now enforced across HR (payroll post/void/pay, bonus post/void, final-settlement post/void, salary-revisions, staff-loans, expense-claims approve/reject/pay, employees create/update/exit/confirm, leave approve/reject, salary-components), inventory (items create/update/archive, stock-counts approve/post, stock-transfers dispatch/receive), payments (customer + supplier payment record/void, WHT remit), and POS (pos.operate for shift open / sale ring; pos.close for shift close). The web side wires `hasPermission()` into action buttons via `apps/web/components/auth/permissions-provider.tsx` — a React context seeded by the app layout from `/auth/me`. `useCan("invoices.post")` returns a boolean; buttons (Post, Void, Pay) return `null` when the caller lacks the key. Dormant-safe default (empty context) treats everything as granted so components outside `/app` don't collapse
- **Final settlement worksheet** — full gross-to-net exit calculation for resigned/terminated/retired employees. Compute pro-rata salary for days worked in settlement month, unused paid-leave encashment (basic × days ÷ salaryDaysPerMonth), gratuity per SL Payment of Gratuity Act (14 days basic × completed years, min 5 years; excluded from EPF/ETF/PAYE per v1 simplification), notice pay-in-lieu (terminated without serving) or notice shortfall (resigned mid-notice), outstanding loan recovery in full (principal + accrued interest), and final EPF employee/employer + ETF + PAYE on eligible portion. Workflow draft → approved → posted (GL): post books the journal (DR Salaries & wages / DR Gratuity expense / DR EPF+ETF employer / CR EPF+ETF payable / CR PAYE / CR Loans receivable / CR Interest income / CR Gratuity payable / CR Salaries payable) and waives all pending loan schedule rows with `closed_reason='final_settlement'`. Settlement letter **PDF** with earnings/deductions breakdown, declaration clause, and dual signature blocks. Document number FS-YYYY-NNNN via the number-series pipeline. Gratuity COA (6003 Gratuity expense + 2250 Gratuity payable) seeded per tenant. Partial unique index `(tenant_id, employee_id) WHERE status <> 'cancelled'` enforces one active settlement per employee

### Reports
- Trial balance, P&L (with compare), balance sheet, general ledger, VAT return, cash flow
- AR aging detail (grouped by customer)
- AP aging detail (grouped by supplier)
- 3-way matching (PO ↔ GRN ↔ bill variance report)

---

## 🛠️ Active Backlog (ranked)

Each item has a spec reference, one-sentence description, and rough sizing (**S** = half-day, **M** = 1–2 days, **L** = 3+ days).

### Must-have (compliance + core workflows)

🎉 **All must-haves are now shipped.** The compliance foundation is complete — period lock, WHT, opening balance, credit enforcement, bad debt + VAT relief, and JE approvals. Previously tracked here, now in the shipped list above:

- **Period lock + year-end close** (PR #32)
- **WHT auto-derivation & remittance** (PR #33)
- **Opening balance upload** (PR #34)
- **Customer credit enforcement** (PR #35)
- **Bad debt write-off with VAT relief** (PR #36)
- **Journal entry approval workflow** (PR #37)

### Should-have (convenience/polish)

🎉 **All original should-haves shipped + #42 role enforcement closed in PR #64 + #44 FX revaluation closed in PR #65 + #45 digest windows closed in PR #70.** One deferred follow-up remains, now scoped into six smaller increments.

| # | Feature | Spec | What it does | Size |
|---|---|---|---|---|
| 43 | ~~Approval engine core + JE wiring~~ | tenant-admin §7 | **Shipped in PR #74.** `approval_requests` + `approval_request_steps` runtime tables; generic engine (`resolveApplicablePolicy`, `createApprovalRequest`, `recordDecision`) snapshots policy steps on submit so in-flight requests bind to the rules in effect at submit time. Journal entries wired: engine path takes precedence; flat `journalApprovalThresholdCents` retained as back-compat fallback. `/app/approvals` queue with `mine` / `submitted_by_me` / `all` scopes; `approval.decide` permission key seeded on Owner/Admin/Accountant; SOD enforced explicitly (submitter ≠ approver). 43a–43e extend the same wiring to the other five domains. | ~~M~~ |
| 43a | ~~Approval engine — expense claim routing~~ | tenant-admin §7 | **Shipped in PR #75.** `expense_claims.approval_request_id` FK added; `/expense-claims/:id/submit` consults `resolveApplicablePolicy("expense_claim", …)` and parks the claim with an `approval_request` when a policy matches. Engine-owned claims must be decided via `/approvals/:id/approve`; legacy `/approve`, `/approve-and-pay` (submitted path), and `/reject` routes refuse engine-owned claims with 409 `ENGINE_OWNED`. `/approve-and-pay` additionally accepts engine-approved direct claims (status='approved') so payment can happen after the engine approves. `finaliseApprovedDocument` flips status submitted → approved (both disbursement methods drain via the existing state machine). Cancel from the Approvals queue flips the claim to `rejected` so it can be edited + re-submitted. Voiding an engine-owned claim cancels the open approval_request too. `deepLinkFor` routes to `/app/expense-claims/:id`. | ~~S~~ |
| 43b | ~~Approval engine — bill routing~~ | tenant-admin §7 | **Shipped in PR #76.** Additive — bills had no approval flow before. New `pending_approval` state between `draft` and `posted`; `bills.approval_request_id` FK added. `POST /bills/:id/post` consults `resolveApplicablePolicy("bill", …)` and parks the bill in `pending_approval` with an open `approval_request` when a policy matches; tenants with no matching policy keep the immediate `draft → posted` path unchanged. Post core extracted into a shared `postBillCore(tx, {billId, allowStatuses, …})` helper so the immediate path and the engine finaliser run identical AP-posting + stock-receipts + bell notifications. `finaliseApprovedDocument` drives the engine path `pending_approval → posted`. Reject and cancel from the Approvals queue flip the bill back to `draft` + clear `approval_request_id` + append the reason to notes so the submitter can edit + re-post. Engine-owned bills refuse `/bills/:id/post` with 409 `ENGINE_OWNED`. `deepLinkFor` routes to `/app/bills/:id`; the bill detail page surfaces an "Awaiting approval" banner when pending. | ~~S~~ |
| 43c | ~~Approval engine — PO routing~~ | tenant-admin §7 | **Shipped in PR #77.** `/purchase-orders/:id/send` consults `resolveApplicablePolicy("purchase_order", …)` + parks matching POs in `pending_approval`. Extended `trigger_rule` with `firstPoFromSupplier: boolean`; submit route pre-computes the flag from existing non-cancelled POs. Engine-owned POs refuse `/send` + `/acknowledge` with 409 `ENGINE_OWNED`. Reject + cancel flip back to `draft` + clear engine handle + stamp reason into notes (mirrors bill routing). | ~~S~~ |
| 43d | ~~Approval engine — payroll + bonus run routing~~ | tenant-admin §7 | **Shipped in PR #78.** Both domain routes consult `resolveApplicablePolicy` at post time; matching policies park the run in `pending_approval`, approvals queue finalises via shared `postPayrollRunCore`/`postBonusRunCore`. Reject + cancel both flip back to draft + stamp reason into notes. Always-approve = empty triggerRule; bonus threshold = `minAmountCents`. | ~~S~~ |
| 43e | ~~Approval engine — final settlement routing~~ | tenant-admin §7 | **Shipped in PR #79.** Additive — final settlements had a simple draft→approved gate before. Migration `71-approval-engine-final-settlement.sql` adds `final_settlements.approval_request_id` FK + partial index and widens the status CHECK to accept `pending_approval`. `POST /final-settlements/:id/approve` consults `resolveApplicablePolicy("final_settlement", netPayableCents, submitter)`; matching policies park the settlement in `pending_approval` with an open `approval_request`, otherwise the legacy immediate `draft → approved` path runs unchanged. Approve core extracted into `approveFinalSettlementCore(tx, {settlementId, approverUserId, allowStatuses})`; immediate path passes `["draft"]`, engine finaliser passes `["pending_approval"]`, so both flows share one atomic code path with an explicit race guard. Engine-owned settlements refuse `/approve` with 409 `ENGINE_OWNED`. Reject + cancel from `/approvals/:id/*` flip the settlement back to `draft` + clear `approval_request_id` + stamp `[Rejected]` / `[Withdrawn]` reason into notes so the submitter can re-approve after edits. Domain `/cancel` also now accepts `pending_approval` and cancels the underlying approval_request as part of terminating the settlement. Tenant-admin §7.1 "always-approve for final settlements" is satisfied by configuring a policy with an empty `triggerRule` (matches every submission). | ~~S~~ |
| 45 | ~~Notification digest windows~~ | tenant-admin §10.2 | **Shipped in PR #70.** See "Employee notification preferences" in shipped list above. | ~~M~~ |
| 47 | ~~API rate limiting (per-IP)~~ | `_gaps.md` A6 | **Shipped in PR #89.** `@fastify/rate-limit` v9 registered globally before the identity plugin so pre-auth floods (credential stuffing / signup spam / OTP enumeration) are throttled without ever touching the session lookup. Redis-backed store reuses the app's existing `ioredis` client (prefix `pp-ratelimit:` keeps keys clear of sessions / OTPs / bullmq). Generous global default of 600 requests / minute / IP (env-tunable: `RATE_LIMIT_GLOBAL_MAX` + `RATE_LIMIT_GLOBAL_WINDOW`) — invisible to a legitimate tenant clicking around. Tight per-route overrides where brute-force is the real threat: `/auth/login` 10/min/IP, `/auth/signup` 5/10min/IP, `/portal/auth/request-otp` 5/10min/IP (second layer on top of the per-email budget in `tryConsumeOtpRateBudget` — one protects a customer from OTP spam, the other protects the platform from IP cycling through emails), `/portal/auth/verify` 10/min/IP (enough headroom for fat-fingered codes). `keyGenerator: req => req.ip` honours the existing `trustProxy: true`. `continueExceeding: true` so repeated requests after the limit extend cooldown rather than resetting on window boundaries. **`skipOnError: true` — fail open on redis outage.** In a redis blip we'd rather serve than blanket-503 every tenant; the observability stack (#46) will surface the outage in minutes, and the brute-force risk window is bounded by how long redis is down. `/health` + `/metrics` allow-listed so the Prometheus scraper + load-balancer probes never clip. Response shape `{ error: { code: "RATE_LIMITED", message, retryAfterSeconds } }` matches the app's envelope; web login + signup forms already rendered `err.message`, now ornamented with a friendly branch on `RATE_LIMITED`. `RATE_LIMIT_DISABLED=true` env short-circuits the whole plugin for local debugging sessions. Zero schema, zero migration, zero new infra. Second promotion from the gaps parking lot. | ~~S~~ |
| 49 | ~~Password policy (server + UI)~~ | `_gaps.md` A2 | **Shipped in this PR.** Fourth promotion from the gaps parking lot. Closes the "any 8-char string goes" hole on signup + gives users a way to rotate. Server-side: new `apps/api/src/modules/identity/password-policy.ts` exports `validatePasswordPolicy(password, {email, name})` (min 10 / max 128 chars, 3-of-4 character classes via Unicode-aware regex `[\p{L}]` + `[\p{N}]` + symbol class, an `OBVIOUSLY_WEAK` set of known-bad strings, and a not-the-user's-email-local-part + not-the-user's-name guard — identity-as-password is an underrated real-world attack) and `checkPasswordBreached(password)` (HIBP k-anonymity: SHA-1 the password, send the first 5 hex chars to `https://api.pwnedpasswords.com/range/<5>` with `Add-Padding: true`, match the suffix in the response; **1500ms timeout, fails open** — if HIBP is down or the network is partitioned we log the skip reason and let the signup go through, bounded by the other policy checks; `PASSWORD_BREACH_CHECK_DISABLED=true` env short-circuits the call entirely for air-gapped deploys). Both helpers are pure and have zero dependency on Fastify — they compose from any route. Wired into `POST /auth/signup`: Zod now accepts `min(1).max(128)` so the policy error comes with the specific reasons (not a generic "too short"), then `validatePasswordPolicy` runs, then `checkPasswordBreached` runs, and only then does `hashPassword` happen — order matters because argon2 is expensive and we don't want attackers to use signup as a free argon2 oracle. Policy failure returns 400 `WEAK_PASSWORD` with a `reasons: string[]` array the web side can bullet-render. New `POST /auth/change-password` (session-gated, `CHANGE_PASSWORD_RATE_LIMIT = 5 per 10 minutes` per IP on top of the global limiter from PR #89): verifies the current password via `verifyPassword`, validates the new one through the same two-stage gate, rejects a same-as-current no-op with `SAME_PASSWORD`, updates the hash under `withTenant(userTenantId, tx)` (keeps RLS context even though we're mutating `users`), writes `audit.user.password_changed`, then **destroys every session for this user including the current one** via `destroyAllSessionsForUser` and immediately mints a fresh session for this tab via `setSessionCookie` — so a parallel session an attacker might be holding is booted, but the legitimate user stays signed in on the tab they just saved from. New audit kind `user.password_changed` added to the `AuditEventKind` union. `ApiError` on the web extended with a `reasons?: string[]` field so `WEAK_PASSWORD` responses surface the specific failed checks as a bulleted list under the error banner (same pattern now used in both signup and change-password). New `PasswordStrengthHint` client component (`apps/web/components/auth/password-strength-hint.tsx`) mirrors the three top-level server checks (length, class-of-3, not-identity) as a live checklist — deliberately does **not** block submission because the server is the source of truth and users might want to see the HIBP result; the hint also advertises "we'll also check against public breach corpora on submit" once the three local checks pass. Signup form now carries controlled `email` / `ownerName` / `password` state so the hint can personalise the identity guard. New `/app/settings/password` page with the three-field (current / new / confirm) change-password form — client-side confirm-match check, inline errors for `WRONG_CURRENT_PASSWORD` / `RATE_LIMITED` / `UNAUTHENTICATED` / `WEAK_PASSWORD` (reasons bulleted) / `SAME_PASSWORD`, success banner "Password changed. Other sessions for your account were signed out." and a `router.refresh()` on success so any stale server-rendered state re-loads under the new cookie. Settings index gets a new card linking to it with a KeyRound icon. Deferred: password-reuse prevention (needs `password_history` table + retention policy — out of scope for an S promotion), forced rotation on nth login (the NIST guidance against mandatory rotation holds unless we have specific cause), admin-driven password reset for team members (next item when we build the team-admin console). Typecheck baselines held (api: 27, web: 10, db: 1). | ~~S~~ |
| 51 | ~~Multi-factor auth (TOTP)~~ | `_gaps.md` A1 | **Shipped in this PR.** Sixth promotion from the gaps parking lot. Closes the single biggest compliance/enterprise-ask gap in auth: "is there 2FA?" is the first question from any prospect with a security checklist. TOTP-only for v1 (Google Authenticator / 1Password / Authy / Bitwarden all work); opt-in per user; admin users only (customer-portal MFA is a different threat model — deferred); SMS / email OTP / WhatsApp / tenant-wide force-MFA toggles all explicitly out of scope for this PR. **Schema.** Migration `80-user-mfa.sql` creates `user_mfa` keyed by `user_id` (PK) with `tenant_id` FK, `totp_secret_encrypted text NOT NULL` (aes-256-gcm ciphertext blob, app-tier encrypted — DB never sees plaintext), `backup_codes_hash text[]` (argon2 hashes, consumption *removes* the hash from the array — used-state is implicit), `enabled boolean`, `enrolled_at` / `last_used_at` timestamps. RLS on `tenant_id = current_tenant_id()`. Three SECURITY DEFINER helpers handle the pre-session paths (same pattern as `44-auth-helpers.sql`): `auth_user_has_mfa(user_id)` (login-step-1 branch), `auth_get_mfa_for_user(user_id)` (login-step-2 verify), `auth_record_mfa_success(user_id, new_backup_codes)` (stamps `last_used_at` + optionally replaces the shortened backup-codes array post-consumption). **Crypto.** `apps/api/src/modules/identity/mfa.ts` wraps `otplib` (6 digits, 30s period, SHA1, ±1 window for clock skew — the Google Authenticator defaults) and `qrcode` for the enrolment image. Secrets are aes-256-gcm with a key loaded from env `MFA_ENCRYPTION_KEY` (32 bytes base64); dev-fallback derives via HKDF-SHA256 from `SESSION_SECRET` with a fixed salt + info label so local environments work out of the box. Ciphertext format is `base64url(iv(12) || authTag(16) || ciphertext)` — single text column, easy to rotate. Server logs a loud warning on boot if `MFA_ENCRYPTION_KEY` is unset in production (error-level) or dev (warn-level). Backup codes are 10 single-use 10-char alphanumeric strings (unambiguous alphabet — no 0/O, no 1/I; zero modulo bias at 256/32=8 — every letter has exactly 8 byte values mapping to it); argon2-hashed on the way in; shown to the user exactly once at enrolment. Verify tries TOTP first (constant-time compare inside otplib, no hash cost), falls through to a linear backup-code scan only on TOTP miss so the hot path stays fast. **Two-step login.** `/auth/login` now branches on `auth_user_has_mfa`: no MFA → session minted inline as before; MFA enrolled → stash a pre-session challenge in Redis (`mfa-challenge:*`, 5-min TTL, snapshots userId / tenantId / email / ip / ua) and return `{ mfaRequired: true, challengeId }` instead of a session. New `POST /auth/login/mfa` (pre-session, rate-limited 5/min — plenty for a human fat-fingering, exhausts the 10^6 code space far slower than the 5-min challenge TTL) takes `{ challengeId, code }`, fetches the MFA row via `auth_get_mfa_for_user`, decrypts + verifies, consumes the challenge, stamps `last_used_at` (and writes the shortened backup-codes array if a backup code was consumed), then mints the real session + csrf cookie pair. Audit events: `user.login` (marks "(MFA)" or "(MFA backup code)" in the summary depending on which path verified), `user.mfa_challenge_failed` on bad codes (so tenant admins see brute-force attempts in `/app/audit-log`). **Enrol / disable.** `POST /auth/mfa/enroll` (session-gated, blocks on `MFA_ALREADY_ENABLED` so re-enrolment is an explicit disable-first two-step) mints a secret + otpauth URI + QR data URL, stashes the secret in Redis under a short-lived tempToken (10-min TTL, `mfa-enrol:*`) — deliberately *not* a half-finished DB row, so a user who bails mid-flow leaves no state behind and no sweeper is needed. `POST /auth/mfa/enroll/verify` takes `{ tempToken, code }`, cross-checks tempToken.userId against the session (defends against a misbehaving client reaching into the enrolment state from another tab with a captured token), verifies the code, generates + hashes + returns the 10 backup codes, upserts the row as enabled, writes `user.mfa_enrolled` audit event. `POST /auth/mfa/disable` (session-gated, rate-limited 5/10min) **requires a valid TOTP or backup code on top of the session** — the key invariant, so a stolen laptop session cookie alone cannot silently disarm the second factor; on success deletes the row and writes `user.mfa_disabled`. Idempotent: disable on an already-off account returns 200. `GET /auth/mfa/status` + `mfa.enabled` folded into `/auth/me` for the settings page. **CSRF.** `/auth/login/mfa` falls under the existing `/auth/login` exemption prefix in `csrf.ts` (both are pre-session; comment updated to call out the coverage explicitly). Enrol / verify / disable / status all run session-gated and therefore go through the double-submit check. **Web.** `apps/web/components/auth/login-form.tsx` becomes a two-state form: password step → if response carries `mfaRequired`, switches in-place to a code-input prompt without page nav so the UX is one continuous flow, "Back to sign in" reset, expired challenges auto-bounce back to the password step with a clear banner. New `/app/settings/security` surface (added to the Settings index with a ShieldCheck icon, under Change password) renders a state-machine client component — `idle` / `enrolling` / `codes` / `disabling` — with the QR + manual-entry secret card for enrol, a copy-all button for backup codes (shown exactly once before returning to idle), a "backup codes left: N" card on the enabled view that warns in danger colour when ≤3 remain, and the disable flow asking for a current TOTP or backup code. `api.ts` login return type widened to a discriminated union `{ user } | { mfaRequired: true; challengeId }`; new `api.loginMfa`, `api.mfaStatus`, `api.mfaEnrollStart`, `api.mfaEnrollVerify`, `api.mfaDisable`. Libraries: `otplib@12` + `qrcode@1.5` + `@types/qrcode` — all MIT, self-hosted, battle-tested; no SaaS dependency. Typecheck baselines held (api: 27, web: 10, db: 1). Deferred to later PRs: SMS / WhatsApp / email OTP second factor, tenant-wide "require MFA for all users" toggle, admin-driven "reset MFA for this user" (needs the team-admin console), WebAuthn / security-key support, portal (customer) MFA, recovery-via-email channel. | ~~M~~ |
| 50 | ~~CSRF double-submit (belt-and-braces)~~ | `_gaps.md` A5 | **Shipped in this PR.** Fifth promotion from the gaps parking lot. Second, independent layer on top of the existing SameSite=Lax + same-origin CORS defence — so a hypothetical SameSite regression, a buggy browser cookie default, or a future relaxation of the CORS allow-list never leaves mutating endpoints unprotected. Server: 32-byte base64url `csrfToken` added to both `Session` and `PortalSession` Redis blobs — minted on `createSession` / `createPortalSession`, destroyed with the session blob (no separate key to GC, no orphan window), back-filled by `readSession` / `readPortalSession` for pre-#50 blobs so existing logged-in tabs don't get booted on deploy. Non-HttpOnly companion cookies `pp_csrf` (Path=/, unsigned) and `pp_portal_csrf` (Path=/portal, unsigned) are set alongside their session cookie pairs on signup / login / change-password / portal verify and cleared on both logouts + the /me customer-archived path. Why non-HttpOnly: the web client needs to read the cookie in JS to mirror it into the `X-CSRF-Token` header — that's the whole point of double-submit. XSS stays a separate concern (the session cookie itself is still HttpOnly). Why unsigned: the token is itself 256 bits of random; signing adds zero defence against the cross-origin threat model (attacker on evil.example can't read the cookie regardless, because SameSite=Lax + same-origin document.cookie). New `apps/api/src/plugins/csrf.ts` (Fastify plugin, declares `dependencies: ["identity"]` so `@fastify/cookie` is live when it registers) adds an `onRequest` hook that: skips GET/HEAD/OPTIONS (never mutate); skips `/health`, `/metrics`, `/auth/login`, `/auth/signup`, `/portal/auth/request-otp`, `/portal/auth/verify` (no session exists yet — login-CSRF is bounded by SameSite + rate-limit from #47 and is a dead-end anyway because the attacker would be logging the victim into the attacker's account); picks portal vs admin cookie pair by URL prefix; skips when there's no session cookie at all (handler will 401 — keep error shape consistent); reads both the CSRF cookie and the `X-CSRF-Token` header; constant-time compares `cookie == header` AND `cookie == session.csrfToken` via `timingSafeEqual` with a length guard; 403 `CSRF_INVALID` on any mismatch. `CSRF_DISABLED=true` env short-circuits the plugin for local debugging. Registered between `identityPlugin` and the business routes in `server.ts` so every route mounted after it gets covered — portal included, because the hook reads the session cookie directly rather than leaning on the portalPlugin's `req.portalSession` decoration. Web: `apps/web/lib/api.ts` gets a `readCookie(name)` helper (document.cookie split/trim/decode) and updates `request()` to read `pp_portal_csrf` (when `path.startsWith("/portal")`) or `pp_csrf` on every non-GET/HEAD/OPTIONS call and attach it as `X-CSRF-Token`. One centralised funnel = one place to maintain — all 160+ call sites through `api.*` in the web package get the header for free. RSC server-side fetches grepped clean of `method: "POST"` / `PUT` / `DELETE` / `PATCH` — the Next.js side only reads, mutations all go through the client funnel. Zero schema, zero migration, zero new infra. Typecheck baselines held (api: 27, web: 10, db: 1). | ~~S~~ |
| 48 | ~~P&L drill-down to source documents~~ | `_gaps.md` B4 | **Shipped in this PR.** Third promotion from the gaps parking lot. Pure frontend wiring — every bit of data this needs already travels in the existing API responses, so zero schema, zero API, zero migration. Two edges close the loop: (1) **P&L → GL.** Every account row on `/app/reports/profit-loss` is now a `<Link>` pointing at `/app/reports/general-ledger?accountId={a.accountId}&from={data.asOfFrom}&to={data.asOfTo}`, carrying the same period the P&L is showing. Whole row is hoverable (`group hover:bg-mint-surface/30`) with the name cell underlining on hover so the affordance is obvious without decorating every line. Code + name + amount cells each wrap the link so click-anywhere-on-the-row works. (2) **GL → source document.** `apps/web/app/app/reports/general-ledger/page.tsx` grows a `deepLinkForSource(sourceType, sourceId)` helper covering all ~35 live sourceType values emitted across the API. Mapped: `invoice` / `invoice_void` / `bad_debt_writeoff` / `bad_debt_writeoff_reversal` → `/app/invoices/:id`; `bill` / `bill_void` → `/app/bills/:id`; `credit_note` → `/app/credit-notes/:id`; `debit_note` → `/app/debit-notes/:id`; `delivery_note` → `/app/delivery-notes/:id`; `manual` → `/app/journals/:id`; `payroll_run` / `payroll_disbursement` / `payroll_payment` / `payroll_void` → `/app/payroll/:id`; `final_settlement` → `/app/final-settlements/:id`; `bonus_run` / `bonus_run_void` → `/app/bonus-runs/:id`; `expense_claim` → `/app/expense-claims/:id`; `employee_loan` → `/app/staff-loans/:id`; `stock_count` → `/app/stock/counts/:id`; `pos_shift` → `/app/pos/shifts/:id`; `petty_cash_float` → `/app/petty-cash/:id`; `cheque` / `cheque_bounce` / `cheque_clear` → `/app/cheques/:id`. Returns `null` for source types that have no detail page today — **`customer_payment` / `supplier_payment`** (both surfaces are list-only right now, no `[id]` route; better to label-only than deep-link to a list and make the user hunt), **`petty_cash_transaction`** (source id is the transaction row but our detail page is the float — would need a new route or a txn→float resolver to make this work), and **`depreciation_run` / `year_close` / `opening_balance` / `fx_revaluation`** which are posting events with no per-event page. Also widened `sourceLabel()` in the same file from 13 entries to 38 so the labels rendered next to entry numbers cover every live sourceType, not just the common ones. When a deep-link exists the label becomes a styled `<Link>` with an `ExternalLink` icon; when it doesn't we fall through to the existing plain `<p>` — the UI degrades gracefully and this guarantees we never render a broken link. The helper deliberately lives inline in the GL page rather than in `@/lib` — it's the only call site today, and the existing precedent is `REF_ROUTES` colocated with `audit-log-client.tsx`. Typecheck baselines held (api 27 / web 10 / db 1). Landed without touching any API or schema file — pure JSX + two pure helpers. | ~~S~~ |
| 46 | ~~Observability stack (self-hosted)~~ | `_gaps.md` K1 + K2 | **Shipped in PR #88.** First item promoted from the gaps parking lot after the M/L nice-to-have pile emptied. API `/metrics` Prometheus endpoint (`http_requests_total` / `http_request_duration_seconds` / `http_requests_in_flight` + `prom-client` default process metrics — heap / GC / event-loop lag / CPU), labelled on Fastify's matched route template so cardinality stays bounded. Worker `/metrics` on a separate port (`WORKER_METRICS_PORT`, default 4100) with `worker_scheduled_jobs_total` + `worker_scheduled_job_duration_seconds` wrapping the existing scheduled-job dispatch. `@sentry/node` SDK on the API + worker and `@sentry/nextjs` on the web — no-op when `SENTRY_DSN`/`NEXT_PUBLIC_SENTRY_DSN` unset, SDK auto-tags requests with `tenant_id`/`user_id`, `beforeSend` drops 4xx responses (they're workflow, not bugs), worker captures scheduled-job exceptions via `captureException(err, {jobName})`. Separate `docker-compose.observability.yml` bootstraps the full stack: Prometheus (30-day TSDB retention, auto-scrapes api + worker on the docker network), Grafana 11 (auto-provisioned Prometheus + Loki datasources, single "API overview" dashboard with rate / error rate / in-flight / p95 + per-route rate + percentile latencies + error breakdown + heap + scheduled-jobs + event-loop-lag panels), Loki 3 single-binary (7-day log retention, filesystem storage), Promtail (tails docker container logs, labels by `container` / `compose_service`), and **GlitchTip** v4 (Sentry-protocol-compatible self-hosted error tracker — drop-in for the Sentry SDK with its own isolated postgres + celery worker, much lighter than full Sentry's 20+ containers). All observability ports bind to `127.0.0.1` only — operator tooling, not tenant-facing. Run-book at `docs/observability.md`. Picked first from `_gaps.md` because it's OSS-native (aligns with the stack preference), additive (no tenant-path changes), and it's the protection every subsequent fragile area flagged in `_status.md` §3 needs. | ~~M+S~~ |

### Nice-to-have (advanced/niche)

| # | Feature | Spec | What it does | Size |
|---|---|---|---|---|
| 28 | ~~POS screen~~ | sell §5 | **Shipped in PR #67** (permission keys added in PR #69). See Sell section above. | ~~L~~ |
| 29 | ~~Commission engine~~ | sell §10 | **Shipped in PR #68** (hardened in PR #69 with cumulative-cap clawback safety + paid-earning memo + net-negative flag). See Accounting section above. | ~~L~~ |
| 30 | ~~Purchase requisition~~ | buy §3 | **Shipped in PR #80.** Off-by-default tenant toggle under Settings → Modules (`tenant_settings.purchaseRequisitionsEnabled`). Two new tables: `purchase_requisitions` (header, status: `draft`/`pending_approval`/`approved`/`rejected`/`converted`/`cancelled`) and `purchase_requisition_lines` (per-line `line_status`: `pending`/`approved`/`rejected`). RLS on both. Shared `approvePurchaseRequisitionCore(tx, {…, allowStatuses})` helper serves the immediate path (`["draft"]`) and the engine finaliser (`["pending_approval"]`) — same atomic code with a race guard. `/purchase-requisitions/:id/approve` consults `resolveApplicablePolicy("purchase_requisition", estimatedTotalCents, submitter)`; matching policies park the PR in `pending_approval` with an open `approval_request`, otherwise the direct path runs. Approvals queue reject/cancel flip back to `draft`/`rejected` + clear engine handle + stamp reason into notes so the submitter can edit + re-submit. Partial approval supported via per-line `rejectedLineIds` + `lineRejectReasons`: header flips to `approved` if any line remains approved, `rejected` otherwise. SOD: submitter ≠ approver enforced at both the immediate and engine-finalised paths. Convert creates a draft PO with `source_pr_id` linkage (lines carry `source_pr_line_id`); the PR flips to `converted` and allocates its PR number at submit time. Permission key: `purchase_requisitions.manage` (coarse-grained, matches `bills.post`/`payroll.manage` precedent) seeded onto Owner/Admin/Accountant system roles via `seed_admin_role_templates_for_tenant` CREATE OR REPLACE. Sidebar entry gated by the tenant toggle — off-by-default tenants don't see the menu item at all. | ~~M~~ |
| 31 | ~~Customer portal~~ | sell §14 | **Shipped in PR #71.** Email+OTP login, scoped portal realm, view invoices (with PDF download), statement, payments, recurring templates. Pay-online deferred to v2. | ~~L~~ |
| 32 | ~~Document attachments~~ | sell §20, buy §21 | **Shipped in PR #81.** Generic `document_attachments` table keyed by `(tenant_id, entity_type, entity_id)` with a CHECK against a curated whitelist of entity types (invoice / sales_order / quotation / credit_note / bill / purchase_order / purchase_requisition / goods_received_note / expense_claim / payment / receipt / final_settlement / journal_entry). S3-compatible object storage via `@aws-sdk/client-s3` pointed at the self-hosted MinIO already in `docker-compose.yml` (bucket `pettahpro-attachments`, auto-created at boot — failure logs a warning rather than crashing the API, so dev workstations without MinIO running still boot). Storage key format `<tenant>/<entity_type>/<entity_id>/<uuid>-<sanitized_filename>` so tenant isolation survives even a bucket-policy misconfig. Per-file 10 MB cap enforced three ways (fastify-multipart limits + DB CHECK constraint + module-level size check). Default 7-year `retention_until` column — soft-delete preserves the row for audit until retention expires. Endpoints: `POST /attachments` (multipart), `GET /attachments?entityType=&entityId=`, `GET /attachments/:id` (attachment download), `GET /attachments/:id/preview` (inline Content-Disposition for PDFs + images), `DELETE /attachments/:id` (soft). Reusable `<AttachmentsPanel entityType entityId />` client component with drag-drop upload, per-row download + preview + soft-delete, wired into invoice / bill / purchase-order / purchase-requisition detail pages as reference integrations. Audit events `attachment.uploaded` and `attachment.deleted`. | ~~M~~ |
| 33 | ~~Document templates builder~~ | sell §19, buy §20, tenant-admin §4 | **Shipped in this PR (v1 scope).** Per-tenant layout records drive the PDF renderer. Migration `79-document-templates.sql` creates `document_templates` (tenant-scoped; `(doc_type, language, version, status, name, description, layout_json, is_default, library_key, created_by_user_id)` with CHECK on `doc_type` covering the 12 rendered doc types we have today and CHECK on `status` covering `draft`/`published`/`archived`). Partial unique index `(tenant_id, doc_type, language) WHERE is_default AND deleted_at IS NULL` enforces at most one default per slot; RLS on `tenant_id = current_tenant_id()`. Template library ships as a hard-coded TS module (`apps/api/src/modules/operations/template-library.ts`) rather than a platform-level DB table — keeps RLS clean, no nullable-tenant trickery, and extending the library is a code-only change. v1 library ships one invoice starter ("Classic"). API routes under `/document-templates`: list / get / create-blank / `POST /clone-library` / `POST /:id/clone` (bumps version) / `POST /:id/set-default` (atomic swap via single UPDATE with CASE to keep the partial unique index consistent mid-statement) / `POST /:id/publish` / PATCH / DELETE (blocks default deletion with `CANNOT_DELETE_DEFAULT_TEMPLATE`) / `GET /active?docType=...&language=...` (used by render routes; falls back EN → null). Permission-gated on `settings.manage`. Web renderer (`apps/web/lib/template-renderer.tsx`) parses `layout_json` of shape `{pageSize, theme, sections[]}` and dispatches each section to a react-pdf block emitter — v1 sections are `header` / `metaRow` / `billTo` / `lineItemsTable` / `totals` / `notes` / `footer` / `spacer` / `text`, matching the hard-coded InvoicePDF component so the Classic library layout reproduces the pre-#33 output exactly. Page sizes A4 / A5 / 80mm-thermal / 58mm-thermal (receipts use a tall page spec so react-pdf auto-paginates overflow). Invoice render route `/app/invoices/[id]/pdf` now races `GET /document-templates/active` alongside `GET /invoices/:id` and `GET /auth/me`; a published+default template wins, any error falls through silently to the hard-coded `InvoicePDF` — tenants who never touch the builder print identically to before. Other 11 doc types still use their hard-coded PDF components; wiring each to the engine is one follow-up PR per doc (migration mechanical). Tenant admin UI at `/app/settings/document-templates` lists tenant templates (doc-type + language + status pills, default-star) with inline Publish / Set default / Clone / Delete, plus a Library section underneath for one-click "Clone to tenant". Editor at `/[id]` is a structured section-list editor (add / reorder / remove sections, per-section toggles, theme colour pickers, page-size picker) — **not** the full drag-drop WYSIWYG called out in sell §19.1; that's a Phase 2 on top of the same JSON schema once the engine proves itself. `/new` is a blank-template create form. Settings page links the new surface under "Documents". Deferred: logo upload (needs MinIO wiring on the `tenant_settings` side), per-customer template override, multi-language auto-translate, full drag-drop WYSIWYG, other doc types migrated, Phase-2 library (PO / GRN / payment advice / thermal POS). Flagged in `_status.md`. | ~~L~~ |
| 34 | ~~Item batch / serial / expiry tracking~~ | inventory §2.7 | **Shipped in this PR.** Per-item opt-in toggles (`items.track_batches` / `track_serials` / `track_expiry` + `warranty_months`) gate enforcement — untracked items keep the pre-#34 WAVG-only path. Migration `78-batch-serial-expiry.sql` creates three new tables: `item_batches` (one row per inbound lot: `batch_number` / `mfg_date` / `expiry_date` / `original_qty` / `remaining_qty` / `unit_cost_cents` / `supplier_id` + source document pointers; FIFO index `item_batches_fifo_idx` on `(tenant_id, item_id, warehouse_id, expiry_date NULLS LAST, received_at) WHERE deleted_at IS NULL AND remaining_qty > 0`), `item_serials` (one row per physical unit: state machine `in_stock` → `sold` → `returned` / `scrapped`, `batch_id` quick-pointer, acquired + sold document pointers, `warranty_expires_at`; partial unique index on `(tenant_id, item_id, serial_number)` WHERE `deleted_at IS NULL` to allow serial reuse after soft-delete), `stock_movement_batch_allocations` (join table `(tenant_id, stock_ledger_id, batch_id, quantity, unit_cost_cents)` driving recall reports). Adds `batch_id` / `serial_id` quick-pointer columns on `stock_ledger` and `tracking_input` JSONB column on `bill_lines` + `invoice_lines` as the draft-to-post carrier. RLS on all new tables via `current_tenant_id()`. `applyStockReceipt` in `stock-posting.ts` now resolves item tracking flags, validates `BATCH_INPUT_REQUIRED` / `EXPIRY_INPUT_REQUIRED` / `SERIAL_COUNT_MISMATCH` / `SERIAL_BLANK` / `SERIAL_DUPLICATE_IN_PAYLOAD` before inserting a single `item_batches` row per inbound line, fans out N `item_serials` rows for serial-tracked lines, catches the unique-index violation as `SERIAL_ALREADY_EXISTS` (prevents reusing a live serial). `applyStockIssue` validates / resolves explicit `batchPicks` or runs FIFO auto-pick (SELECT ... FOR UPDATE on candidate batches, ordered by the FIFO index so concurrent issues serialise deterministically), decrements `remaining_qty` per batch, writes `stock_movement_batch_allocations` rows, flips serials to `sold` with customer + doc provenance + `warranty_expires_at = sold_at + warranty_months × 30 × 24 × 60 × 60 × 1000`. COGS for batch-tracked items is now `Σ (qty × batchUnitCost)` (batch-weighted) — untracked items keep the `qty × runningAvg` path unchanged. Tagged-union `InvoicePostError` + `PostBillCoreError` extended with the new tracking error codes; route layer maps each to a 400 / 409 with a friendly message. New read API under `/items`: `GET /items/:id/batches` (all lots for an item with `?active=true` filter), `GET /items/:id/serials` (every serial acquired for the item), `GET /items/batches/:batchId/recall` (outbound allocations joined to stock-ledger — who got which lot), `GET /items/serials/:serialId` (full trace: acquired from, sold to, warranty through), `GET /items/tracking/expiring?days=N` (batches with remaining stock expiring within N days, clamped 1–365). Items module (`operations/items.ts`) accepts the four new fields on `POST` + `PATCH`; services and bundles silently coerce all tracking toggles to false since neither carries stock. Web: item create form has a new "Batch & Serial Tracking" section with three toggles + a warranty-months input revealed when serial tracking is on; bill create form renders a conditional sub-row under each line with batch / mfg / expiry / notes fields and a serial-numbers textarea when the picked item has the matching toggle on; invoice create form renders a serial-numbers textarea when the picked item is serial-tracked (batch items use FIFO auto-pick — no UI picker in v1); new pages `/app/reports/expiring-batches` (window tabs 7 / 30 / 90 / 180, ascending by expiry, client-side days-until calc), `/app/items/batches/[id]` (batch detail + allocation table linking to source documents), `/app/items/serials/[id]` (serial trace with acquired / sold two-column layout + warranty-expires). Draft save never blocks on tracking input — validation is server-side at post; UI shows soft warnings below textareas when serial count doesn't match line quantity. DN / stock count / credit note reversal / stock transfer integration deferred to a follow-up — v1 wires at bill post (inbound) + invoice post (outbound) only. | ~~L~~ |
| 35 | ~~Kit / bundle items~~ | inventory §9 | **Shipped in PR #82.** Virtual bundle SKUs with static component lists. `items.item_type='bundle'` forces `track_inventory=false` — the bundle never carries stock itself; components do. New `item_bundle_components` table keyed by `(tenant_id, bundle_item_id, component_item_id)` with a unique-pair index, `CHECK (component_item_id <> bundle_item_id)` self-ref guard, `ON DELETE RESTRICT` on the component FK so a referenced component can't be hard-deleted from under a bundle, and RLS on tenant_id. No nested bundles (app-layer check in `PUT /items/:id/components`). Invoice post (`invoice-posting.ts`) explodes every bundle line into per-component `peekStockIssue` / `applyStockIssue` calls at `(lineQty × componentQty)`; each component goes through WAVG as its own stock-ledger row but accumulates into one `totalCogsCents` so the JE still has one inventory-CR + one COGS-DR pair per invoice. Invoice line stays as one row (spec §9 — no sub-line disaggregation). Same bundle explosion applied to the DN `deliver` path when `stockRelieveOn='delivery_note'`. POS inherits automatically via `postDraftInvoice`. `NEGATIVE_STOCK` from a bundle component surfaces the component's name (not the bundle's) so the user knows which SKU to replenish. Empty component list legal and intentionally permissive (service-like placeholder) — UI warns but doesn't block. API: `GET /items/:id` (inlines components), `PATCH /items/:id`, `GET /items/:id/components`, `PUT /items/:id/components` (replace-all, consolidates duplicates). New audit kind `item_bundle.updated`. Web: bundle badge on item list, bundle-typed items show a Components section in the create drawer + a dedicated detail page at `/app/items/:id` where the component list is editable post-creation. Credit-note stock reversal deferred (pre-existing: credit notes don't reverse stock at all yet — a follow-up PR will mirror the bundle explosion in reverse). | ~~M~~ |
| 36 | ~~Inventory category hierarchy~~ | inventory §2.4 | **Shipped in PR #73.** Unlimited-depth self-referential tree (`item_categories.parent_id`) with DB trigger for cycle prevention + same-tenant parent enforcement, partial-unique sibling names, and `item_category_effective_defaults(uuid)` recursive CTE that walks ancestors picking the first non-null default (child overrides parent). Defaults cover valuation method, tax code, income/COGS/asset accounts, SKU prefix, and reorder point — null = inherit. Soft-delete blocks on active children (HAS_CHILDREN) and referenced items (HAS_ITEMS) so nothing gets accidentally orphaned; items FK is ON DELETE SET NULL as a safety net. Tree view with expand/collapse + inline add-sub/edit/delete, category picker in the item form. | ~~S~~ |
| 37 | ~~Stale cheque auto-flag~~ | business-tenant-layer2 §6.2 | **Shipped in PR #66.** Daily cron flips active cheques past their 6-month `stale_at` date to `status='stale'` with per-cheque notifications. Issued-direction cheques get a Reissue action that mints a new cheque linked via `replaced_by_cheque_id` (preserves the original JE; AP stays the same). Manual `POST /cheques/flag-stale` for ad-hoc triggers. | ~~S~~ |
| 38 | ~~Petty cash float~~ | business-tenant-layer2 §7 | **Shipped in this PR.** Per-branch non-mandatory floats (`petty_cash_floats` with partial unique `(branch_id)` WHERE `status='active'`), denormalised `current_balance_cents` updated inside the same tx as each ledger post via `SELECT ... FOR UPDATE`. Shared `1005 Petty Cash` COA account across all tenants' floats (single account, per-float balance tracking). Ledger (`petty_cash_transactions`) covers `expense` / `advance_out` / `advance_return` / `top_up` / `variance_short` / `variance_over` / `close_transfer`. JE-per-transaction via `postJournal` choke point; voids go through `postReversingJournal` and roll back the float balance (+ flip top-up-request `posted` → `approved`). Top-up workflow (`petty_cash_top_up_requests`: `pending` → `approved` / `rejected` → `posted` / `cancelled`) with SOD check (requester ≠ approver) and ceiling enforced only at post (so approvals can park the request). EOD reconciliation (`petty_cash_reconciliations`) snapshots expected-vs-counted with variance to 5190 Cash Over/Short and updates the float balance to counted. Attachments via reuse of `document_attachments` with new entity type `petty_cash_transaction`. Permission gate: `petty_cash.operate` (holders + accountants) and `petty_cash.approve` (approvers). Seeded onto Owner/Admin/Accountant role templates. Web: list + open-float form with optional seed top-up + detail page with Ledger / Top-ups / Reconciliations tabs, inline modals for expense / advance / return / top-up / reconcile / close, `<AttachmentsPanel>` per ledger row. | ~~M~~ |
| 39 | ~~Attendance capture~~ | business-tenant-layer2 §5 | **Shipped in this PR.** Per-employee daily attendance backed by four tables — `attendance_devices` (registry of biometric machines / QR kiosks / manual sources with learned `column_template` JSONB for re-imports), `biometric_employee_map` (device-ID ↔ our-employee-ID), `attendance_records` (one row per `(employee, attendance_date)` enforced by partial unique — the dedup choke point), `attendance_imports` (file-import audit log with errors JSONB). Five capture methods in v1: `qr` / `biometric` (CSV file import) / `geofence` (+ photo via `document_attachments`) / `manual_muster` (supervisor EOD bulk) / `self` (employee click check-in/out). Multi-method collisions on the same day flip `has_conflict=true` with a reason rather than rejecting — supervisor resolves from the exceptions queue. Biometric import is stateless: each file creates an `attendance_imports` row and bulk-upserts records keyed on `(employee, date)` with earliest-in / latest-out merge; re-running the same file is idempotent. Column template learned from the wizard saves on the device row so repeat uploads auto-map. Geofence photos attach via reuse of `document_attachments` with new entity type `attendance_record`. Permission keys `attendance.operate` (mark / import / edit) + `attendance.view` (read + exceptions) seeded onto Owner/Admin/Accountant role templates. Web: list with date-range filter (default last 7 days) + employee/branch/status/method filters + exceptions alert banner, detail page with `<AttachmentsPanel>`, device registry with inline mapping editor modal, CSV import wizard (3 steps: pick device + upload, map columns, preview-and-commit). Shift rules / late-policy / OT / payroll wire-up deferred — this module captures events; payroll reads `total_minutes` when that integration lands. | ~~M~~ |
| 40 | ~~Dual depreciation (book vs tax)~~ | accounting §8 | **Shipped in this PR.** Parallel tax-depreciation schedule alongside the existing book schedule. Book posts to GL (unchanged). Tax is memo-only — used by the CA for tax computation, never touches the ledger. Migration `77-dual-depreciation.sql` adds `tax_depreciation_method` / `tax_useful_life_months` / `tax_salvage_cents` / `tax_annual_rate_bps` (for WDV) / `tax_depreciation_start_date` / `tax_accumulated_depreciation_cents` / `tax_last_depreciation_run_date` to `fixed_assets` and creates `fixed_asset_tax_depreciation_entries` (same shape as book entries minus `journal_entry_id`). Method CHECK widened to `straight_line` / `wdv` / `sum_of_years_digits` on both schedules. Backfill defaults tax = book so existing assets keep a single effective schedule until the CA flips divergence on. New API: `POST /fixed-assets/run-tax-depreciation` (memo-only, idempotent per (asset, year, month)), `PATCH /fixed-assets/:id` (edits the tax schedule post-creation; book schedule is intentionally non-editable after posting — that's a prior-period adjustment, not a casual edit), `GET /fixed-assets/schedule?year=...` (per-asset book-vs-tax side-by-side with year charge + closing NBV + gap). Monthly cron now runs both schedules end-to-end per tenant. Web: list shows book-method / book-NBV / tax-method / tax-NBV columns + book/tax run buttons + link to schedule page; detail page has book/tax tabs with progress bars on each schedule + "edit tax schedule" modal; registration form has a "tax differs from book" toggle with method + life + salvage + annual rate (for WDV) + start date. New `/fixed-assets/schedule?year=YYYY` page shows the full book-vs-tax comparison annually with a timing-difference hint for deferred-tax recognition. | ~~M~~ |
| 41 | ~~Credit note PDF + debit note PDF~~ | — | **Shipped in PR #46.** Bill PDF shipped in PR #45. Document PDF set is now complete: invoice / bill / credit note / debit note / delivery note / GRN / PO / quotation / stock transfer / payslip. | ~~S each~~ |

---

## Platform/Admin (separate from tenant app)

Specified but out of scope for the main tenant app:

- **Super Admin Layer 1 console** (`super-admin-layer1-spec.md`) — platform-owner surface: tenant directory, billing ops, impersonation (consent-gated), revenue analytics.
- **Pricing plan engine** (`pricing-plan-architecture-spec.md`) — tier matrix, feature gating, add-ons, dunning.
- **Landing page** (`landing-page-design-spec.md`) — marketing site.

These are their own workstreams — track separately from this roadmap.

---

## Build cadence

One PR = one feature. Ship, merge, move on. Batched PRs allowed when features are clearly related (e.g. DN + PO PDFs were batched because they follow the same pattern).

Current recommendation: compliance + convenience + role enforcement + FX revaluation + POS + commission engine + digest windows + customer portal + inventory categories + **approval engine (#43 + #43a–#43e, every domain wired)** + purchase requisition (#30) + document attachments (#32) + **kit/bundle items (#35)** + **petty cash float (#38)** + **attendance capture (#39)** + **dual depreciation (#40)** + **batch/serial/expiry tracking (#34)** + **document template builder (#33, v1)** all done. **The M/L nice-to-have pile is empty.** What remains is spec-level follow-ups (drag-drop WYSIWYG for #33, migrating the other 10 PDFs to the template engine, filling out the template library, DN / stock-count / credit-note-reversal / stock-transfer tracking integration from #34's deferred list, credit-note stock reversal, payroll ↔ attendance integration, e-filing + bank feeds + observability from `_gaps.md`) and the platform workstreams (super-admin console, pricing engine, landing page) — all ranked separately.

**#33 document template builder (v1) — shipped.** Per-tenant layout records drive the PDF renderer. Migration `79-document-templates.sql` creates `document_templates` (tenant-scoped, `doc_type` allow-listed via CHECK to the 12 doc types we render today, `status` = `draft` / `published` / `archived`, `is_default` + partial unique `(tenant_id, doc_type, language) WHERE is_default AND deleted_at IS NULL` enforcing at-most-one default per slot, `version` integer, `library_key` preserved when cloned from the library so "based on Classic invoice" surfaces in the UI, RLS on `tenant_id = current_tenant_id()`). Deliberate design choice: the template library ships as a hard-coded TS module (`apps/api/src/modules/operations/template-library.ts`) rather than a platform-level DB table with nullable `tenant_id`. This keeps RLS clean (single "all templates are tenant-scoped" policy, no dual-tenant reads), makes library additions a code-only change (no DDL per new starter), and dodges the subtle bug where a tenant could accidentally UPDATE a shared library row under a relaxed policy. V1 library ships one invoice starter ("Classic") whose layout JSON reproduces the existing hard-coded `InvoicePDF` output byte-for-byte — cloning it and setting it default gives the tenant exactly the same printed output they had before, but now editable. Phase 2 adds PO / GRN / payment advice / quotation / thermal POS receipt (80mm + 58mm) starters to the library. API routes under `/document-templates`: list with doc-type/language/status filters, library list, single get, create-blank (starts as draft), `POST /clone-library` (copies a library entry into a tenant-owned draft), `POST /:id/clone` (bumps version — family tracked by content, no `parent_template_id` column in v1; if we need parent-child chains later we add the FK without breaking existing rows), `POST /:id/set-default` (atomic swap via single UPDATE with a CASE expression so the partial unique index never sees two defaults between statements — the "update B to true, update A to false" two-statement approach would briefly violate the uniqueness constraint), `POST /:id/publish` (draft → published; required before set-default), PATCH (name / description / layout / status), DELETE (soft; blocks with `CANNOT_DELETE_DEFAULT_TEMPLATE` if the row is currently default — user must unset default first), and `GET /active?docType=...&language=...` for the render path (published + default row in the requested language; falls back to EN if the requested language has no default, falls back to null if EN has no default either — render route then drops to hard-coded). All permission-gated on `settings.manage` matching the tenant-admin §2.4 "Documents" grouping. Renderer at `apps/web/lib/template-renderer.tsx` parses `layout_json` of shape `{pageSize, theme, sections[]}` and dispatches each section to a react-pdf block — v1 section types are `header` / `metaRow` / `billTo` / `lineItemsTable` / `totals` / `notes` / `footer` / `spacer` / `text`. Missing / malformed fields fall back to sane defaults (brand-kit tokens, "Thank you for your business.", etc.) so a broken template never 500s a PDF render. Page sizes A4 / A5 / 80mm-thermal / 58mm-thermal (receipts use `[226.77, 1200]` / `[164.41, 1200]` so react-pdf auto-paginates overflow on long line lists). Invoice render route `/app/invoices/[id]/pdf/route.ts` now races `GET /document-templates/active?docType=invoice&language=en` alongside `GET /auth/me` and `GET /invoices/:id`; a published+default template wins, any error (403 from perm check on portal users, 404, malformed body) silently falls through to the hard-coded `InvoicePDF` component — tenants who never touch the builder print identically to pre-#33. The other 11 PDFs (bill / quotation / credit-note / debit-note / delivery-note / proforma / PO / GRN / stock-transfer / payslip / settlement-letter) still use their hard-coded components; wiring each is mechanical (race the active-template lookup, branch on presence, pass context to the renderer) and will land one follow-up PR per doc type as we migrate. The deferred-list rationale: each migration needs its own context builder (what does `{{customer.name}}` map to for a payslip? for a stock-transfer?) and v1 ships proof-of-concept on the highest-volume doc. Tenant admin UI at `/app/settings/document-templates` is a list + editor pair: list page groups tenant templates by (doc-type, language, published-first, recently-updated), shows status / default / version / library-key pills, inline Publish / Set default / Clone / Delete, plus a Library section underneath for one-click "Clone to tenant". Editor at `/[id]` is a structured section-list editor (add from a menu, reorder with ↑/↓, remove, per-section toggles like "show status pill", meta-row fields as a comma-separated list, theme colour pickers for accent + muted, font-family dropdown, page-size dropdown) — **not** the full drag-drop WYSIWYG called out in sell §19.1 "Drag-drop builder". The structured editor covers every block the engine supports today; full drag-drop WYSIWYG with pixel-positioning and typography controls is a Phase 2 on top of the same JSON schema and deferred deliberately. `/new` is a blank-template create form defaulting to an invoice A4 layout with the full section set, so the editor is never empty-state on first open. Settings → Documents gets a new entry. Deferred (Phase 2+): logo upload via MinIO on `tenant_settings`, per-customer / per-supplier template overrides, multi-language auto-translate with manual override per template, full drag-drop WYSIWYG, the other 10 doc types migrated to the engine, Phase 2 library (PO / GRN / payment advice / quotation / thermal POS receipts at 80mm + 58mm), variable interpolation syntax beyond the fixed field map (today `{{customer.name}}` etc. are hardcoded accessors in the renderer — a future `resolveVariable(path, ctx)` with a whitelist map generalises this without schema changes). All deferrals flagged in `_status.md`. Typecheck baselines held (api: 27 ≤ 28, web: 10, db: 1).

**#34 item batch / serial / expiry tracking — shipped.** Opt-in per-item tracking with three independent toggles (`track_batches` / `track_serials` / `track_expiry`) and a `warranty_months` setting for serial-tracked items. Untracked items keep the pre-#34 WAVG-only posting path — this is strictly additive. Migration `78-batch-serial-expiry.sql` creates three new tables and threads two quick-pointer columns onto `stock_ledger` plus a `tracking_input` JSONB on `bill_lines` + `invoice_lines` as the draft-to-post carrier. **`item_batches`** is the FIFO lot ledger: one row per inbound lot carrying `batch_number` / `mfg_date` / `expiry_date` / `original_qty` / `remaining_qty` (floats down as outbound movements consume it; `original_qty` is immutable audit) / `unit_cost_cents` / source document pointers / `supplier_id`. The partial index `item_batches_fifo_idx` on `(tenant_id, item_id, warehouse_id, expiry_date NULLS LAST, received_at) WHERE deleted_at IS NULL AND remaining_qty > 0` is the FIFO workhorse — consumption always orders by expiry (nulls last so batches without an expiry slot after dated batches of the same age), then earliest received. **`item_serials`** is one row per physical unit with a three-state machine (`in_stock` → `sold` → `returned` / `scrapped`), a `batch_id` quick-pointer, acquired + sold document pointers, and `warranty_expires_at`; partial unique on `(tenant_id, item_id, serial_number) WHERE deleted_at IS NULL` so a serial becomes reusable after soft-delete (for the "misprinted serial, scrap and reissue" case) without allowing two live units with the same number. **`stock_movement_batch_allocations`** is the join table `(tenant_id, stock_ledger_id, batch_id, quantity, unit_cost_cents)` — every outbound line that touches a specific lot leaves a row here, which is what drives the recall report without needing to traverse the ledger. On Drizzle's side, `stock_ledger.batch_id` / `serial_id` are plain `uuid()` columns (no Drizzle FK) to dodge the circular-import through `item_batches` / `item_serials` — FKs exist at the SQL level as `REFERENCES item_batches(id) ON DELETE SET NULL` so a scrapped lot's historical ledger entries survive with a nulled pointer. The shared `InboundTrackingInput` / `OutboundTrackingInput` types in `stock-posting.ts` are the single contract every caller (bill post, invoice post, DN post down the road) translates their per-line payload into; inbound has batch number + mfg + expiry + notes + serials[] + supplierId (stamped on both `item_batches.supplier_id` and `item_serials.supplier_id` so recall / warranty queries don't need a join back through the source document), outbound has optional serials[] + optional batchPicks[] (empty batchPicks means FIFO auto-pick). `applyStockReceipt` loads the item's tracking flags up front, validates required-ness (`BATCH_INPUT_REQUIRED` / `EXPIRY_INPUT_REQUIRED` when the toggle is on but the field is missing; `SERIAL_COUNT_MISMATCH` when serial array length ≠ quantity; `SERIAL_BLANK` / `SERIAL_DUPLICATE_IN_PAYLOAD` as cheap input guards) before inserting a single `item_batches` row and N `item_serials` rows per inbound line, threads `batch_id` onto the ledger row, and catches the `item_serials_live_serial_number_idx` unique-index violation as `SERIAL_ALREADY_EXISTS` (preventing reuse of a live serial). `applyStockIssue` first loads item flags, then either validates explicit `batchPicks` (sum matches line qty, every batchId exists with enough `remaining_qty`) or runs FIFO auto-pick (`SELECT ... FOR UPDATE` on candidate batches ordered by the FIFO index so concurrent issues on the same warehouse serialise deterministically instead of oversubscribing a popular lot), validates serial picks against `in_stock` state, decrements `remaining_qty` per batch in the same tx, writes one `stock_movement_batch_allocations` row per batch touched, and flips serials to `sold` with customer + doc + `warranty_expires_at = sold_at + warranty_months × 30 × 24 × 60 × 60 × 1000`. The 30-day-month warranty shortcut is adequate for commercial warranty tracking; exact calendar months deferred (a 12-month warranty lasts 360 days, not 365 — most customer-facing warranties quote "12 months" not "1 year" so the linear-month math is conventional). COGS for batch-tracked items is now `Σ (qty × batchUnitCost)` (batch-weighted — the lot you physically shipped dictates COGS, not the running average), which lines up with the inventory-spec §2.7.4 "cost flows through batch" expectation. Untracked items stay on `qty × runningAvg` — zero behavioural change. Tagged-union `InvoicePostError` + `PostBillCoreError` gain the six new tracking codes, each carrying the specific context needed for a useful error message (which item, expected vs actual serial count, which batch ran short). JSONB `tracking_input` on line tables rather than a side table is a deliberate simplicity call — it's write-once (draft save), validated at the Zod layer, and shape-evolves without migrations. New read-only API under `/items`: `/batches`, `/serials`, `/batches/:id/recall`, `/serials/:id`, `/tracking/expiring?days=N` — all mutation flows through bill/invoice post, these endpoints are purely traceability + reporting. Mounted under `/items` so URLs read naturally (`/items/serials/:id`, `/items/batches/:id/recall`). V1 scope intentionally wires at bill post (inbound) + invoice post (outbound) only — DN/SO outbound, stock count adjust, credit-note reversal, stock transfer are all still non-tracked in this PR (deferred follow-up). Web: item create form has a new "Batch & Serial Tracking" section with three labelled toggles and a conditional warranty-months input; bill create form renders a compact per-line sub-row with batch/mfg/expiry/notes when the line's item has batch or expiry tracking on and a serial-numbers textarea (newline-separated) when serial-tracked; invoice create form shows only a serial-numbers textarea since batch items get FIFO auto-pick in v1. Soft warnings below the serial textarea when the count doesn't match line quantity — draft save never blocks (that's the server's job at post). New `/app/reports/expiring-batches` page with 7/30/90/180-day window tabs, ascending expiry sort, and a days-until-expiry column computed client-side. `/app/items/batches/[id]` shows batch meta + allocation table linking each row to its source invoice / bill / credit note / debit note. `/app/items/serials/[id]` is a two-column trace showing acquired-from / sold-to with warranty-expires. Typecheck baselines held (api: 27 ≤ 28, web: 10, db: 1).

**#40 dual depreciation (book vs tax) — shipped.** Parallel tax-depreciation schedule alongside the existing book schedule, directly out of accounting-spec §8.3. Book schedule continues posting to GL (unchanged). Tax schedule is memo-only — CA uses it in the tax-computation workflow, it never touches the ledger. Migration `77-dual-depreciation.sql` adds seven tax-* columns to `fixed_assets` (`tax_depreciation_method`, `tax_useful_life_months`, `tax_salvage_cents`, `tax_annual_rate_bps` for WDV, `tax_depreciation_start_date`, `tax_accumulated_depreciation_cents`, `tax_last_depreciation_run_date`) and creates `fixed_asset_tax_depreciation_entries` — same shape as the existing book entries table minus `journal_entry_id` (no JE linkage because tax doesn't post). Method CHECK widened on both sides to `straight_line` / `wdv` / `sum_of_years_digits`; the existing book code was SLM-only but there's no reason to block a tenant whose accounting policy is WDV. Backfill defaults every tax column = the book equivalent for existing rows (`tax_depreciation_method := depreciation_method`, same life, same salvage, same start date) so assets registered before this migration keep a single effective schedule until the CA actively flips divergence on. `computeMonthlyDepreciation(method, {cost, salvage, life, accumulated, rate, monthsSinceStart})` is the new shared compute: SLM = `(cost−salvage)/life` capped at remaining; WDV = `NBV × rateBps/10000/12` capped at remaining, where rate falls back to `12/life×10000` bps when the CA hasn't wired an IRD rate; SOYD = annualised per year-digit divided by 12. Book and tax both route through the same function — the difference is the (life, salvage, accumulated, rate) tuple each schedule feeds in. New endpoints: `POST /fixed-assets/run-tax-depreciation` (memo-only, idempotent per (asset, year, month) via the new unique index — no JE is posted, just inserts into the tax entries table and stamps `tax_accumulated` on the asset), `PATCH /fixed-assets/:id` (tax-schedule edits + name/code/category/notes; book schedule is intentionally non-editable post-creation because retroactively restating book depreciation is a prior-period adjustment, not a casual edit), `GET /fixed-assets/schedule?year=YYYY` (per-asset book-vs-tax side-by-side with year-total charges from both entry tables + closing NBV on both sides + gap). The existing `runMonthlyDepreciationForAllTenants` BullMQ cron now runs both schedules end-to-end per tenant — book first (posts JE), then tax (memo). Web: list shows book-method / book-NBV / tax-method / tax-NBV columns side-by-side + separate "Run book" and "Run tax" buttons with clear "no GL impact" labelling on the tax success card + link to the schedule page; detail page has Book history / Tax history tabs with progress bars on each schedule + a "Book ↔ tax gap" stat card showing the timing difference + an inline "Edit tax schedule" modal; registration form has a "tax differs from book" toggle that reveals tax method / life / salvage / annual rate (shown only for WDV) / start date fields. New `/app/fixed-assets/schedule?year=YYYY` page shows full book-vs-tax annual comparison with a timing-difference summary hint for deferred-tax recognition ("Book > tax: deferred tax asset builds" / "Tax > book: deferred tax liability builds"). Typecheck baselines held (api: 28, web: 10, db: 1).

**#39 attendance capture — shipped.** Per-employee daily attendance with four capture methods (self / QR / biometric file import / geofence+photo / manual muster) folded onto one record per `(employee, attendance_date)`. Migration `76-attendance.sql` creates four tables: `attendance_devices` (biometric / QR / manual device registry with `column_template` JSONB remembering the last import's column → field mapping, `last_import_at`, per-tenant partial-unique `name` WHERE deleted_at IS NULL), `biometric_employee_map` ((device_id, biometric_employee_id) → employee_id — varchar device ID so alphanumeric schemes round-trip, partial unique on the triple), `attendance_records` (the event — `check_in_at` / `check_out_at` / `total_minutes` / `method` / `status` / `source_device_id` / `supervisor_user_id` / `location_lat,lng` / `has_conflict` + reason, partial unique `(tenant_id, employee_id, attendance_date)` WHERE `deleted_at IS NULL` as the dedup choke point, CHECK `check_out_at >= check_in_at`), `attendance_imports` (audit row per biometric file: `rows_total` / `imported` / `skipped` / `errored` + `errors` JSONB array of `{row, reason, biometricEmployeeId?}`, status `processing` / `completed` / `failed`). RLS on all four via `current_tenant_id()`. Dedup is at the DB level: a second write for an existing `(employee, date)` updates with earliest-in / latest-out merge; a DIFFERENT method triggers `has_conflict=true` with a reason string rather than rejecting — we surface the exception, we don't pick a winner. Widens `document_attachments` entity-type CHECK to add `'attendance_record'` so geofence group photos attach via the existing #32 infrastructure. Import endpoint is stateless JSON: the web wizard parses CSV client-side (split on comma, strip quotes), the user maps columns → fields (biometric_id / punch_at / direction), and POSTs `{attendance_device_id, file_name, column_template, rows: [{biometric_employee_id, punch_at, direction?}]}`. Server resolves each row via `biometric_employee_map`; unknown IDs go to errors + `rows_skipped` rather than failing the batch. On success the column_template saves back to the device row so next file from the same device auto-maps. Self check-in / out works against the current day and the employee linked to the authenticated user (falls back to an `employee_id` body param under `attendance.operate`). Manual muster is a bulk upsert: `{attendance_date, employee_ids[], status?, notes?}` returns per-employee `{created|updated|conflict}`. Exceptions endpoint returns records where `has_conflict=true` OR (`check_in_at IS NOT NULL AND check_out_at IS NULL AND attendance_date < today`) — a missing clock-out is an exception by default. Permission keys `attendance.operate` (write — mark / import / edit / delete) and `attendance.view` (read — list / detail / exceptions) seeded onto Owner / Admin / Accountant role templates via the `seed_admin_role_templates_for_tenant` CREATE OR REPLACE + backfill UPDATE pattern. Audit events: `attendance_device.created` / `.updated` / `.deleted`, `biometric_map.updated`, `attendance_record.created` / `.updated` / `.deleted` / `.check_in` / `.check_out` / `.muster`, `attendance_import.completed` / `.failed`. Web: list at `/app/attendance` with default last-7-days window, employee / branch / status / method / conflict filters + exceptions alert banner; detail page with edit form + `<AttachmentsPanel entityType="attendance_record">`; devices registry at `/app/attendance/devices` with inline biometric-mapping modal (replace-all semantics); import wizard at `/app/attendance/import` (3 steps: pick device + CSV upload, map columns with heuristic pre-fill + saved-template pre-fill, preview first 20 rows + commit, then success card with per-row error list). XLSX parsing and live device API polling deferred to Phase 2 (per spec §5.4); payroll wire-up (converting `total_minutes` into wages) deferred to the payroll-integration follow-up. Typecheck baselines held (api: 28, web: 10).

**#38 petty cash float — shipped.** Per-branch non-mandatory operational cash floats. Migration `75-petty-cash.sql` creates four tables: `petty_cash_floats` (header with `ceiling_cents`, `current_balance_cents`, `petty_cash_account_id` pointing at the shared `1005 Petty Cash` COA account, `status` active/closed, `float_holder_user_id`), `petty_cash_transactions` (ledger with `txn_type` one of `expense` / `advance_out` / `advance_return` / `top_up` / `variance_short` / `variance_over` / `close_transfer`, `is_voided` + reversal linkage), `petty_cash_top_up_requests` (workflow `pending` → `approved` / `rejected` → `posted` / `cancelled`), `petty_cash_reconciliations` (EOD snapshots: expected balance, counted balance, variance JE handle). RLS on all four via `current_tenant_id()`, partial unique index enforces one active float per branch. Balance atomicity via `SELECT ... FOR UPDATE` on the float row inside the same tx as the JE post and the `current_balance_cents` update. One shared `1005 Petty Cash` COA account across all floats in the tenant (not per-float, not per-branch) — the per-float balance lives on the `petty_cash_floats` row, the GL rolls up. (Note: the COA seed had `1020` already taken by "Bank — cheques in transit", so this ships as `1005` rather than the common `1020` convention; cosmetic only since it's a whitelisted asset code, flagged for awareness.) JE-per-transaction through the existing `postJournal` choke point — expenses debit the expense account + credit petty cash, advances debit a staff-advance asset + credit petty cash, returns flip, top-ups debit petty cash + credit bank/cash source, variance posts to `5190 Cash Over/Short` mirroring the POS shift variance pattern. Voids go through `postReversingJournal` which flips DR/CR on every line, rolls back the float balance, and (for voided top-ups) rolls the originating top-up-request from `posted` back to `approved` so it can be re-posted. Top-up workflow enforces SOD (requester ≠ approver) at the API layer. Ceiling is enforced only at top-up post (not at approve) so approvals can park while a float is still above ceiling; `advance_return` is allowed to temporarily exceed ceiling since that's the natural shape of a refund. Reconciliation computes the expected balance from the window `(prev_recon_date, recon_date]` by summing `txn_type`-grouped movements, compares to the user-entered counted total, books any delta to 5190 Cash Over/Short, and stamps the float balance to match the counted figure. Attachments reuse `document_attachments` — new entity type `petty_cash_transaction` added to the whitelist CHECK. Permission keys `petty_cash.operate` (holders + accountants) and `petty_cash.approve` (approvers only) seeded onto Owner/Admin/Accountant role templates via the `seed_admin_role_templates_for_tenant` CREATE OR REPLACE. Close-float transfers the remaining balance out to a bank/cash account via a final `close_transfer` transaction and flips status to `closed` — reopening a closed float is not supported (open a new one instead). Web: list with active/closed sections + near-ceiling warning (>90%), open-float form with optional seed top-up in the same transaction (filters cash/bank asset accounts excluding `1005` itself), detail page with Ledger / Top-ups / Reconciliations tabs and inline modals for expense / advance-out / advance-return / top-up request / post top-up / reconcile / close, `<AttachmentsPanel entityType="petty_cash_transaction">` per ledger row. Audit events: `petty_cash_float.opened` / `.closed` / `.updated`, `petty_cash_transaction.posted` / `.voided`, `petty_cash_top_up.requested` / `.approved` / `.rejected` / `.posted` / `.cancelled`, `petty_cash_reconciliation.posted`. Typecheck baselines held (api: 28, web: 10).

**#35 kit/bundle items — shipped (PR #82).** Lightweight, no-BOM assembly (inventory spec §9). A bundle item is a virtual SKU with a static component list; selling one explodes at invoice post time into per-component stock issues with weighted-average COGS. Migration `74-item-bundle-components.sql` creates `item_bundle_components` keyed by `(bundle_item_id, component_item_id)` unique pair with `quantity numeric(18,4)`, a `CHECK (component_item_id <> bundle_item_id)` self-ref guard, `ON DELETE CASCADE` from the bundle side (hard-deleting a bundle removes its rows) but `ON DELETE RESTRICT` from the component side (a component referenced by a bundle can't be hard-deleted from under it — soft-delete via `items.deleted_at` is fine and the explosion filters soft-deleted components out). `items.item_type='bundle'` forces `track_inventory=false` at write time — bundles have no `item_balances` row and never appear in stock reports, keeping WAVG math simple (no assembly receipts, no rollup cost stored anywhere). No nested bundles: the rule lives at the application layer in `PUT /items/:id/components` rather than a DB trigger since the bundle universe is single-writer and the app-level check returns friendlier errors. The main invariant work sits in `invoice-posting.ts`: a new preload query fetches every referenced bundle's components in one shot, then the line loop skips `peekStockIssue` on the bundle itself and instead runs one peek per component at `(lineQty × componentQty)`. Each component's `cogsCents` rolls into the same `totalCogsCents` so the JE still has one inventory-CR + one COGS-DR pair per invoice. Per-component movements commit to `stock_ledger` individually (one row each, memo `Invoice N — bundle X component Y`) so the audit trail shows exactly which SKU left which warehouse. The invoice document keeps one line per bundle (spec §9 — no sub-line disaggregation). `NEGATIVE_STOCK` surfaces the component item's name not the bundle's so the user knows which SKU to replenish. `BUNDLE_COMPONENT_MISSING` is a new defensive error for the impossible-but-audit-worthy case where a bundle references a missing component. Same explosion logic lives in `delivery-notes.ts` for tenants whose `stockRelieveOn='delivery_note'`. POS sales go through the shared `postDraftInvoice` path so they inherit bundle support for free. Empty component list legal and permissive (tenants sometimes use bundles as placeholder SKUs for hand-assembly or as service-like revenue with zero COGS); UI warns but doesn't block. API endpoints: `GET /items/:id` (inlines `components: []` for non-bundles for a stable client shape), `PATCH /items/:id` (rejects bundle→non-bundle flips while components exist; forces `trackInventory=false` on any move to bundle), `GET /items/:id/components`, `PUT /items/:id/components` (replace-all in one transaction: delete-then-insert with `sort_order = array index`; validates no duplicates, no self-reference, no nested bundles, every component exists + tenant-scoped + not soft-deleted). New audit kind `item_bundle.updated`. Web: bundle badge on the item list, bundle-typed items show a Components section in the create drawer with an item picker (filters out bundles + self) + quantity input + add/remove rows; the editor consolidates duplicate componentItemIds client-side so the wire contract stays clean. New detail page at `/app/items/:id` with a read/edit view; bundle items get the same component editor there for post-creation edits. Credit-note stock reversal was considered but deferred — credit notes don't currently reverse stock at all (pre-existing limitation predating this PR), so reversing only bundle stock would be inconsistent. A follow-up PR will add full credit-note stock reversal (mirroring the bundle explosion in reverse) — noted inline in `credit-notes.ts` and flagged in `_gaps.md`. Fragile-area row added to `_status.md` §3: "Bundle explosion at invoice post" — the new branch affects every invoice with a tracked line. Typecheck baselines held (api: 28, web: 10, db: 1).

**#32 document attachments — shipped (PR #81).** Generic cross-entity attachments backed by MinIO (self-hosted S3-compatible object storage already in `docker-compose.yml`). One `document_attachments` table keyed by `(tenant_id, entity_type, entity_id)` with a CHECK-constrained whitelist of entity types so new attachable surfaces are explicit additions, not ambient. Storage keys include the tenant UUID as the first path segment — tenant isolation stays intact even if a bucket policy is ever too permissive. 10 MB per-file cap enforced at three layers (fastify-multipart, DB CHECK, module validation). 7-year `retention_until` default (SL audit norm) + soft-delete preserves rows for audit until retention expires. API: `POST /attachments` (multipart), `GET /attachments?entityType=&entityId=`, `GET /attachments/:id`, `GET /attachments/:id/preview` (inline for PDF/image), `DELETE /attachments/:id` (soft). Shared `<AttachmentsPanel />` client component with drag-drop + preview + download + soft-delete, wired into invoice / bill / PO / PR detail pages as reference integrations — other detail pages can bolt it on in one line. `ensureBucket()` runs at API boot and logs a warning rather than crashing if MinIO is unreachable, so dev workstations without the `minio` container running still boot the API. Audit events `attachment.uploaded` and `attachment.deleted`.

**#30 purchase requisition — shipped (PR #80).** Off-by-default module under Settings → Modules. Internal "request to buy" document with per-line approval, routed through the generic approval engine (#43) when a `purchase_requisition` policy matches — otherwise direct approve. Converts to a draft PO linked back via `source_pr_id` / `source_pr_line_id`. Shares the same shared-core + `allowStatuses` race-guard pattern as bills/POs/payroll/final-settlements, so the immediate path and engine finaliser run one atomic code path. SOD enforced at both entry points. Sidebar gated by the tenant toggle — off-by-default tenants don't see the menu item at all, and existing tenants keep the Buy menu unchanged until they flip it on.

**#43e final-settlement routing — shipped (PR #79).** The last approval-engine domain. `/final-settlements/:id/approve` now consults `resolveApplicablePolicy` first and parks matching settlements in `pending_approval`; the approval queue drives the flip to `approved` (posting stays as a separate step) via `finaliseApprovedDocument` → `approveFinalSettlementCore`. Tenant-admin §7.1 "always-approve" = policy with empty triggerRule. Reject / cancel flip back to draft + clear engine handle + stamp reason into notes — same pattern as bills / POs / payroll / bonus runs. With this merge the generic approval engine work (#43) is fully complete.

**#43d payroll + bonus run routing — shipped (PR #78).** Same dual-path shape: `postPayrollRunCore` / `postBonusRunCore` serve both immediate and engine-finalised paths with `allowStatuses` race guards. Payroll runs are `idempotent on runNumber` if the finaliser retries. Empty triggerRule = always-approve (the spec default for payroll); bonus runs are threshold-gated via `minAmountCents`.

**#43c purchase order routing — shipped (PR #77).** `/purchase-orders/:id/send` checks `resolveApplicablePolicy` first and parks matching POs in `pending_approval`; the approval queue drives the flip to `sent` (number allocation) via `finaliseApprovedDocument` → `sendPurchaseOrderCore`. Extended `trigger_rule` with `firstPoFromSupplier: boolean` — the submit route pre-computes `isFirstPoFromSupplier` from existing non-cancelled POs and `evaluateTrigger` matches it. Approval-policy editor UI exposes the new flag when documentType=purchase_order. Reject/cancel flip back to draft + clear engine handle + stamp reason into notes (same pattern as bills).
