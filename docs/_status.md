# PettahPro Build Status

Living counterweight to [`_roadmap.md`](./_roadmap.md). The roadmap says what's shipped; this says what's *broken, fragile, or at-risk right now.* Consult before picking up any task that touches a listed module — the fastest way to ship a regression is to assume everything is fine because a feature is in the "shipped" list.

Every PR should end with a check: does anything here need to be added, cleared, or bumped?

Last updated: 2026-04-24 (#47 API rate limiting, per-IP). Second promotion from the `_gaps.md` parking lot (A6). `@fastify/rate-limit` v9 registered globally before the identity plugin — the order matters: a pre-auth flood (credential stuffing / signup spam / OTP enumeration) now gets throttled before it ever hits the session-cookie lookup, so the attack surface we care about (unauthenticated brute force) eats into the IP budget without giving the attacker any feedback beyond a 429. **Redis-backed store reuses the app's existing `ioredis` client**, just prefixed `pp-ratelimit:` to keep keys clear of sessions / OTPs / bullmq on the same redis instance — zero new infra, zero new docker container. `apps/api/src/plugins/rate-limit.ts` is the only new file on the API. Global default **600 req/min/IP** (env-tunable: `RATE_LIMIT_GLOBAL_MAX` + `RATE_LIMIT_GLOBAL_WINDOW`) — chosen to be invisible to a legitimate tenant clicking hard through the UI; anything above is automation or genuine abuse and deserves a 429. The load-bearing limits are the per-route overrides on the four brute-force hotspots: `/auth/login` 10/min/IP (fat-fingered retries + locked-out users will push this; anything higher is automation), `/auth/signup` 5/10min/IP (tenants don't spin up that fast), `/portal/auth/request-otp` 5/10min/IP (layered ON TOP of the existing per-email `tryConsumeOtpRateBudget` — one protects a single customer from OTP spam, the other protects the platform from an IP cycling through different emails), `/portal/auth/verify` 10/min/IP (enough headroom for fat-fingered 6-digit codes inside the 10-minute OTP expiry without locking out legitimate customers). Per-route overrides are set via route-level `config.rateLimit` on each `fastify.post(...)` call so they live next to the handler instead of in a central routing table that drifts. `keyGenerator: req => req.ip` honours the existing `trustProxy: true` in `server.ts` so the real client IP is used when behind a reverse proxy (not the proxy's IP). `continueExceeding: true` means repeated requests after exhausting the budget extend the cooldown rather than resetting neatly at window boundaries — stops "wait for the minute to flip over" timing games. **`skipOnError: true` — fail open on redis outage.** In a redis blip we'd rather serve the app than blanket-503 every tenant; the observability stack shipped in #46 will surface the outage in minutes and the brute-force risk window is bounded by how long redis is down. `/health` / `/health/` / `/metrics` allow-listed so the Prometheus scraper + load-balancer probes never get clipped no matter how hot the IP. Response envelope `{ error: { code: "RATE_LIMITED", message: "Too many requests. Please retry in <n>s.", retryAfterSeconds } }` matches the app's existing error shape; web login + signup forms already rendered `err.message` on unknown ApiError codes, so the default UX is "user sees the actual retry message" — a friendly branch on `err.code === "RATE_LIMITED"` was added to both forms so the phrasing reads "Too many sign-in attempts. Please wait a moment and try again." / "Too many signup attempts from this network. Please wait a few minutes." instead of the raw seconds countdown. Portal auth paths already had their own RATE_LIMITED envelope (from the per-email budget) so the 429 renders identically. `RATE_LIMIT_DISABLED=true` env short-circuits the whole plugin for local debugging sessions where the limit keeps clipping your repro cycle. New ops knobs documented as env: `REDIS_URL`, `RATE_LIMIT_DISABLED`, `RATE_LIMIT_GLOBAL_MAX`, `RATE_LIMIT_GLOBAL_WINDOW`. New fragile area: the registration order in `server.ts` is load-bearing — rate-limit MUST sit between `errorTrackingPlugin` and `identityPlugin`, because identity mounts `/auth/*` and the per-route `config.rateLimit` overrides only work if the rate-limit plugin was registered at parent scope first. Zero schema, zero migration, zero new docker infra, one dependency (`@fastify/rate-limit@^9.1.0`). Typecheck baselines held (api: 27, web: 10, db: 1). Previously: PR #88 — #46 observability stack, self-hosted OSS. First item promoted from the `_gaps.md` parking lot after the M/L nice-to-have pile emptied — covers both K1 (metrics / dashboards / alerting) and K2 (error tracking) in one PR because they share infrastructure (a second docker-compose file), one docs run-book, and one SDK wire-up moment. All self-hosted OSS per the stack preference — no Datadog / Sentry SaaS bills. Seven containers in `docker-compose.observability.yml`, every port bound to `127.0.0.1` because this is operator tooling, not tenant-facing: **Prometheus** (9090, 30-day TSDB retention, scrapes `pettahpro-api:4000/metrics` + `pettahpro-worker:4100/metrics` on 15s interval), **Grafana** (3001, admin/admin default password warned in docs, datasources + dashboards both file-provisioned), **Loki** (3100, single-binary filesystem TSDB, 7-day retention), **Promtail** (tails `/var/run/docker.sock` ro — relabels by `container` + `compose_service` + `compose_project` so LogQL can filter `{compose_service="api"} \| json \| tenantId="…" \| level="error"`), **GlitchTip** (8000, Sentry-protocol-compatible drop-in, MIT, single Django container vs full Sentry's 20+ containers — if a tenant ever needs native Sentry the SDK DSN is the only thing that changes), **glitchtip-postgres** (isolated — never reuses the tenant DB), **glitchtip-worker** (celery for async error ingestion). GlitchTip reuses the main redis at database index 1 so we don't run a second redis. API exposes `/metrics` via `apps/api/src/plugins/telemetry.ts` — `prom-client` v15 registry with `collectDefaultMetrics` (heap, GC pause, event-loop lag, CPU seconds) plus three HTTP series: `http_requests_total{method,route,status_code}` counter, `http_request_duration_seconds{method,route,status_code}` histogram (buckets up to 10s), `http_requests_in_flight{method}` gauge. **Route label cardinality is the load-bearing design choice** — labels use Fastify's matched template (`req.routeOptions?.url` → `/invoices/:id`, fallback `"unmatched"`) so a hot endpoint hit 10M times with different ids is still one series, not 10M. `onRequest` hook stashes `process.hrtime()` + bumps in-flight; `onResponse` hook decrements + records duration — registered AFTER `tenantContextPlugin`, BEFORE `identityPlugin` so request duration covers the full middleware stack but route-label lookup works regardless of auth state. Worker has no Fastify instance so `apps/api/src/worker-metrics.ts` stands up a separate `http.createServer` on `WORKER_METRICS_PORT` (default 4100; set to `0` to disable) with its own registry labelled `{process: "worker"}` plus two domain series — `worker_scheduled_jobs_total{name,outcome}` counter + `worker_scheduled_job_duration_seconds{name,outcome}` histogram (buckets up to 600s). The existing scheduled-worker `Worker` callback was refactored to extract a `runScheduledJob(name)` helper so the wrapping try/catch can call `recordScheduledJob(name, outcome, durationSeconds)` on every branch and `captureException(err)` on failure — one line of metrics + one line of error tracking per job. Error tracking via `@sentry/node` v8 (`apps/api/src/plugins/error-tracking.ts`) + `@sentry/nextjs` v8 (`apps/web/sentry.{client,server,edge}.config.ts` + `apps/web/instrumentation.ts` with `experimental.instrumentationHook: true` in `next.config.mjs` — Next 14 opt-in, flips on by default in 15). **No-op-if-DSN-empty is the dev ergonomics primitive** — `ensureInitialised()` bails when `SENTRY_DSN` is unset so dev workstations never ship traces anywhere and the same image deploys unchanged across envs; prod wires the DSN from GlitchTip's project-settings page. `beforeSend` drops status 400–499 so a tenant typing a bad invoice date doesn't land in the ops backlog — only 5xx + unhandled throws capture. Fastify `onRequest` hook tags the scope with `tenant_id` + `user_id` from req so every exception carries context; `onError` hook calls `Sentry.captureException(err, {contexts: {response: {status_code}}, tags: {method, route}})`. `captureException(err, context?)` + `initErrorTrackingForWorker()` exported for non-HTTP callers (worker). Web client SDK reads `NEXT_PUBLIC_SENTRY_DSN` (public, fine to ship), web server reads `SENTRY_DSN` (private). `tracesSampleRate: 0` default — raise per-env only when debugging a specific latency issue. One Grafana dashboard `PettahPro → API overview` auto-loaded from `docker/observability/grafana/dashboards/api-overview.json` with a 30s file-provisioner poll so the JSON is editable live without redeploy: headline strip (rps / 5xx rps / in-flight / p95), request rate by route, latency percentiles (p50/p95/p99), errors by route, node heap (api + worker), worker scheduled jobs (rate by name + outcome), event-loop lag p95. Dashboard JSON left intentionally hackable — tenants can fork it into new JSONs in the same folder and Grafana picks them up. Docs run-book at `docs/observability.md` walks the bring-up (`docker compose -f docker-compose.yml -f docker-compose.observability.yml up -d`), GlitchTip wiring, endpoint catalog, LogQL example, retention tuning table, troubleshooting for the three most likely failure modes (Prometheus target "down" / Grafana datasource proxy error / Promtail docker.sock permission), and the "deliberately deferred" list: alertmanager rules (no paging in v1), synthetic probes, per-tenant dashboards, trace sampling. **Dev defaults to zero overhead** — `SENTRY_DSN` empty + the observability compose file stays down. Cosmetic roadmap fix bundled: row #43c (approval engine — PO routing) was missing strike-through despite PR #77 shipping — fixed now. Roadmap row #46 added, `_gaps.md` K1 + K2 deleted per the file's promotion discipline. Typecheck baselines held (api: 27, web: 10, db: 1). Previously: PR #87 — #33 document template builder v1. Eighth — and **final** — post-#43 nice-to-have. Drag-drop-ish PDF layout engine per tenant with multi-language support, backing sell §19 / buy §20 / tenant-admin §2.4. Pragmatic v1 scope: the engine + persistence + structured section editor + one migrated doc type (invoice) — not the full WYSIWYG the spec gestures at (deferred, flagged at the editor UI). Migration `79-document-templates.sql` creates `document_templates` (id, tenant_id, doc_type varchar(40) with a CHECK allow-listing the 12 supported document types, language varchar(5) default `'en'`, name, description, `layout_json jsonb default '{}'` — the opaque schema owned by the web renderer, version int, status `draft`/`published`/`archived`, is_default boolean, library_key varchar(80), created_by_user_id, timestamps, deleted_at). Partial unique index `document_templates_one_default_idx` on `(tenant_id, doc_type, language) WHERE is_default AND deleted_at IS NULL` is the uniqueness invariant — exactly one default per (doc_type, language) slot. RLS via `current_tenant_id()`. Template library is a **hard-coded TS module** (`template-library.ts`) not a platform-level DB table — avoids nullable-tenant RLS gymnastics, library additions become code-only changes, and there's no risk of a tenant accidentally writing to shared rows. `CLASSIC_INVOICE_LAYOUT` seeds a faithful copy of the existing hard-coded `InvoicePDF` output (header, metaRow, billTo, lineItemsTable, totals with tax breakdown, notes, footer). New API module `modules/operations/document-templates.ts` mounted at `/document-templates` with 11 routes (list / library / get / create / clone-library / clone / set-default / publish / patch / delete / GET /active). Set-default is the interesting atomic primitive — instead of two statements (clear old + set new) that would briefly violate the partial unique index, it's a **single UPDATE with a CASE expression** (`SET is_default = (id = $target)`) so the invariant holds through the whole statement. Delete refuses a default row with `CANNOT_DELETE_DEFAULT_TEMPLATE`. `/active?docType=invoice&language=xx` is the public render-time lookup — falls back EN → null. New renderer `apps/web/lib/template-renderer.tsx` walks the JSON layout: `parseLayout(raw)` defensively normalises shape (missing sections / bad theme → sensible defaults), `pageSizeProp` maps `a4`/`a5`/`thermal_80`/`thermal_58` to react-pdf size props (thermal uses mm-derived width + `height: 1200` for auto-pagination), `buildInvoiceStyles(theme)` parameterises `StyleSheet.create` on theme colours / font family, `renderInvoiceSection` switches on section.type emitting react-pdf primitives. Invoice PDF route wires the fallback: `Promise.all` races the existing invoice + org-settings fetches with a new `/document-templates/active` fetch (with `.catch(() => null)`); `activeTemplate ? renderInvoiceTemplate(...) : InvoicePDF(...)` so any template-fetch failure (403 on portal users, 404, JSON parse error) silently falls through to the hard-coded component — **pre-existing tenants print identically to pre-#33** until they actively clone + publish + default a template. Web admin: `/app/settings/document-templates` list grouped by doc_type/language with inline publish / setDefault / clone / delete actions + library card grid with "Clone to tenant" CTA; `/app/settings/document-templates/new` blank-template form that seeds a default A4 invoice layout so the editor isn't empty-state; `/app/settings/document-templates/[id]` structured section editor (not full WYSIWYG) with page-size dropdown, theme colour pickers (primary / accent / muted / text), font family, and add/reorder/remove sections with a per-section `SectionFields` sub-form (meta-row fields as comma-separated list, text emphasis picker, footer text, spacer height). Only invoice is wired to the template engine in v1 — every other doc_type (bill, PO, GRN, payslip, settlement letter, etc.) still uses its hard-coded PDF module until follow-up PRs migrate them. The CHECK allow-lists them now so the DB schema doesn't have to change per migration. New fragile area: template renderer fallback path (§3) — if the `/active` endpoint ever fails loudly (instead of `.catch(() => null)`), every invoice PDF would 500; and if a tenant publishes a malformed layout, `parseLayout` must never throw. Typecheck baselines held (api: 27, web: 10, db: 1). **The M/L nice-to-have pile is empty.** Previously: PR #86 — #34 batch / serial / expiry tracking. Seventh post-#43 nice-to-have. Opt-in per-item tracking with three independent toggles (`track_batches` / `track_serials` / `track_expiry`) and a `warranty_months` setting for serial-tracked items — strictly additive, untracked items stay on the pre-#34 WAVG-only path. Migration `78-batch-serial-expiry.sql` creates three new tables: `item_batches` (FIFO lot ledger with `remaining_qty` floating as outbound consumes it; partial FIFO index `(tenant_id, item_id, warehouse_id, expiry_date NULLS LAST, received_at) WHERE deleted_at IS NULL AND remaining_qty > 0`), `item_serials` (one row per unit, state machine `in_stock` → `sold` → `returned`/`scrapped`, partial unique on `(tenant, item, serial_number) WHERE deleted_at IS NULL` so serial reuse after scrap works), `stock_movement_batch_allocations` (join table driving recall reports — every outbound touches this). Adds `batch_id`/`serial_id` quick-pointer columns on `stock_ledger` + `tracking_input` JSONB on `bill_lines` + `invoice_lines` as the draft-to-post carrier. Drizzle quick-pointers are plain `uuid()` columns (no Drizzle FK) to dodge circular imports through `item_batches`/`item_serials`; SQL-level FKs exist as `REFERENCES item_batches(id) ON DELETE SET NULL`. Tracking input is JSONB, not a side table — write-once at draft, validated at Zod, shape-evolves without migrations. `applyStockReceipt` loads item flags, validates required-ness (`BATCH_INPUT_REQUIRED` / `EXPIRY_INPUT_REQUIRED` / `SERIAL_COUNT_MISMATCH` / `SERIAL_BLANK` / `SERIAL_DUPLICATE_IN_PAYLOAD`) before inserting a single `item_batches` row + N `item_serials` rows per inbound line, catches the unique-index violation as `SERIAL_ALREADY_EXISTS`. `applyStockIssue` either validates explicit `batchPicks` or runs FIFO auto-pick (`SELECT ... FOR UPDATE` on batches ordered by the FIFO index so concurrent issues serialise deterministically), decrements `remaining_qty`, writes allocation rows, flips serials to `sold` with `warranty_expires_at = sold_at + warranty_months × 30 × 24 × 60 × 60 × 1000` (30-day months — conventional for "12-month warranty" semantics, exact calendar months deferred). COGS for batch-tracked items is now `Σ (qty × batchUnitCost)` (batch-weighted — you ship the lot you physically grab); untracked items keep `qty × runningAvg` unchanged. Tagged-union `InvoicePostError` + `PostBillCoreError` gain six tracking codes carrying the specific context (which item, expected vs actual serial count, which batch ran short). New read-only API under `/items`: `/batches`, `/serials`, `/batches/:id/recall`, `/serials/:id`, `/tracking/expiring?days=N` — all mutation through bill/invoice post, these are purely traceability + reporting. V1 scope wires inbound-at-bill-post + outbound-at-invoice-post only — DN/SO outbound, stock count adjust, credit-note reversal, and stock transfer are all still non-tracked in this PR (deferred follow-up). Web: item create form gains a "Batch & Serial Tracking" section with three toggles + conditional warranty-months input; bill create form renders a per-line sub-row with batch/mfg/expiry/notes when the picked item has those toggles, and a serials textarea when serial-tracked; invoice create form shows only a serials textarea (batch items use FIFO auto-pick in v1 — no UI picker); new `/app/reports/expiring-batches` page with 7/30/90/180 day windows, `/app/items/batches/[id]` with allocation table linking to source docs, `/app/items/serials/[id]` two-column acquired/sold trace. Draft save never blocks on tracking input — validation is server-side at post, UI shows soft warnings. New fragile area: FIFO `SELECT ... FOR UPDATE` is the only safeguard against two concurrent invoices oversubscribing the same lot. Typecheck baselines held (api: 27, web: 10, db: 1). Previously: PR #85 — #40 dual depreciation. Sixth post-#43 nice-to-have. Parallel tax-depreciation schedule alongside the existing book schedule, directly out of accounting-spec §8.3. Book schedule keeps posting to GL (unchanged). Tax schedule is memo-only — used by the CA in tax computation, never touches the ledger. Migration `77-dual-depreciation.sql` adds seven `tax_*` columns to `fixed_assets` (`tax_depreciation_method`, `tax_useful_life_months`, `tax_salvage_cents`, `tax_annual_rate_bps` for WDV, `tax_depreciation_start_date`, `tax_accumulated_depreciation_cents`, `tax_last_depreciation_run_date`) and creates `fixed_asset_tax_depreciation_entries` — same shape as the existing book-entries table minus `journal_entry_id` (no JE linkage because tax doesn't post). Method CHECK widened on both sides to `straight_line` / `wdv` / `sum_of_years_digits`; the existing book code was SLM-only but there's no reason to block a tenant whose accounting policy is WDV. Backfill defaults every tax column = the book equivalent for existing rows (`tax_depreciation_method := depreciation_method`, same life, same salvage, same start date) so assets registered before this migration keep a single effective schedule until the CA actively flips divergence on. New `computeMonthlyDepreciation(method, {cost, salvage, life, accumulated, rate, monthsSinceStart})` is the shared compute: SLM = `(cost−salvage)/life` capped at remaining; WDV = `NBV × rateBps/10000/12` capped at remaining (with a `12/life` fallback rate when the CA hasn't wired an IRD rate — keeps the compute well-defined); SOYD = annualised per year-digit divided by 12. Book and tax both route through the same function — only the (life, salvage, accumulated, rate) tuple differs. New endpoints: `POST /fixed-assets/run-tax-depreciation` (memo-only, idempotent per (asset, year, month) via the new unique index — no JE is posted, just inserts into the tax entries table and stamps `tax_accumulated` on the asset), `PATCH /fixed-assets/:id` (tax-schedule edits + name/code/category/notes; book schedule is intentionally non-editable post-creation because retroactively restating book depreciation is a prior-period adjustment, not a casual edit), `GET /fixed-assets/schedule?year=YYYY` (per-asset book-vs-tax side-by-side with year-total charges from both entry tables + closing NBV on both sides + gap). The existing `runMonthlyDepreciationForAllTenants` BullMQ cron now runs both schedules end-to-end per tenant — book first (posts JE), then tax (memo). Web: list shows book-method / book-NBV / tax-method / tax-NBV columns side-by-side + separate "Run book" and "Run tax" buttons with clear "no GL impact" labelling on the tax success card + link to the schedule page; detail page has Book history / Tax history tabs with progress bars on each schedule + a "Book ↔ tax gap" stat card showing the timing difference + an inline "Edit tax schedule" modal; registration form has a "tax differs from book" toggle that reveals tax method / life / salvage / annual rate (shown only for WDV) / start date fields. New `/app/fixed-assets/schedule?year=YYYY` page shows the full book-vs-tax annual comparison with a timing-difference summary hint for deferred-tax recognition ("Book > tax: deferred tax asset builds" / "Tax > book: deferred tax liability builds"). No new fragile areas — the tax schedule never posts to GL, so the worst failure mode is an incorrect tax computation (visible in the schedule page), not a broken ledger. Typecheck baselines held (api: 28, web: 10, db: 1). Previously: PR #84 — #39 attendance capture. Fifth post-#43 nice-to-have. Per-employee daily attendance with four capture methods (self / QR / biometric file import / geofence+photo / manual muster) folded onto one record per `(employee, attendance_date)`. Migration `76-attendance.sql` creates four tables: `attendance_devices` (biometric / QR / manual registry with `column_template` JSONB remembering the last import's column → field mapping + `last_import_at`), `biometric_employee_map` ((device_id, biometric_employee_id) → employee_id; varchar device ID for alphanumeric schemes; partial unique on the triple), `attendance_records` (the event: `check_in_at` / `check_out_at` / `total_minutes` / `method` / `status` / `source_device_id` / `supervisor_user_id` / `location_lat,lng` / `has_conflict` + reason; partial unique `(tenant_id, employee_id, attendance_date)` WHERE `deleted_at IS NULL` — the dedup choke point; CHECK `check_out_at >= check_in_at`), `attendance_imports` (per-file audit: row counts + `errors` JSONB array of `{row, reason, biometricEmployeeId?}` + status). RLS on all four via `current_tenant_id()`. Multi-method conflict policy: a second write for an existing `(employee, date)` with a DIFFERENT method flips `has_conflict=true` with a reason string and merges (earliest-in / latest-out) — we surface the exception, we don't pick a winner. Same-method second write just merges. Widens `document_attachments` entity-type CHECK to add `'attendance_record'` so geofence group photos attach via the existing #32 infrastructure. Import endpoint is stateless JSON: the web wizard parses CSV client-side (simple split on comma, strip quotes), lets the user map columns → fields (biometric_id / punch_at / direction) with heuristic pre-fill + saved-template pre-fill, then POSTs `{attendance_device_id, file_name, column_template, rows:[…]}`. Server resolves each row via `biometric_employee_map`; unknown IDs log to errors + `rows_skipped` rather than failing the batch. On success the `column_template` saves back to the device row so the next file auto-maps. Self check-in / out works against today's record for the authenticated user's employee (falls back to `employee_id` body param under `attendance.operate`). Manual muster bulk-upserts `{attendance_date, employee_ids[], status?, notes?}` and returns per-employee `{created|updated|conflict}`. Exceptions endpoint surfaces records where `has_conflict=true` OR (`check_in_at IS NOT NULL AND check_out_at IS NULL AND attendance_date < today`) — a missing clock-out is an exception by default. Permission keys `attendance.operate` (write) + `attendance.view` (read) seeded onto Owner / Admin / Accountant via `seed_admin_role_templates_for_tenant` CREATE OR REPLACE + idempotent backfill for existing system rows. Web: list at `/app/attendance` with default last-7-days window + filters + exceptions banner, detail page with edit form + `<AttachmentsPanel>`, device registry at `/app/attendance/devices` with inline biometric-mapping modal, CSV import wizard at `/app/attendance/import` (3 steps — pick device + upload, map columns, preview + commit — with a success card listing per-row errors). XLSX parsing, live device API polling, and payroll wire-up (converting `total_minutes` into wages) all intentionally deferred (per spec §5.4 Phase 2 + the separate payroll-integration follow-up). New fragile area: the `(employee, date)` dedup key is the only thing stopping multi-method double-counting — losing that index would silently start duplicating days. Typecheck baselines held (api: 28, web: 10, db: 1). Previously: PR #83 — #38 petty cash float. Fourth post-#43 nice-to-have. Per-branch non-mandatory operational cash floats with denormalised `current_balance_cents` on the float row updated atomically alongside every ledger post via `SELECT ... FOR UPDATE`. Migration `75-petty-cash.sql` creates four tables: `petty_cash_floats` (header with `ceiling_cents`, `current_balance_cents`, `petty_cash_account_id` pointing at the shared `1005 Petty Cash` COA account, `status` active/closed, `float_holder_user_id`, partial unique `(branch_id)` WHERE `status='active'`), `petty_cash_transactions` (ledger with `txn_type` one of `expense` / `advance_out` / `advance_return` / `top_up` / `variance_short` / `variance_over` / `close_transfer`, `is_voided` + `reversed_by_transaction_id`, nullable `reconciliation_id` + `top_up_request_id` linkages forward-declared as plain UUID columns with FKs added via `ALTER TABLE ... ADD CONSTRAINT IF NOT EXISTS` after the dependent tables exist — breaks the schema cycle cleanly), `petty_cash_top_up_requests` (workflow `pending` → `approved` / `rejected` → `posted` / `cancelled` with `requested_by_user_id` / `approved_by_user_id` / `posted_by_user_id` + `amount_cents` + `source_account_id`), `petty_cash_reconciliations` (EOD snapshot: expected balance, counted balance, variance cents, variance JE handle, `reconciled_by_user_id`). All four tenant-scoped + RLS via `current_tenant_id()`. One shared `1005 Petty Cash` COA account seeded per tenant via idempotent INSERT ... WHERE NOT EXISTS — not per-float, not per-branch. (Note: `1005` instead of the common `1020` convention because `1020` is already seeded for "Bank — cheques in transit"; cosmetic divergence only.) Widens the `document_attachments` entity-type CHECK whitelist to add `'petty_cash_transaction'` so receipt photos can attach to individual ledger rows. Permission keys `petty_cash.operate` (holders + accountants, gates expense / advance / advance-return / top-up request / reconciliation) and `petty_cash.approve` (approvers only, gates top-up decide + post + close) seeded onto Owner/Admin/Accountant system-role templates via `seed_admin_role_templates_for_tenant` CREATE OR REPLACE + idempotent backfill for existing rows. New API module `modules/accounting/petty-cash.ts` mounted at `/petty-cash` with ~17 routes across floats / transactions / top-up-requests / reconciliations. All ledger posts go through the existing `postJournal` choke point (so period lock + balance check + audit hooks work for free). Helpers: `resolveCashOverShortAccount` looks up `code='5190'`, `resolvePettyCashAccount` looks up `code='1005'`, `getFloatForUpdate` does the raw `SELECT ... FOR UPDATE`, `bumpBalanceOrFail({enforceCeiling, allowNegative})` is the one place the float balance changes. Ceiling is enforced only at top-up post (not at request or approve) — approvals can park while a float is still above ceiling. `advance_return` allowed to temporarily exceed ceiling because that's the natural shape of a refund. Voids go through `postReversingJournal` which flips DR/CR on every line, rolls back the float balance, and — for voided top-ups — rolls the originating top-up-request from `posted` back to `approved` so it can be re-posted. Top-up SOD enforced at `/top-up-requests/:id/approve`: `rqst.requestedByUserId === ctx.userId` → 403 `SOD_VIOLATION`. Reconciliation computes expected balance from the window `(prev_recon_date, recon_date]` grouped by `txn_type`, compares to the user-entered counted cents, books delta to 5190 Cash Over/Short (DR when short, CR when over — mirrors POS shift variance pattern), and stamps the float balance to match the counted figure. Close-float transfers remaining balance out to a bank/cash account via a final `close_transfer` transaction and flips status to `closed`. 11 new audit-event kinds. Web: list at `/app/petty-cash` with active/closed sections + near-ceiling warning (>90%), open-float form at `/app/petty-cash/new` with optional seed top-up in the same transaction (filters cash/bank asset accounts excluding `1005` itself), detail page at `/app/petty-cash/[id]` with Ledger / Top-ups / Reconciliations tabs and inline modals for expense / advance-out / advance-return / top-up request / post top-up / reconcile / close, `<AttachmentsPanel entityType="petty_cash_transaction">` per ledger row. Sidebar entry added under Accounting. New fragile area: denormalised float balance atomicity (§3) — the `SELECT ... FOR UPDATE` guard is the only protection against double-spends. Typecheck baselines held (api: 28, web: 10, db: 1). Previously: PR #82 — #35 kit / bundle items. Third post-#43 nice-to-have. New `item_type='bundle'` variant — virtual SKUs that never carry stock themselves (`track_inventory` forced false at create/patch) and explode into per-component stock issues at invoice post time. Migration `74-item-bundle-components.sql` creates `item_bundle_components(id, tenant_id, bundle_item_id, component_item_id, quantity numeric(18,4), sort_order)` with `CHECK (component_item_id <> bundle_item_id)` as the self-reference guard, `ON DELETE CASCADE` from the bundle side, `ON DELETE RESTRICT` from the component side (so a component SKU can't be deleted out from under a live bundle), a `(bundle_item_id, component_item_id)` unique index, and RLS on `tenant_id`. The `items.item_type` CHECK already allowed `'bundle'` from migration 04 — re-asserted idempotently in the new migration to keep it self-contained. No nested bundles (component row rejected with `NESTED_BUNDLE` if the target itself has `item_type='bundle'`) and no self-reference (PUT component list rejects with `SELF_REFERENCE`). Empty component lists are allowed intentionally (bundle can be sold for zero-COGS revenue — hand-assembly placeholder) with a warning in the editor UI. New API surface on `modules/operations/items.ts`: GET `/:id` (inline components), PATCH `/:id` (type-change guard `BUNDLE_HAS_COMPONENTS` refuses product→bundle when existing component rows would leak; forces `trackInventory=false` on bundles), GET `/:id/components`, PUT `/:id/components` (replace-all transaction with dedup + validation — `COMPONENT_NOT_FOUND`, `DUPLICATE_COMPONENT`). Shared `loadBundleComponents` helper joins `items` for `componentName`/`componentSku` and filters soft-deleted components. Invoice posting (`modules/sell/invoice-posting.ts`): `BUNDLE_COMPONENT_MISSING` added to the error variants; a single pre-pass loads every referenced bundle's components via `inArray` (one round-trip regardless of invoice line count); tracked lines carry `issueQuantity` + optional `memoSuffix` so the `peekStockIssue`/`applyStockIssue` loop issues at `(lineQty × componentQty)` per component. `NEGATIVE_STOCK` now surfaces the component item id + name, not the bundle, so the AR clerk knows what to restock. Because POS's `modules/pos/sales.ts` delegates to `postDraftInvoice` (the shared helper), bundle support on POS receipts is free — no POS changes. Delivery notes (`modules/sell/delivery-notes.ts`, `stockRelieveOn='delivery_note'` path) mirror the same explosion logic. Credit notes intentionally DEFERRED to a follow-up PR: the existing credit-note module does NOT call `applyStockReceipt` at all today (i.e. no stock reversal of any kind), so this PR does not introduce a new reversal path that would be inconsistent with current behaviour — a code comment at the top of `modules/sell/credit-notes.ts` flags the future work. Web: items list now surfaces a Bundle badge in the Type column + links row name to `/app/items/:id`; item-create drawer gains a Components editor section when `itemType='bundle'` with client-side duplicate consolidation + empty-list warning; new item detail page `/app/items/:id` with stats strip + post-creation `BundleComponentsEditor` (replace-all save via `PUT /components`). `apps/web/lib/api.ts` extended with `BundleComponent` type, `UpdateItem` type, and methods `getItem`, `updateItem`, `listItemComponents`, `replaceItemComponents`. One new audit event: `item_bundle.updated`. New fragile area: bundle explosion at invoice post (§3) — the new branch runs on every invoice with a tracked line from now on, regression risk is high. Typecheck baselines held (api: 28, web: 10, db: 1). Previously: PR #81 — #32 document attachments. Second post-#43 nice-to-have. Generic cross-entity attachments backed by MinIO (self-hosted S3-compatible — already sitting in `docker-compose.yml` so no new infra to run). Migration `73-document-attachments.sql` creates `document_attachments` keyed by `(tenant_id, entity_type, entity_id)` with a CHECK whitelist of 13 entity types (invoice, sales_order, quotation, credit_note, bill, purchase_order, purchase_requisition, goods_received_note, expense_claim, payment, receipt, final_settlement, journal_entry) — adding a new attachable surface is an explicit migration + API validation edit, not ambient. Storage keys prefixed with the tenant UUID so tenant isolation survives a bucket-policy misconfig. Three-layer 10 MB cap: fastify-multipart `limits.fileSize`, DB CHECK `size_bytes <= 10485760`, module-level size guard. 7-year `retention_until` default (SL audit norm); soft-delete preserves the row for audit until retention expires. New API module `modules/platform/attachments.ts` with 5 routes (`POST` multipart, `GET` list by entity, `GET` download, `GET` preview with inline Content-Disposition, `DELETE` soft). New `apps/api/src/lib/object-storage.ts` wraps `@aws-sdk/client-s3` configured for MinIO at boot (`S3_ENDPOINT`, `S3_ACCESS_KEY`, `S3_SECRET_KEY`, `S3_BUCKET`, `S3_FORCE_PATH_STYLE=true`). `ensureBucket()` runs after server.register so dev workstations without the `minio` container still boot the API — the attachment endpoints 503 on use if S3 is unreachable. Web: reusable `<AttachmentsPanel entityType entityId />` client component with drag-drop upload + per-row download + inline-preview modal (iframe for PDF/image, new-tab for everything else) + soft-delete. Wired into invoice / bill / PO / PR detail pages as reference integrations; adding another detail page is a one-line import. Two new audit events: `attachment.uploaded` + `attachment.deleted`. New fragile area: MinIO reachability + retention-vs-hard-delete coupling (see §3). Typecheck baselines held (api: 28, web: 10, db: 1). Previously: PR #80 — #30 purchase requisition. First post-#43 nice-to-have to ship. Off-by-default tenant module under Settings → Modules (`tenant_settings.purchaseRequisitionsEnabled` — merged at read via `SETTINGS_DEFAULTS`). Migration `72-purchase-requisitions.sql` creates two new tables — `purchase_requisitions` (header, status: `draft`/`pending_approval`/`approved`/`rejected`/`converted`/`cancelled`, `approval_request_id` nullable FK to `approval_requests`, `converted_po_id` nullable FK to `purchase_orders`) and `purchase_requisition_lines` (per-line `line_status`: `pending`/`approved`/`rejected` so partial approval lands granularly). RLS policies on both. Adds `purchase_orders.source_pr_id` (no inline Drizzle `.references()` — avoids a circular type import since `purchase_requisitions.ts` already imports `purchaseOrders`; the FK is declared in SQL) and `purchase_order_lines.source_pr_line_id` (intentionally no FK — line-level linkage is advisory). Document sequence kind `purchase_requisition` seeded per tenant with prefix `PR` / scope `year` / pad 4 / display_name 'Purchase requisition'. New permission key `purchase_requisitions.manage` (coarse-grained, matches `bills.post`/`payroll.manage` precedent — finer `create`/`submit`/`approve`/`convert` would have invented a pattern unused elsewhere) seeded onto Owner/Admin/Accountant via `seed_admin_role_templates_for_tenant` CREATE OR REPLACE + a one-shot UPDATE backfilling the key onto existing system roles. New API surface `/purchase-requisitions` with 9 routes (list, get, create, update, submit, approve, reject, cancel, convert). Shared `approvePurchaseRequisitionCore(tx, {…, allowStatuses, rejectedLineIds?, lineRejectReasons?})` helper serves both the immediate path (`allowStatuses: ["draft"]`) and the engine finaliser (`["pending_approval"]`) — same atomic code with a race guard, mirroring the bills/POs/payroll/final-settlement pattern. `/purchase-requisitions/:id/approve` consults `resolveApplicablePolicy("purchase_requisition", estimatedTotalCents, submitter)`; matching policies snapshot steps into `approval_request_steps` and park the PR in `pending_approval` with the engine handle set. `/approvals/:id/approve` drives `finaliseApprovedDocument` → `approvePurchaseRequisitionCore` for the engine path. SOD enforced at both entry points: immediate path checks `submittedByUserId !== ctx.userId` directly; engine path goes through `recordDecision`'s universal `SELF_APPROVAL` check. Partial approval: per-line `lineStatus` drives header flip — any line remaining `approved` → header `approved`, else `rejected`. PATCH auto-resets `rejected` back to `draft` on edit so the submitter can iterate without going through cancel-and-re-create. Convert creates a draft PO (copies lines with `source_pr_line_id` linkage back to the originating PR line), stamps `converted_po_id` + `convertedAt` on the PR, and flips status to `converted`. `approvals/queue-client.tsx` knows the new `purchase_requisition` document type (`prettyDocType` label + `deepLinkFor` route to `/app/purchase-requisitions/:id`). Sidebar is tenant-gated: the Buy menu hides the Purchase requisitions entry entirely when the toggle is off, via new `SidebarFeatureFlags` prop on `<Sidebar>` — the app layout fetches `/settings` in parallel with `/auth/me` and threads the flag through. Web surface: list page + new page (with dynamic line editor + preferred-supplier + branch dropdowns + needed-by date) + detail page (status tone/label, action buttons for submit/approve/reject/cancel/convert, awaiting-approval banner when `pending_approval`, rejected/cancelled reason banners, per-line status column, linked-PO pointer when converted). Six new `AuditEventKind` variants (`purchase_requisition.created/submitted/approved/rejected/cancelled/converted`). Typecheck baselines held (api: 28, web: 10, db: 1). Previously: PR #79 — #43e approval engine final settlement routing. Seventh and **final** domain wired to the engine shipped in PR #74 — the generic approval engine work (#43) is now fully complete. Migration `71-approval-engine-final-settlement.sql` adds nullable `final_settlements.approval_request_id` FK + partial index and widens the status CHECK to accept the new `pending_approval` parking state. Additive flow — settlements had a simple immediate `draft → approved` gate before. `POST /final-settlements/:id/approve` now consults `resolveApplicablePolicy("final_settlement", netPayableCents, submitter)` first; matching policies snapshot steps into `approval_requests` and park the settlement in `pending_approval` with the engine handle set. Tenants with no matching policy fall through to the pre-engine immediate path. Approve core extracted into `approveFinalSettlementCore(tx, {settlementId, approverUserId, allowStatuses})` in `hr/final-settlement.ts`; immediate path passes `allowStatuses: ["draft"]`, the engine finaliser passes `["pending_approval"]`, so both flows share one atomic code path with an explicit race guard. Approve flips status → `approved`, stamps `approvedAt`/`approvedByUserId`, clears `approvalRequestId`, and records the audit event — posting to the ledger stays as a separate `/post` step afterwards. Engine-owned settlements refuse `/approve` with 409 `ENGINE_OWNED`. Domain `/cancel` now accepts `pending_approval` in addition to `draft`/`approved` and cancels the underlying `approval_request` as part of terminating the settlement — the settlement is the domain-of-record. Reject + cancel from `/approvals/:id/*` flip the settlement back to `draft`, clear `approval_request_id`, and append the reason (as `[Rejected]` or `[Withdrawn]`) to `notes` — same pattern as bills/POs/payroll/bonus. Approval-policy editor widens `documentType` to include `final_settlement`. `deepLinkFor` routes `final_settlement` → `/app/final-settlements/:id`; the settlement detail page surfaces an "Awaiting approval" banner when `status='pending_approval'`. Status tone maps + list + detail pages updated across web. Spec coverage: tenant-admin §7.1 "always-approve for final settlements" = policy with empty triggerRule (matches every submission). Typecheck baselines held (api: 28, web: 10, db: 1). Previously: PR #78 — #43d approval engine payroll + bonus run routing. Fifth and sixth domains wired to the engine shipped in PR #74. Migration `70-approval-engine-payroll-bonus.sql` adds nullable `approval_request_id` FK + partial index to both `payroll_runs` and `bonus_runs` and widens their status CHECKs to accept the new `pending_approval` parking state. Additive flow — neither had any prior approval gate. `POST /payroll-runs/:id/post` and `POST /bonus-runs/:id/post` now consult `resolveApplicablePolicy("payroll_run"|"bonus_run", grossCents, submitter)` first; matching policies snapshot steps into `approval_requests` and park the run in `pending_approval`. Tenants with no matching policy fall through to the pre-engine immediate draft → posted path — both coexist. Post cores extracted into `postPayrollRunCore` (in `hr/payroll-runs.ts`) and `postBonusRunCore` (in `hr/bonuses.ts`); the immediate routes pass `allowStatuses: ["draft"]`, the engine finaliser passes `["pending_approval"]`, so the same code path serves both flows with an explicit race guard. `postPayrollRunCore` is idempotent on `runNumber`: if the finaliser crashed mid-way and a number was already allocated, it reuses it on retry. Engine-owned runs refuse `/post` with 409 `ENGINE_OWNED`. Reject + cancel from `/approvals/:id/*` flip the run back to `draft`, clear `approval_request_id`, and append the reason (as `[Rejected]` or `[Withdrawn]`) to `notes` — same pattern as bills/POs. Approval-policy editor widens `documentType` to include `payroll_run` + `bonus_run`. `deepLinkFor` routes `payroll_run` → `/app/payroll/:id` and `bonus_run` → `/app/bonus-runs/:id`; both detail pages surface an "Awaiting approval" banner when `status='pending_approval'`. Status tone maps + list + detail pages updated across web. Spec coverage: tenant-admin §7.1 "always → Owner" for payroll runs is satisfied by configuring a policy with empty triggerRule (matches every submission); bonus runs are threshold-gated via `minAmountCents`, same shape as bills. Typecheck baselines held (api: 28 observed, web: 10, db: 1 — api improved by one vs. the 29 baseline). Previously: PR #77 — #43c approval engine purchase order routing. Fourth domain wired to the engine shipped in PR #74. Migration `69-approval-engine-purchase-orders.sql` adds nullable `purchase_orders.approval_request_id` FK + partial index and widens `po_status_check` to accept the new `pending_approval` parking state. Additive flow — POs had no approval gate before. `POST /purchase-orders/:id/send` now consults `resolveApplicablePolicy("purchase_order", totalCents, submitter, flags)` first; the submit route pre-computes `isFirstPoFromSupplier` by counting existing non-cancelled/non-draft POs for the supplier. The approval engine gains a new `firstPoFromSupplier: boolean` trigger-rule key and a `TriggerFlags` input to `resolveApplicablePolicy`/`evaluateTrigger` — so admins can wire "first PO from a new supplier → Owner approval" without code changes. `sendPurchaseOrderCore` helper extracted in `buy/purchase-orders.ts` carries the "allocate po_number + flip to sent" transition; the immediate route passes `allowStatuses: ["draft"]`, the engine finaliser passes `["pending_approval"]`, so the same code path serves both flows with an explicit race guard. Engine-owned POs refuse `/send` (409 ENGINE_OWNED) and `/acknowledge` (409 ENGINE_OWNED). PO-level `/cancel` on a pending row cancels the underlying approval_request then marks the PO cancelled — PO is the domain-of-record. `/convert` refuses `draft` or `pending_approval` with NOT_ISSUED. Reject + cancel from `/approvals/:id/*` flip the PO back to `draft`, clear `approval_request_id`, and append the reason to `purchase_orders.notes` (same pattern as bills). Approvals-policy editor UI exposes the new `firstPoFromSupplier` checkbox only when `documentType='purchase_order'`. `deepLinkFor` routes `purchase_order` → `/app/purchase-orders/:id`; the PO detail page surfaces an "Awaiting approval" banner when `status='pending_approval'`. Previously: PR #76 — #43b approval engine bill routing. Third domain wired to the engine shipped in PR #74. Migration `68-approval-engine-bills.sql` adds nullable `bills.approval_request_id` FK + partial index. Additive flow — bills had no approval gate before. New `pending_approval` state sits between `draft` and `posted`; `POST /bills/:id/post` consults `resolveApplicablePolicy("bill", totalCents, submitter)` and, if a policy matches, snapshots steps into `approval_requests` + parks the bill as `pending_approval` with the `approval_request_id` set. Tenants with no matching policy fall through to the pre-engine immediate `draft → posted` path. Post core extracted into a shared `postBillCore(tx, {billId, allowStatuses, postedByUserId})` helper in `buy/bills.ts` so both paths run identical AP-posting + input-tax grouping + stock receipts + tenant bell notifications — zero duplication. `finaliseApprovedDocument` for `documentType='bill'` invokes `postBillCore` with `allowStatuses=["pending_approval"]`; BAD_STATUS and NOT_FOUND bail idempotently (covers void / race edge cases). Engine-owned bills refuse `/bills/:id/post` with 409 `ENGINE_OWNED`. Reject + cancel from `/approvals/:id/*` flip the bill back to `draft`, clear `approval_request_id`, and append the reason to `bills.notes` so the submitter can edit + re-post (no dedicated `rejected` state for bills — keeps the state machine minimal). `deepLinkFor` routes `bill` → `/app/bills/:id`; the bill detail page surfaces an "Awaiting approval" banner with a link to the approvals queue when `status='pending_approval'`. New fragile area: dual-path bill posting (§3) — the `postBillCore` helper's `allowStatuses` param is the only safety against double-posting. No new typecheck errors; baselines held (api: 29, web: 10, db: 1). Previously: PR #75 — #43a approval engine expense claim routing. Second domain wired to the engine shipped in PR #74. Migration `67-approval-engine-expense-claims.sql` adds nullable `expense_claims.approval_request_id` FK + partial index. `/expense-claims/:id/submit` consults `resolveApplicablePolicy("expense_claim", amount, submitter)` after allocating the claim number; if a policy matches, snapshots its steps into `approval_request_steps` and parks the claim with the request id. Legacy `/approve`, `/reject` routes refuse engine-owned claims with 409 `ENGINE_OWNED` (mirrors the JE pattern). `/approve-and-pay` gets a dual-state shape: legacy path still requires `status='submitted'`, engine path requires `status='approved'` (the engine already approved — this call now only executes payment). SOD kept in both paths — submitter can never be the payer. `finaliseApprovedDocument` flips submitted → approved for both disbursement methods (no JE posting in the approval branch: direct-pay posts via the subsequent `/approve-and-pay`, payroll-method drains via the next payroll run). Rejecting the approval flips `expense_claims.status = 'rejected'` + stamps `rejectedByUserId`; cancelling from the approvals queue also flips to rejected so the claim can be edited + re-submitted. Voiding an engine-owned claim cancels the open `approval_request` so the `/approvals` queue drops it. `/app/approvals` deep-link routes `expense_claim` → `/app/expense-claims/:id`. New fragile area: dual-path expense-claim submission (§3) — engine path + legacy submitter-≠-approver SOD coexist. Previously: PR #74 — #43 approval engine core + JE wiring. New runtime tables `approval_requests` + `approval_request_steps` (migration `66-approval-engine.sql`) drive a generic engine that snapshots `approval_policies.steps` on submit — in-flight requests stay bound to the rules in effect at submit time, even if the policy is later edited. Journal entries wired: `resolveApplicablePolicy("journal_entry", amount, submitter)` takes precedence in the POST handler; the legacy `journalApprovalThresholdCents` path is kept as a back-compat fallback for tenants who haven't designed a policy yet. New `/approvals` API surface (GET list with `mine` / `submitted_by_me` / `all` scopes, POST approve / reject / cancel); new `/app/approvals` web queue page. SOD enforced explicitly — `recordDecision` throws `SELF_APPROVAL` regardless of what the approver list says. New `approval.decide` permission key seeded on Owner/Admin/Accountant via idempotent backfill. Legacy `POST /journal-entries/drafts/:id/approve` refuses engine-owned drafts (409 ENGINE_OWNED) so decisions don't race. No new typecheck errors; baselines held (api: 29, web: 10, db: 1). Previously: PR #73 — #36 inventory category hierarchy. Unlimited-depth `item_categories` self-referential tree with DB trigger (`item_categories_prevent_cycle`) walking the parent chain up to 64 hops to block cycles + cross-tenant parents, sibling-uniqueness via partial unique index with `COALESCE(parent_id, zero-uuid)` sentinel, and `item_category_effective_defaults(uuid)` recursive CTE picking first non-null along the chain (child overrides parent). Defaults: valuation method / tax code / income / COGS / asset accounts / SKU prefix / reorder point; null = inherit. Soft-delete guarded by HAS_CHILDREN + HAS_ITEMS counts (items FK is ON DELETE SET NULL as a safety net if the hard-delete path ever fires). Web: tree view with expand/collapse, inline add-sub/edit/delete, category picker wired into item form. New fragile area: the cycle trigger (§3). No new typecheck errors; baselines held (api: 29, web: 10, db: 1). Previously: PR #72 — #31 customer portal hardening. No new known bugs; no shipped regressions. Typecheck baseline held (api: 29 observed, web: 10, db: 1). PR #71 adds a new `portal_otps` table with 5 SECURITY DEFINER helpers (`portal_find_customers_by_email`, `portal_mint_otp`, `portal_find_otp`, `portal_consume_otp`, `portal_resolve_session`), a portal session realm backed by Redis (`portal-session:` prefix, separate from admin `pp_session`), new `/portal/*` API routes (auth + invoices + statement + payments + recurring), and the `/portal/*` Next.js App Router surface (login → verify → invoices / statement / payments / recurring + invoice detail with PDF download). Modules touched in §5 below: **Customer portal (new)**.

---

## 1. Known bugs

Open bugs, grouped by module. **Severity**: `S1` = data loss or posting correctness, `S2` = workflow broken, `S3` = UX / cosmetic. Include a reproducer when you have one.

| # | Module | Severity | Summary | Reproducer | Opened |
|---|---|---|---|---|---|
| — | — | — | *No known open bugs.* | — | — |

> Adding one? Keep the reproducer concrete enough that a cold reader can hit it. Empty reproducer is fine if unknown — mark it "Unknown, reported by …".

---

## 2. Typecheck debt baseline (frozen 2026-04-22)

These errors existed *before* PR #44. Every one is known, intentional-ish, and not a blocker for shipping features. **Don't let the total grow.** New PRs should add zero new errors; fixing entries here is welcome but out-of-scope for feature PRs.

**Baseline total: 32 errors** — api: 21, web: 10, db: 1. PR #47 executed both queued sweeps: the `pdfResponse()` helper collapses 9 PDF-route `Buffer → BodyInit` errors into one typed boundary; the `nextDocumentNumber()` helper (now in `@pettahpro/db`) removes 6 destructure-pattern errors. Net −15 errors from the baseline (47 → 32). Remaining api errors are all `noUncheckedIndexedAccess` array-index access inside bills / invoices / cheques / payroll / salary-components / statutory and the dashboard aging-label union mismatch; remaining web errors are marketing tuple drift + one `TaxCode.taxType` field rename + the reveal.tsx TS2590.

### `packages/db` (1)

| File:Line | Error | Why we tolerate |
|---|---|---|
| `drizzle.config.ts` (top) | TS6059 — outside `rootDir` | `drizzle.config.ts` is at package root but tsconfig rootDir is `src/`. Fix = `exclude` it from the build tsconfig or move it. Low value; fix when adjusting tsconfig next. |

### `apps/api` (21)

All `TS2339: Property 'X' does not exist on type '{…} | undefined'` and `TS2538: Type 'undefined' cannot be used as an index type` errors stem from the same root cause: **destructuring `await tx.execute(sql\`…\`)` results without guarding `undefined`** (under `noUncheckedIndexedAccess`). The fix pattern (already applied in `stock-transfers.ts`):

```ts
const rows = (await tx.execute(sql`…`)) as unknown as Array<{ … }>;
const first = rows[0];
if (!first) throw new Error("…");
```

| File:Line | Error | Notes |
|---|---|---|
| `modules/buy/bills.ts:506,601,602` | TS2538 × 3 | Array index access without guard |
| `modules/cheques/routes.ts:225,453` | TS2538 × 2 | Array index access without guard |
| `modules/hr/payroll-runs.ts:1166,1302,1303` | TS2538 × 3 | Array index access without guard |
| `modules/hr/salary-components.ts:170,213,370` | TS2538 × 3 | Array index access without guard |
| `modules/hr/statutory.ts:162,165` | TS2538 × 2 | Array index access without guard |
| `modules/reports/dashboard.ts:117,124,168,175` | TS2322 + TS2345 × 4 | `AgingBucket['label']` is a string literal union, dashboard uses generic `string` — fix is to narrow the computed label to the union or widen `AgingBucket` |
| `modules/sell/invoices.ts:742` | TS2322 `string \| undefined` → `string` | Missing null coalesce before assignment |
| `modules/sell/invoices.ts:761,859,860` | TS2538 × 3 | Array index access without guard |

### `apps/web` (10)

| File:Line | Error | Notes |
|---|---|---|
| `app/app/bills/[id]/page.tsx:36` | TS2339 `taxType` on `TaxCode` | Old field name; schema renamed. Trivial rename. |
| `components/features.tsx:28,30` | TS2339 `badge` on tuple union | Marketing site tuple type is too narrow. |
| `components/migration.tsx:34,39,58` | TS2339 `highlight` on tuple union | Same root cause as above (readonly tuples + optional member). |
| `components/pricing.tsx:56,61,86` | TS2339 `highlight` on tuple union | Same root cause. |
| `components/reveal.tsx:45` | TS2590 union too complex | `Motion` component type inference blowup. |

### Recommended debt-paydown sweeps (when we get to them)

1. ~~**`next_document_number` helper**~~ — **Done in PR #47**. Helper lives in `packages/db/src/next-document-number.ts` and is re-exported from `@pettahpro/db`. All 6 broken destructure call sites migrated.
2. ~~**PDF route `Buffer → BodyInit`**~~ — **Done in PR #47**. Helper lives in `apps/web/lib/pdf-response.ts`. All 9 PDF routes now call `pdfResponse(pdf, filename)` instead of hand-rolling the Response.
3. **Remaining `noUncheckedIndexedAccess` array-index leaks in `apps/api`** — bills / invoices / cheques / payroll / salary-components / statutory all still have 1–3 sites apiece. Same pattern: `const row = rows[i]; if (!row) throw …`. Mechanical sweep; ~15 minutes.
4. **Reports dashboard aging bucket labels** — widen the source or narrow via `as const` at the call site.
5. **Marketing tuple types** (`components/features.tsx`, `migration.tsx`, `pricing.tsx`) — add optional fields to the declared tuple type or use `satisfies`.

---

## 3. Fragile areas

Modules where a past change surprised us. Touch with care and re-test the listed surface.

| Module | What's fragile | What to re-test when touching |
|---|---|---|
| `postJournal` choke point (`apps/api/src/modules/accounting/journal-posting.ts`) | Every new module that posts accounting entries routes through here. Period-lock, approval-threshold, and balance-check are all enforced at this single point. A bad refactor here breaks every post-invoice / post-bill / payroll / bonus flow. | Post: invoice, bill, customer payment, supplier payment, payroll, bonus run, manual JE under + over approval threshold, JE into a soft-closed period. |
| `next_document_number(kind)` sequence helper | ~~Used by invoices, bills, delivery notes, POs, GRNs, payroll runs, stock transfers, bonus runs, journals.~~ **Now funnelled through `nextDocumentNumber(tx, kind)` in `@pettahpro/db`** (PR #47). 9 call sites still use the hand-rolled guard-and-return-error pattern because they're inside discriminated-union return shapes; those still need to return `{ error: "NUMBER_ALLOC_FAILED" }` rather than throw. Helper is the canonical shape for any new doc type. | Any new doc-type allocation: use `await nextDocumentNumber(tx, 'kind')`. |
| PDF routes (9 of them) | ~~All share the same `renderToBuffer` → `new Response(pdf)` pattern~~ **Now funnelled through `pdfResponse(pdf, filename)` in `apps/web/lib/pdf-response.ts`** (PR #47). Single place to change response headers, `Cache-Control`, `Content-Disposition`, or the Buffer-to-BodyInit boundary. | Any new PDF route: use `pdfResponse()`, don't hand-roll. |
| WAVG propagation on stock movements | `quantity_on_hand`, `total_value_cents`, `average_cost_cents`, `last_movement_at` on `item_balances` + matching `stock_ledger` row must move together. Any path that mutates one without the others silently breaks WAVG. | Bill post, invoice post, delivery-note deliver (if stockRelieveOn=deliver), credit-note post (reversal), stock-transfer dispatch + receive, future: stock count adjustments. |
| Multi-tenant via `withTenant` + `current_tenant_id()` | Every raw SQL query (`tx.execute(sql\`\`)`) must include `WHERE tenant_id = current_tenant_id()`. Forgetting this leaks across tenants. Drizzle ORM queries inherit the RLS context automatically; raw SQL does not. Since PR #48 the app connects as `pettahpro_app` (NOBYPASSRLS), so a missing tenant predicate on an RLS table will now silently return 0 rows instead of leaking — fail-closed but still a bug. | Search for new `tx.execute(sql\`` additions and verify tenant predicate. |
| Cross-tenant auth lookups | Signup/login/me run before we know the tenant, so they call SECURITY DEFINER helpers (`auth_find_user_by_email`, `auth_find_user_by_id`, `auth_email_in_use`, `auth_touch_last_login`) in `docker/postgres/init/44-auth-helpers.sql`. These functions are owned by the superuser and bypass RLS. Adding a new auth-time cross-tenant read means adding a new helper there — don't widen any existing one to return more columns. | Run signup → login → logout → login → /me end-to-end. Verify `Set-Cookie` fires, /me returns the right user/tenant pair. |
| Cheque lifecycle (9 states) | Every state transition has a reverse path; transitions are gated by current state + what document linked the cheque. | When adding a cheque-using module, run through: issued → handed → deposited → presented → cleared, and separately: bounced, stopped, cancelled, stale. |
| Payroll compute engine (`sl-tax`) | Used by regular payroll runs + bonus runs. Any change to EPF/ETF/PAYE math affects both. | Run a regular payroll + a bonus run (both flat_amount and percent_of_basic) and spot-check EPF/ETF/PAYE deductions. |
| Period lock on back-dated transactions | Soft-closed = warn, closed = block. Enforced in `postJournal`. Salary revisions also enforce against revision effective dates. | Try posting a JE into a closed period, try a salary revision effective in a closed period. |
| Notification digest cron (`apps/api/src/modules/notifications/digest-cron.ts`) | Hourly job that fires against every tenant at their local digest hour (`tenantLocalNow` uses `Intl.DateTimeFormat` with `tenants.timezone`). Dedupe is a lookback over `notification_digest_emails` — two ticks in the same tenant-local hour won't double-send, but a regression in the dedupe query or the tenant-local hour math will silently spam every user. All per-tenant reads are inside `withTenant()` because the queue + emails tables are RLS-gated; cross-tenant reads without that context return zero rows (fail-closed, but still a bug). | Seed a queue row (digest cadence), force the cron to fire with a mocked "now" matching the tenant's digest hour, assert one email + one log row + delivered_at stamped. Re-fire the cron seconds later — assert no duplicate send (dedupe window). Test in a non-Colombo timezone if you change `tenantLocalNow`. |
| Item category cycle trigger (`docker/postgres/init/65-item-categories.sql` — `item_categories_prevent_cycle()`) | BEFORE INSERT OR UPDATE OF parent_id on `item_categories`. Walks the parent chain up to 64 hops checking NEW.id doesn't reappear (cycle) and that every ancestor shares NEW.tenant_id (cross-tenant parent). Also the sole guard against orphaned-by-cycle subtrees. The 64-hop cap doubles as a max-depth enforcement — raise with care, recursion here runs on every mutation. Any schema change that adds a new parent column or bypasses `parent_id` updates skips this trigger. | After touching the trigger: create a 5-deep chain, then try to reparent the root under the leaf → must raise "cycle"; create two tenants with categories, try to reparent across tenants → must raise "different tenant". Re-run the migration idempotently. |
| Approval engine snapshot + SOD (`apps/api/src/modules/admin/approval-engine.ts`) | `createApprovalRequest` clones `approval_policies.steps` into `approval_request_steps` at submit time — if the policy is later edited to relax approvers, in-flight requests MUST stay bound to the original rules. Any refactor that switches to a live policy lookup at decision time silently breaks the audit property. SOD is enforced in `recordDecision` (throws `SELF_APPROVAL` if decider === submitter) regardless of what the approver list says — never loosen that check even when the approver list looks "safe". Partial unique index `approval_requests_open_document_unique` keeps one pending request per document at a time; dropping it allows double-submits with divergent snapshots. | Submit a JE with a 2-step policy, edit the policy to a different approver list mid-flight, decide step 1 — must use the original approver. Add the submitter to the approver list of step 2, try to self-approve — must 403 `SELF_APPROVAL`. Submit, then re-submit the same draft before decision — must collide on the partial unique index. |
| Dual-path JE submission (`apps/api/src/modules/accounting/journal-entries.ts`) | Engine path + legacy flat-threshold path coexist until every tenant has designed a `journal_entry` policy. Submit handler tries `resolveApplicablePolicy` first; falls back to `journalApprovalThresholdCents` if no policy matches. Legacy `/drafts/:id/approve` + `/drafts/:id/reject` refuse engine-owned drafts (`ENGINE_OWNED` 409) so decisions don't race between the two code paths. Removing the fallback before tenants migrate breaks existing `pending_approval` drafts; removing the `ENGINE_OWNED` guard allows a draft to be posted twice. | With no `journal_entry` policy configured, post a manual JE above threshold — must land in the legacy draft bucket (`approval_request_id IS NULL`). With a policy, the same submission must produce an `approval_request` and set `approvalRequestId` on the draft. Try to approve the engine-owned draft via `POST /journal-entries/drafts/:id/approve` — must 409. |
| Dual-path bill posting (`apps/api/src/modules/buy/bills.ts`) | `POST /bills/:id/post` either parks a bill as `pending_approval` (policy match) or runs the full post pipeline immediately (no policy). Both paths land in the shared `postBillCore(tx, {billId, allowStatuses, postedByUserId})` helper — the `allowStatuses` param is the ONLY guard against double-posting: the immediate path passes `["draft"]`, the engine finaliser passes `["pending_approval"]`. Removing or loosening it (e.g. `["draft", "pending_approval"]` for both) allows a race to post the same bill twice. Engine-owned bills (approval_request_id set) refuse `/post` with 409 `ENGINE_OWNED`. Reject + cancel flip back to `draft` and clear approval_request_id — editing the bill is then free until the submitter calls `/post` again. | With no `document_type='bill'` policy: post a draft bill → `status='posted'`, `journalEntryId` set, `approvalRequestId IS NULL`. With a policy configured: post a draft → `status='pending_approval'`, `approvalRequestId` set, no JE yet. Decide via `/approvals/:id/approve` → `postBillCore` runs, `status='posted'`, JE posted. Try `POST /bills/:id/post` on an engine-owned bill → 409. Reject from approvals queue → bill flips to `draft`, approval_request_id cleared, notes appended with `[Rejected] <reason>`. Voiding a posted bill still works (untouched path). |
| Dual-path PO issuance (`apps/api/src/modules/buy/purchase-orders.ts`) | `POST /purchase-orders/:id/send` either parks in `pending_approval` (policy match) or allocates a po_number + flips to `sent` immediately (no policy). Both paths call `sendPurchaseOrderCore(tx, {purchaseOrderId, allowStatuses})` — the immediate route passes `["draft"]`, the engine finaliser passes `["pending_approval"]`. Loosening the gate allows a race to issue the same PO twice, burning two numbers from the sequence. Helper is idempotent on re-entry: if `po_number` is already set (crash recovery) it reuses the existing number instead of allocating a new one. Engine-owned POs refuse `/send` and `/acknowledge` with 409 `ENGINE_OWNED`. PO-level `/cancel` on a pending row cancels the underlying approval_request first, then marks the PO cancelled — PO is domain-of-record and its lifecycle wins. `/convert` refuses `draft` and `pending_approval` with NOT_ISSUED so only issued POs can become bills. The new `firstPoFromSupplier` trigger rule depends on a pre-computed flag (`isFirstPoFromSupplier`) from the submit route counting non-cancelled/non-draft rows — changing that query's status filter silently changes who gets routed through which policy. | With no `document_type='purchase_order'` policy: send a draft PO → `status='sent'`, po_number allocated, `approvalRequestId IS NULL`. With a matching policy (amount threshold or `firstPoFromSupplier`): send a draft → `status='pending_approval'`, `approvalRequestId` set, no po_number yet. Approve via `/approvals/:id/approve` → `sendPurchaseOrderCore` runs, po_number allocated, `status='sent'`. Try `/send` on a pending PO → 409 ENGINE_OWNED. Reject → PO flips to `draft`, `approvalRequestId` cleared, notes gain `[Rejected] <reason>`. PO cancel on a pending row → approval_request gets status='cancelled' and PO gets status='cancelled'. Set a `firstPoFromSupplier=true` policy, submit a PO to a fresh supplier → parks; submit another to the same supplier → goes through immediately. |
| Dual-path payroll + bonus run posting (`apps/api/src/modules/hr/payroll-runs.ts`, `apps/api/src/modules/hr/bonuses.ts`) | `POST /payroll-runs/:id/post` and `POST /bonus-runs/:id/post` either park in `pending_approval` (policy match) or run the full post pipeline immediately (no policy). Both paths land in the shared `postPayrollRunCore` / `postBonusRunCore` helpers — `allowStatuses` is the ONLY guard against double-posting (immediate path passes `["draft"]`, engine finaliser passes `["pending_approval"]`). Loosening it allows a race to post twice and double-book the JE + double-allocate the run number. `postPayrollRunCore` is idempotent on `runNumber`: if the finaliser crashed mid-way and a number was already allocated, it reuses it instead of burning a second one. Engine-owned runs refuse `/post` with 409 `ENGINE_OWNED`. Reject + cancel flip back to `draft` and clear `approvalRequestId`, stamping `[Rejected]` / `[Withdrawn]` into notes. Payroll runs are conventionally "always → Owner" (spec §7.1) via an empty-triggerRule policy; bonus runs are threshold-gated via `minAmountCents`. | With no payroll/bonus policy: post a draft run → `status='posted'`, JE linked, `approvalRequestId IS NULL`. With a policy: post a draft → `status='pending_approval'`, `approvalRequestId` set, no JE yet. Decide via `/approvals/:id/approve` → `post*Core` runs, `status='posted'`, JE posted. Try `/post` on an engine-owned run → 409. Reject → run flips to `draft`, `approvalRequestId` cleared, notes appended with `[Rejected] <reason>`. Crash-recover test: inject a throw after `runNumber` allocation inside `postPayrollRunCore`, retry finaliser → same number reused, no duplicate JE. |
| Dual-path final settlement approval (`apps/api/src/modules/hr/final-settlement.ts`) | `POST /final-settlements/:id/approve` either parks in `pending_approval` (policy match) or flips `draft → approved` immediately (no policy). Both paths land in the shared `approveFinalSettlementCore(tx, {settlementId, approverUserId, allowStatuses})` — immediate passes `["draft"]`, engine finaliser passes `["pending_approval"]`. The helper's `allowStatuses` race guard is the ONLY protection against two concurrent approvers flipping the same settlement. Posting to the ledger stays as a separate `/post` step afterwards — the approve core only changes status + stamps approved metadata + clears `approvalRequestId`. Engine-owned settlements refuse `/approve` with 409 `ENGINE_OWNED`. Domain `/cancel` now accepts `pending_approval` and cancels the underlying `approval_request` — the settlement is domain-of-record. Reject + cancel from the approvals queue flip back to `draft` + clear `approvalRequestId` + stamp `[Rejected]` / `[Withdrawn]` into notes. Tenant-admin §7.1 "always-approve" = empty triggerRule policy. | With no `final_settlement` policy: approve a draft → `status='approved'`, `approvedAt` + `approvedByUserId` set, `approvalRequestId IS NULL`. With a policy: approve a draft → `status='pending_approval'`, `approvalRequestId` set, no approved metadata yet. Decide via `/approvals/:id/approve` → `approveFinalSettlementCore` runs, `status='approved'`, approved metadata stamped, `approvalRequestId` cleared. Try `/approve` on an engine-owned settlement → 409 `ENGINE_OWNED`. Reject from approvals queue → settlement flips to `draft`, `approvalRequestId` cleared, notes gain `[Rejected] <reason>`. Cancel a `pending_approval` settlement via domain `/cancel` → approval_request gets status='cancelled' and settlement gets status='cancelled'. Submitter ≠ approver SOD still enforced via `recordDecision`'s `SELF_APPROVAL` check. |
| Document attachment storage (`apps/api/src/lib/object-storage.ts` + `modules/platform/attachments.ts`) | The S3 client is the only module that talks to MinIO. `ensureBucket()` runs at API boot and logs a warning on failure rather than crashing — attachment endpoints will 503 until MinIO is reachable, so a stopped `minio` container in docker-compose is a silent partial outage (everything else works, attachments silently fail). Storage keys are `<tenant_id>/<entity_type>/<entity_id>/<uuid>-<sanitized_filename>` — the tenant UUID prefix is the belt-and-braces guard on top of RLS: if a bucket policy is ever misconfigured, cross-tenant reads still fail because the DB row with the storage_key lives behind tenant RLS. The 10 MB cap is enforced three ways (fastify-multipart limits, DB CHECK, module-level size check) — removing any one layer degrades one failure mode. Soft-delete (`deleted_at`) preserves rows until `retention_until` (default 7 years) — a future GC job should hard-delete rows where `deleted_at IS NOT NULL AND retention_until < now()` and only then remove the S3 object. Until that GC lands, soft-deleted objects linger in S3 indefinitely — intentional (cheap, audit-safe), but eats disk. Entity-type whitelist lives in both the migration CHECK and the module's Zod schema — both must be updated when adding a new attachable surface. | Upload a file larger than 10 MB → fastify-multipart rejects at 10485761 bytes (fastest guard). Upload with `size_bytes` manipulated past the cap somehow → DB CHECK rejects. Upload to an unknown `entity_type` → Zod validation rejects. Upload + soft-delete → `deleted_at` stamped, GET list excludes it, object still exists in S3. Stop the `minio` container + try to upload → 503 (not 500). Restart MinIO → `ensureBucket()` is NOT automatically re-run, but uploads start working again if the bucket already exists (the adapter lazily handles bucket-not-found). Try to GET a preview of an attachment owned by a different tenant (same user session) → 404 (tenant RLS). Extending the entity-type whitelist: update both the migration CHECK and the Zod union or the API will 400 before the DB gets a chance to reject. |
| Bundle explosion at invoice post (`apps/api/src/modules/sell/invoice-posting.ts`, `modules/sell/delivery-notes.ts`) | The new `item_type='bundle'` branch runs on every invoice with a tracked line from now on — a bad refactor here silently breaks every POST of an invoice that contains a stocked product (bundles or not). A single pre-pass loads all referenced bundle components in one `inArray` round-trip and builds a `trackedLines[]` entry per physical component with `issueQuantity = lineQty × componentQty` and a `memoSuffix` pointing back to the bundle. The `peekStockIssue` / `applyStockIssue` loop then issues at the component id using `issueQuantity`, NOT the original line quantity. If anyone "simplifies" the loop back to `peekStockIssue(itemId, lineQty)` it double-breaks: regular products post wrong quantities, and bundles no-op because the bundle item id isn't a stocked row. `NEGATIVE_STOCK` surfaces the component item id + name (not the bundle) — swapping that back to the line's bundle id hides the real shortfall from the AR clerk. Component item lookup filters soft-deleted components; a bundle containing an archived component will throw `BUNDLE_COMPONENT_MISSING` at post time. Delivery notes (`stockRelieveOn='delivery_note'`) mirror the exact same explosion logic — the two sites must stay in sync. POS (`modules/pos/sales.ts`) inherits the behaviour for free via `postDraftInvoice`; re-introducing a separate POS post path would silently skip bundle explosion. Credit notes intentionally don't touch stock today (by design — flagged in the module comment); adding reversal there MUST mirror the same `inArray` pre-load + per-component explosion or WAVG drifts. | Sell a bundle with three components (two products + one service) on an invoice — assert one stock_ledger ISSUE per stocked component at `(lineQty × componentQty)`, and assert the invoice-line → journal-line mapping still renders one line per bundle (no sub-line explosion on the PDF). Sell a bundle with an archived component → `BUNDLE_COMPONENT_MISSING`. Sell an invoice with ONE stocked non-bundle line + ONE bundle line → assert both paths work in the same post. Sell a bundle when a component is short → `NEGATIVE_STOCK` surfaces the component id + name, not the bundle. POS: ring the same bundle through a POS sale → same ledger movements. Delivery-note delivery path (set a line's parent item to `stockRelieveOn='delivery_note'`, then deliver the DN) → bundle explosion still fires. Credit-note post → no stock_ledger rows created (unchanged behaviour). Repeat everything with an empty-components bundle → invoice posts successfully, zero ledger rows, revenue booked at full sell price. |
| Dual-path purchase-requisition approval (`apps/api/src/modules/buy/purchase-requisitions.ts`) | `POST /purchase-requisitions/:id/approve` either parks in `pending_approval` (policy match) or flips `draft → approved` / `rejected` immediately (no policy). Both paths land in the shared `approvePurchaseRequisitionCore(tx, {…, allowStatuses, rejectedLineIds?, lineRejectReasons?})` — immediate passes `["draft"]`, engine finaliser passes `["pending_approval"]`. The helper's `allowStatuses` race guard is the ONLY protection against two concurrent approvers acting on the same PR. Partial approval: the helper derives header status from per-line `line_status` after applying the rejection list — any remaining `approved` line → header `approved`, else `rejected`. Changing the header-derivation rule or loosening the guard silently breaks either partial-approval correctness or double-approval protection. Engine-owned PRs refuse `/approve` with 409 `ENGINE_OWNED`. Domain `/cancel` accepts `pending_approval` and cancels the underlying `approval_request` — the PR is domain-of-record. Reject from the approvals queue flips the PR to `rejected`, stamps `rejectedAt`/`by`/`reason`, marks every pending line `rejected`, and clears `approval_request_id`. Cancel from the approvals queue flips back to `draft` + stamps `[Withdrawn] <reason>` into notes so the submitter can re-submit. Convert on an `approved` PR creates a draft PO with `source_pr_id` set and copies lines with `source_pr_line_id` — no FK on `source_pr_line_id` (advisory only, avoids cascade complexity). PR number allocated at submit time (not create) so drafts don't burn numbers. Feature toggle `tenant_settings.purchaseRequisitionsEnabled` is the outermost guard — every mutating route short-circuits with 403 `FEATURE_DISABLED` when off. | With the toggle off: any `/purchase-requisitions/*` mutating call → 403 `FEATURE_DISABLED`. With the toggle on + no policy: submit draft → `status='pending_approval'` briefly? No — direct path flips `draft → approved`. Approve a draft → `status='approved'`, `approvedAt`/`approvedByUserId` set, `approvalRequestId IS NULL`. With a policy: approve a draft → `status='pending_approval'`, `approvalRequestId` set, no approved metadata yet. Decide via `/approvals/:id/approve` → `approvePurchaseRequisitionCore` runs with `allowStatuses=["pending_approval"]`, `status='approved'` (or `rejected` if all lines rejected), metadata stamped, `approvalRequestId` cleared. Try `/approve` on an engine-owned PR → 409 `ENGINE_OWNED`. Partial approval test: submit a 3-line PR with policy → approve via queue with `rejectedLineIds=[line1.id]` and `lineRejectReasons={[line1.id]: "out of budget"}` → line 1 `line_status='rejected'` with reason, lines 2+3 `approved`, header `approved`. Reject all 3 lines → header flips to `rejected`. Convert an `approved` PR → `purchase_orders` row created with `source_pr_id` set, `source_pr_line_id` on each copied line, PR flips to `converted` with `converted_po_id` set. Toggle the tenant setting off while a PR is `pending_approval` → approvals queue still shows the request (engine is unaware of the feature flag) but any direct domain action on the PR itself is blocked. Submitter ≠ approver SOD enforced via direct check + `recordDecision`'s `SELF_APPROVAL`. |
| Petty cash float balance denormalisation (`apps/api/src/modules/accounting/petty-cash.ts`) | `petty_cash_floats.current_balance_cents` is a cached running total updated inside the same transaction as every ledger post. The `SELECT ... FOR UPDATE` raw SQL in `getFloatForUpdate` is the ONLY guard against two concurrent holders double-spending: two sessions reading a balance of 10000, both posting an expense of 8000, both succeeding to 2000 when the truth is -6000. Every mutating route (expense / advance-out / advance-return / top-up post / reconcile / close) must take this lock BEFORE computing the new balance and BEFORE calling `postJournal`, and must update `current_balance_cents` in the same transaction. `bumpBalanceOrFail({enforceCeiling, allowNegative})` centralises the balance-math + ceiling-check + negative-balance-check so every caller stays consistent; bypassing it (writing the balance inline) silently breaks the invariant. Ceiling is enforced only on top-up post (not on request or approve) — approvals can park while the float is over ceiling because of a pending refund. `advance_return` is allowed to temporarily push balance above ceiling. Voids go through `postReversingJournal` AND must subtract the original movement from `current_balance_cents` in the same tx — a void that posts the reversing JE without touching the cached balance causes the two to drift apart permanently. Voiding a `top_up` additionally rolls its `petty_cash_top_up_requests` row from `posted` → `approved` so it can be re-posted. Reconciliation snaps the balance to `counted_balance_cents` and posts a variance JE to 5190 Cash Over/Short; if the balance cache was already wrong going in, the variance just papers over the drift rather than surfacing the bug. The denormalisation is deliberate (cheap reads, needed for the dashboard and near-ceiling warning) but every new petty-cash code path must respect it. | Open a float with ceiling=10000, seed=5000. Two parallel expense posts of 3000 each — assert both succeed sequentially (one 5000→2000, one 2000→-1000 would fail `INSUFFICIENT_BALANCE`). Try a top-up that would push balance past ceiling → `CEILING_EXCEEDED`. Approve a top-up request on a float that's currently above ceiling (due to an advance_return) → must succeed (ceiling check only at post). Post an expense, then void it — assert `current_balance_cents` returns to the pre-void figure AND the JE is reversed AND an audit `petty_cash_transaction.voided` row exists. Void a posted top-up — assert `petty_cash_top_up_requests` row flips `posted` → `approved`. Reconcile a float with expected=10000 and counted=9800 — assert variance JE posts 200 DR to 5190 Cash Over/Short and `current_balance_cents` is stamped to 9800. Close-float with balance=2500 → `close_transfer` transaction zeroes the float balance and moves the cash to the chosen bank/cash account. Attempt to post any ledger entry against a closed float → `FLOAT_CLOSED`. |
| Dual-path expense-claim submission (`apps/api/src/modules/hr/expense-claims.ts`) | Engine + legacy submitter-≠-approver paths coexist per-tenant. Submit handler calls `resolveApplicablePolicy("expense_claim", …)` after allocating the claim number; matches produce an `approval_request` and set `approval_request_id`. Legacy `/:id/approve` and `/:id/reject` refuse engine-owned rows with 409 `ENGINE_OWNED`. `/:id/approve-and-pay` is dual-state: legacy needs `status='submitted'`, engine needs `status='approved'` (engine already approved — the call only executes payment). SOD (submitter ≠ actor) applies in both paths. Voiding an engine-owned claim must cancel the open approval_request so the `/approvals` queue drops it. Payroll-method engine-owned claims need `status='approved'` (not 'submitted') before the next payroll run picks them up — miss that and payroll silently ignores them. | With no policy configured: submit a claim → status='submitted', `approval_request_id IS NULL`; legacy `/approve-and-pay` path works as before. With a policy: submit → parked with `approval_request_id`; decide via `/approvals/:id/approve` → claim flips to 'approved'; call `/approve-and-pay` → post JE + flips to 'paid' without re-stamping `approvedAt` (engine's stamp preserved). Try legacy `/approve` on an engine-owned claim → 409 `ENGINE_OWNED`. Void an engine-owned pending claim → assert the `approval_request` row moves to `status='cancelled'`. Payroll-method engine-owned claim + payroll compute → picks it up at `status='approved'`. |
| Customer-portal auth realm (`apps/api/src/modules/portal/*` + `apps/web/app/portal/*`) | Two realms live on the same host: admin app at `/app/*` with `pp_session` cookie (path=`/`), customer portal at `/portal/*` with `pp_portal_session` cookie (path=`/portal`). They must never cross-contaminate. The portal plugin's onRequest hook early-returns on non-`/portal` URLs so admin requests never see a portal session; the admin auth hook only reads `pp_session`. Redis keys use different prefixes (`pp-session:` vs `portal-session:`). Pre-auth cross-tenant lookups go through SECURITY DEFINER helpers (`portal_find_customers_by_email`, `portal_mint_otp`, `portal_find_otp`, `portal_consume_otp`, `portal_resolve_session`) — don't widen any of these to return more columns than strictly needed. Every `/portal/*` data query runs inside `withTenant(session.tenantId)` AND carries an explicit `customer_id = session.customerId` predicate so even a dropped RLS context can't leak another customer's data. Drafts are gated at the route level (`/portal/invoices/:id` returns 404 on `status='draft'`). | Log into admin app, then into portal in the same browser; verify both sessions coexist without one overwriting the other. Deactivate a customer → portal `/portal/auth/me` returns 401 on next request. Mint an OTP for an email that exists at two tenants → verify returns 409 `AMBIGUOUS_TENANT` with both candidates. Try to hit `/portal/invoices/:id` with a draft id → 404. Try to hit another customer's invoice id (same tenant) → 404. |

---

## 4. Regression log

Bugs that shipped, were caught, and how. One-liner each. Teaches the project its own sore spots — consult before touching a module listed here.

| Date | PR | Module | What broke | How it was caught |
|---|---|---|---|---|
| — | — | — | *No shipped regressions logged yet.* | — |

> The point of this table isn't shame — it's so the next person touching that module knows where the landmines were.

---

## 5. Module health

Quick read on which corners are stale. `Tests`: "unit" = unit tests exist, "route" = API route-level tests exist, "—" = none. `Last touched` = most recent PR that substantively changed the module.

| Module | Last PR | Tests | Open issues | Notes |
|---|---|---|---|---|
| Auth / session | #1 | — | 0 | Foundational, hasn't needed change in ages — a good sign. |
| Customers | early | — | 0 | Stable. |
| Quotations | early | — | 0 | Stable. |
| Sales orders | early | — | 0 | Stable. |
| Delivery notes | early | — | 0 | Has `taxType` → new field name drift (see typecheck debt). |
| Invoices | early + #32 (period lock) + #35 (credit) + #36 (bad debt) + #69 (channel filter) | — | 5 typecheck | Heavy module. Any change risks post-flow. `?channel=web\|pos\|all` defaults to `web` so POS receipts don't flood AR. |
| POS terminal | #67 + #69 (permission keys) | — | 0 | Shift open / sale / shift close. `pos.operate` + `pos.close` permission keys. Uses shared `postDraftInvoice()` helper with rest of invoice post path. |
| Recurring invoices | mid | — | 0 | Hourly BullMQ cron. Has never misfired in dev but untested in load. |
| Credit notes | early + #46 (PDF) | — | 1 typecheck (new PDF route) | Stable. |
| Customer payments | early + #36 (bad debt reverse) | — | 1 typecheck | Stable. |
| Proforma invoices | #51 | — | 0 | Converts 1:1 to invoices, idempotent. PRO-series document numbers. |
| Customer statement email | #55 | — | 0 | On-demand + day-1 monthly cron. SMTP retries on transient failure. |
| Suppliers | early | — | 0 | Stable. |
| Document attachments | #81 | — | 0 | Roadmap #32. Generic cross-entity attachments backed by MinIO. `document_attachments` keyed by `(tenant_id, entity_type, entity_id)` with 13-entry CHECK whitelist. 10 MB per-file cap (three layers). 7-year retention default + soft-delete. Reusable `<AttachmentsPanel />` wired into invoice / bill / PO / PR detail pages. See fragile areas §3 (storage reachability + retention-vs-hard-delete). |
| Purchase requisitions | #80 | — | 0 | Roadmap #30. Off-by-default tenant module (`tenant_settings.purchaseRequisitionsEnabled`). Internal "request to buy" with per-line approval, routed through the generic approval engine when a `purchase_requisition` policy matches, otherwise immediate approve. Converts to a draft PO via `source_pr_id` linkage. Shared `approvePurchaseRequisitionCore` + `allowStatuses` race guard — see fragile areas §3. Single coarse permission `purchase_requisitions.manage` seeded onto Owner/Admin/Accountant. Sidebar entry gated by the tenant toggle. PR-series document numbers allocated at submit (not create). |
| Purchase orders | early + #77 (engine approval) + #80 (PR conversion source) | — | 0 | Stable. Engine approval routing shipped #43c: matching policies park sends in `pending_approval` + new `firstPoFromSupplier` trigger. PR #80 adds optional `source_pr_id` / `source_pr_line_id` columns populated when a PO is converted from an approved purchase requisition (#30) — both nullable, no change to any existing code path. See fragile areas §3 (dual-path PO issuance). |
| GRNs | early | — | 0 | Stable. |
| Bills | early + #45 (PDF) + #59 (landed cost) + #76 (engine approval) | — | 5 typecheck (4 existing + 1 new PDF route) | Feeds WAVG — fragile. See fragile areas §3. Landed-cost allocation folds freight / insurance / customs / clearing / loading / other charges into inventory unit cost at post time. PR #76 adds a `pending_approval` state between draft and posted; the post pipeline is now split into `postBillCore` (shared helper) and a thin route wrapper. Dual-path posting — see fragile areas §3. |
| Landed cost on bills | #59 | unit (allocateCharges pure fn) | 0 | Charges table + bill-level allocation method. Largest-remainder cents distribution. Fallback to `5130 Freight & handling` when no stocked lines. |
| Recurring bills | #51 | — | 0 | Same hourly cron as recurring invoices. Auto-post vs review-queue toggle. |
| Debit notes | early + #46 (PDF) | — | 1 typecheck (new PDF route) | Stable. |
| Supplier payments | early + #33 (WHT) | — | 1 typecheck | WHT integration here is non-obvious. |
| Items | early + #73 (category picker) + #82 (bundle variant) | — | 0 | Stable. Now linkable to a category via `items.category_id` (ON DELETE SET NULL). PR #82 adds the `item_type='bundle'` variant (virtual SKU, `track_inventory` forced false) with its own component-list editor in the drawer + detail page. See Bundle items row below. |
| Bundle items | #82 | — | 0 | Roadmap #35. `item_bundle_components(bundle_item_id, component_item_id, quantity)` with self-ref CHECK + `ON DELETE CASCADE` from bundle + `ON DELETE RESTRICT` from component. No nesting, no self-reference, empty lists permissive. PUT `/items/:id/components` replace-all transaction; PATCH `/items/:id` type-change guarded by `BUNDLE_HAS_COMPONENTS`. Invoice post explodes each bundle line into per-component stock issues at `(lineQty × componentQty)` via the same `peekStockIssue` / `applyStockIssue` primitives — one invoice line still renders as one PDF line (no sub-line disaggregation) but WAVG rolls across components correctly. POS inherits via `postDraftInvoice`. DN delivery path mirrors. Credit-note reversal intentionally deferred (existing module doesn't reverse stock at all — code comment flags the future PR). New audit event `item_bundle.updated`. See fragile areas §3 (Bundle explosion at invoice post). |
| Item categories | #73 | — | 0 | Unlimited-depth hierarchy. DB cycle trigger + same-tenant parent check — fragile. See §3. Inheritance via `item_category_effective_defaults()` recursive CTE. Soft-delete blocks on HAS_CHILDREN / HAS_ITEMS. |
| Stock on-hand / ledger | #43 (in-transit) | — | 0 | Just touched. |
| Stock transfers | #43 | — | 0 | Just shipped. |
| Stock counts | #50 | — | 0 | Blind count + tiered approval. Feeds WAVG on post. |
| Low-stock report | mid | — | 0 | Stable. |
| Chart of accounts | early | — | 0 | Stable. |
| Tax codes | early | — | 0 | Schema field rename drift (see `bills/[id]/page.tsx:36`). |
| Journal entries | early + #32 + #37 | — | 1 typecheck | Choke point. See fragile areas. |
| Petty cash | this PR | — | 0 | Roadmap #38. Per-branch non-mandatory operational cash floats. Four tables: `petty_cash_floats` / `petty_cash_transactions` / `petty_cash_top_up_requests` / `petty_cash_reconciliations`. Denormalised `current_balance_cents` on the float row updated atomically alongside every ledger post via `SELECT ... FOR UPDATE` — see fragile areas §3. One shared `1005 Petty Cash` COA account per tenant (not per-float). JE-per-txn through `postJournal`; voids via `postReversingJournal` roll back balance AND flip voided top-ups `posted` → `approved`. Top-up workflow `pending` → `approved` / `rejected` → `posted` / `cancelled` with SOD check (requester ≠ approver). Ceiling enforced only at top-up post. EOD reconciliation books variance to 5190 Cash Over/Short. Attachments via `document_attachments` (entity type `petty_cash_transaction`). Permissions `petty_cash.operate` + `petty_cash.approve` on Owner/Admin/Accountant. Close-float transfers remaining balance out via `close_transfer` txn. 11 audit-event kinds. Web: list / open / detail (Ledger / Top-ups / Reconciliations tabs + 7 modals). Sidebar entry under Accounting. |
| Fixed assets | mid + #69 (manual → daily cron) | — | 0 | `runMonthlyDepreciationForAllTenants` now dispatched by BullMQ scheduled worker on 24h interval; guards on `getUTCDate() !== 1` so it only does work on the 1st. Idempotent via `(tenant, year, month)` unique index. |
| Bank reconciliation | mid | — | 0 | CSV parsing has sharp edges per bank. |
| Cheques | mid | — | 2 typecheck | 9-state lifecycle — fragile. |
| Period lock | #32 | — | 0 | Enforced in one place. See fragile areas. |
| WHT | #33 | — | 0 | Supplier-payment integration is non-obvious. |
| Opening balance | #34 | — | 0 | One-shot, has guardrails. |
| Credit enforcement | #35 | — | 0 | Stable. |
| Bad debt write-off | #36 | — | 0 | VAT-relief math is load-bearing. |
| JE approval workflow | #37 + #74 (engine path added) | — | 0 | SOD enforced. PR #74: dual path — when a `journal_entry` approval policy is configured, submit goes through the generic engine (`approval_requests` + `/app/approvals` queue) and the legacy `/drafts/:id/approve` route refuses engine-owned drafts with `ENGINE_OWNED`. Tenants without a policy stay on the flat `journalApprovalThresholdCents` path unchanged. See fragile areas §3. |
| Approval engine (generic) | #74 + #75 (expense_claim) + #76 (bill) + #77 (purchase_order) + #78 (payroll_run + bonus_run) + #79 (final_settlement) + #80 (purchase_requisition) | — | 0 | Roadmap #43 + #43a + #43b + #43c + #43d + #43e complete — now gained one more domain via the first nice-to-have (#30 purchase requisitions) which reuses the engine rather than inventing its own approval flow. Consumes `approval_policies` (from #26), snapshots steps on submit into `approval_request_steps` so in-flight requests bind to the rules in effect at submit time. `/approvals` queue supports `mine` / `submitted_by_me` / `all` scopes + deep-links to every wired domain (JE drafts, expense claims, bills, POs, payroll runs, bonus runs, final settlements, purchase requisitions). `approval.decide` permission (Owner/Admin/Accountant). SOD is belt-and-braces: `recordDecision` throws `SELF_APPROVAL` regardless of approver list. Wired domains: `journal_entry` (#74), `expense_claim` (#75), `bill` (#76), `purchase_order` (#77), `payroll_run` + `bonus_run` (#78), `final_settlement` (#79), `purchase_requisition` (#80). `trigger_rule` supports `minAmountCents`, `submitters`, and `firstPoFromSupplier` (PO-only). See fragile areas §3. |
| Recurring journals | #52 | — | 0 | Hourly cron. Auto-post vs review-queue toggle. Variable-amount prompts per cycle. |
| Number series config | #53 | — | 0 | Tenant-level template override per doc kind. All allocations funnelled through `nextDocumentNumber()`. |
| Audit log viewer | #54 | — | 0 | `recordAuditEvent` never throws — a failed audit write doesn't break the primary action. |
| Employees | early + #41 (exit) | — | 0 | Stable. |
| Salary components | early | — | 3 typecheck | Sprouted debt but functional. |
| Payroll runs | early + #41 (pro-rata) + #42 (bonus integration) + #69 (void endpoint + expense-claim bundling) + #78 (engine approval) | — | 4 typecheck | Compute engine = load-bearing. See fragile areas. PR #69 added `POST /payroll-runs/:id/void` that reverses the JE (line-flip + `isReversed=true`), releases commission earnings, salary revisions, loan EMIs, and expense-claim reimbursements. Also wired expense-claim payroll disbursement into compute: draft-time load + REIMBURSE/REIMBURSE_TAX emission + atomic claim stamp; post-time per-category DR lines subtracted from wages. PR #78: engine routing — when a `document_type='payroll_run'` policy matches at post time, the run parks with `approval_request_id` and goes through the generic `/approvals` queue. Shared `postPayrollRunCore` serves both immediate and engine-finalised paths; `allowStatuses` guards against double-posting. Engine-owned runs refuse `/post` with 409 `ENGINE_OWNED`. Idempotent on `runNumber` if finaliser retries. See fragile areas §3 (dual-path payroll + bonus run posting). |
| Leave types + requests | mid | — | 0 | Stable. |
| Statutory filings | mid | — | 2 typecheck | EPF/ETF/PAYE remit. |
| Salary revisions (arrears) | late | — | 0 | Period-lock enforced on revision dates. |
| Staff loans | late | — | 0 | Atomically claimed at draft. |
| Mid-period payroll events | #41 | — | 0 | Pro-rata engine. |
| Bonus schemes | #42 | — | 0 | Just shipped. |
| Bonus runs | #42 + #78 (engine approval) | — | 0 | Draft → posted via `postBonusRunCore`. PR #78: engine routing — when a `document_type='bonus_run'` policy matches at post time, the run parks with `approval_request_id` and goes through `/approvals`. Threshold-gated via `minAmountCents`, same shape as bills. Engine-owned runs refuse `/post` with 409 `ENGINE_OWNED`. Reject + cancel flip back to draft + stamp reason into notes. See fragile areas §3 (dual-path payroll + bonus run posting). |
| Final settlements | #57 + #79 (engine approval) | — | 0 | Gross-to-net exit calc. Waives pending loan schedules on post. FS-series numbers. Partial unique index enforces one active settlement per employee. PR #79: engine routing — when a `document_type='final_settlement'` policy matches at approve time, the settlement parks with `approval_request_id` and goes through `/approvals`. Shared `approveFinalSettlementCore` serves both immediate and engine-finalised paths; `allowStatuses` guards against concurrent approvals. Engine-owned settlements refuse `/approve` with 409 `ENGINE_OWNED`. Posting to the ledger stays as a separate step — engine only gates the approve transition. See fragile areas §3 (dual-path final settlement approval). |
| Expense claims | #60 + #69 (payroll-compute wired) + #75 (approval engine wired) | — | 0 | Five-state lifecycle, SOD-enforced, category → GL expense account mapping. Direct-pay path posts DR expense / CR bank. Payroll-bundling path (PR #69): payroll-compute loads approved rows with `applied_in_run_id IS NULL`, emits REIMBURSE / REIMBURSE_TAX earnings per employee by `is_taxable`, atomically stamps the applied columns at draft. Post books per-category DR lines (grouped by `expense_account_id`, falling back to wages when null) and subtracts reimburse total from Salaries expense. Payroll void releases the claim. EXP-series numbers. Category snapshot on submit. PR #75: engine routing — when a `document_type='expense_claim'` policy matches at submit time, the claim parks with `approval_request_id` and goes through the generic `/approvals` queue. Legacy `/approve` + `/reject` refuse engine-owned claims with 409 `ENGINE_OWNED`; `/approve-and-pay` accepts both legacy (`submitted`) and engine-approved (`approved`) states so payment can happen after the engine approves without re-stamping approval metadata. Void on an engine-owned claim cancels the open request. See fragile areas §3 (dual-path expense-claim submission). |
| Commissions engine | #68 + #69 (cumulative-cap clawback + memo + net-negative flag) | — | 0 | Tiered rules, on-invoice vs on-collection recognition. Accrues DR 5170 / CR 2260 at invoice post. Clawback on credit-note post reverses pro-rata, capped cumulatively so repeat CNs can't claw >100% of the original earning. `/commissions/ledger` sorts accrued ASC (negatives first) and flags `netNegative` for stuck balances. Payroll bundling via `applied_in_run_id`; released on payroll void. |
| Reports (TB, P&L, BS, GL, VAT, cash flow) | mid | — | 0 | Read-only, low risk. |
| Reports (dashboard) | mid | — | 4 typecheck | Aging bucket label union mismatch. |
| AR aging / AP aging / 3-way match | mid | — | 0 | Stable. |
| Notifications | mid + #63 (prefs) + #70 (digest windows) | — | 0 | In-app bell + per-kind preferences + daily / weekly digest emails. `emitNotification()` branches on `notification_preferences.cadence`: immediate → bell, daily/weekly → `notification_digest_queue`. Hourly `send-notification-digests` cron coalesces per user at tenant-local 08:00 (Monday-only for weekly), logs every attempt to `notification_digest_emails` for dedupe (20h daily / 6d weekly lookback). Failed sends stay queued. Switching cadence flushes any pending queue rows for that kind. |
| Customer portal | #71 + #72 (hardening) | — | 0 | Email+OTP self-serve. Separate realm from admin app (different cookie + Redis prefix + path-scoped `/portal` + distinct onRequest hook). OTP is 6-digit, SHA-256 hashed at rest, 10-min expiry, 5-per-email-per-hour request limit + (PR #72) 10-per-email-per-hour verify limit so the code space can't be brute-forced inside the 10-min window. Cross-tenant enumeration-safe: always returns 200 at request-otp, mints one OTP per matching (tenant, customer) with the same plaintext code, and verify handles ambiguity via 409 `AMBIGUOUS_TENANT`. All reads double-scoped (`withTenant()` + explicit `customer_id =` predicate). Drafts return 404. PDF download reuses `InvoicePDF` renderer via portal-scoped route. PR #72 adds `customers.portal_enabled` (default true) — toggling via `PATCH /customers/:id/portal-access` revokes both pre-auth login (`portal_find_customers_by_email` filters it out) and any live session (`portal_resolve_session` returns `customer_active=false`, so `/portal/auth/me` destroys the session on the next hit). `portal.login` / `portal.logout` / `portal.access_toggled` now stream into `audit_events`. v1 is read-only — pay-online deferred. |
| Marketing site | ongoing | — | 8 typecheck | Tuple type drift. Doesn't affect app. |

---

## 6. Current environment drift (things to know)

Session-start checks that aren't bugs but will surprise you:

- **No CI gate exists yet.** `pnpm typecheck` is a local concern; broken typechecks won't fail a merge. If you want to raise the floor, a CI workflow that fails on **new** errors (baseline-delta, not total count) would be high-value. Not yet built.
- **No automated tests exist.** There is no `pnpm test` target that runs meaningfully. Every "test plan" in a PR is manual. Treat this as a known constraint, not a bug.
- **No lint on commit.** Prettier / ESLint exist but aren't gated. Style drift is low-priority but ambient.
- **`packages/db/drizzle.config.ts`** is outside the `rootDir` — see typecheck debt. Doesn't affect runtime, does affect `pnpm -r typecheck` exit code.

---

## 7. Update rules

Every PR should:

1. **Check §2 before blaming your own code** for a typecheck error.
2. **Add to §1** if you found (or caused) a new bug that isn't landing in this PR.
3. **Update §3** if you discovered something fragile, or if your PR hardened something (then remove / downgrade the entry).
4. **Add to §4** if a previously-shipped bug was fixed or a post-merge regression was caught.
5. **Bump `Last PR` in §5** for every module you touched substantively.
6. **Bump `Last updated`** at the top.

Don't be precious — stale is worse than terse. A one-line entry today beats a detailed one next month.
